# 🤖 AI Collaboration Note (`ai_note.md`)

เอกสารนี้จัดทำขึ้นเพื่อให้ AI และนักพัฒนาทำงานร่วมกันอย่างต่อเนื่องผ่านหลายเครื่องคอมพิวเตอร์

---

## 📌 1. ข้อมูลโดเมนและสภาพแวดล้อม (Environment Architecture)

* **Dev (สภาพแวดล้อมทดสอบ)**:
  * URL: `https://dev-ptnedinfo.timevela.com`
  * Git Branch: `develop`
  * Plesk Path: `/dev-ptnedinfo.timevela.com` (หรือ path ที่กำหนดบน Plesk)

* **Production (สภาพแวดล้อมใช้งานจริง)**:
  * URL: `https://ptnedinfo.timevela.com`
  * Git Branch: `main`
  * Plesk Path: `/httpdocs` หรือ `/ptnedinfo.timevela.com`

---

## ☁️ 2. ข้อมูลการเชื่อมต่อ Hosting & GitHub

* **Hosting Provider**: HostAtom (จัดการด้วย **Plesk Control Panel**)
* **GitHub Repository**: `https://github.com/astportal/ptnedinfodev.git`
* **GitHub Username**: `astportal` / `aungkiko`
* **Deployment Mechanism**: 
  * ใช้ **Plesk Git Extension** + **Webhook** ในการ Auto Pull โค้ดลง Server ทันทีที่มีการ Push

---

## 🔄 3. สายการพัฒนาและข้อตกลง Git Workflow

1. **การพัฒนาและทดสอบ**:
   * โค้ดฟีเจอร์ใหม่หรือการแก้ไข ให้ทำบน branch **`develop`**
   * คำสั่ง Push ไปยัง Dev:
     ```bash
     git checkout develop
     git add .
     git commit -m "feat/fix: อธิบายสิ่งที่แก้ไข"
     git push origin develop
     ```
   * ➔ Plesk จะดึงโค้ดไปแสดงผลที่ **`dev-ptnedinfo.timevela.com`** ทันที

2. **การนำขึ้นระบบจริง (Production)**:
   * เมื่อทดสอบระบบบน Dev ผ่านเรียบร้อยแล้ว ให้ทำการ Merge เข้า branch **`main`**:
     ```bash
     git checkout main
     git merge develop
     git push origin main
     ```
   * ➔ Plesk จะดึงโค้ดไปแสดงผลที่ **`ptnedinfo.timevela.com`** (เว็บจริง) ทันที

---

## 📝 4. บันทึกโครงสร้างเทคโนโลยี & โน้ตเพิ่มเติมสำหรับ AI

* **Tech Stack**: PHP ล้วน (ไม่ใช้ framework, ไม่ใช้ Composer) + MySQL — จงใจเลือกแบบนี้เพราะ hosting
  ทั่วไปรองรับได้แน่นอนโดยไม่ต้องพึ่ง dependency manager ใช้แค่ `ZipArchive` + `DOMDocument`
  (built-in ของ PHP) ในการอ่านไฟล์ .xlsx เอง
* **ข้อควรระวังเรื่องความปลอดภัย**: ห้าม Commit ไฟล์ที่มี API Keys หรือ Passwords ลงใน GitHub Repository โดยเด็ดขาด ให้ใช้ `.env` หรือสภาพแวดล้อมเฉพาะ
  * ไฟล์ `config.php` (มีรหัสผ่านฐานข้อมูลจริง) ถูกกันไว้ใน `.gitignore` แล้ว — ต้องอัปโหลดขึ้นเซิร์ฟเวอร์
    แยกต่างหากผ่าน Plesk File Manager เสมอ ไม่ใช่ผ่าน git

---

## 🗂️ 5. ระบบรวบรวมข้อมูลจากแบบฟอร์ม Excel — สรุปสถาปัตยกรรมสำหรับ AI ที่มาทำงานต่อ

### ภาพรวม
ระบบนี้ให้ผู้ดูแล (แอดมิน) อัปโหลดไฟล์ Excel ที่หน่วยงานต่าง ๆ กรอกและส่งกลับมา (ต้นฉบับอยู่ใน
`exelform/`, ไม่ commit ขึ้น git) แล้วรวบรวมเป็นฐานข้อมูลเดียว ดู/แก้ไข/ลบ/ส่งออกได้ผ่านเว็บ
ไฟล์ต้นฉบับมี **16 ไฟล์ 55 ชีท** โครงสร้างตารางเป็นแบบ double/triple header เกือบทุกชีทและ
แตกต่างกันมาก จึงออกแบบเป็น **engine กลางแบบ generic** แทนการ hand-map ทีละคอลัมน์
ปัจจุบันรองรับ **15 ไฟล์ / 49 ชีท** (ไฟล์ 15 ถูกตัดออกตามคำขอผู้ใช้งาน — ดูหัวข้อ "ชีทที่ไม่รองรับ")

### ไฟล์หลักที่ต้องรู้จัก
* `forms/registry.php` — **จุดเดียวที่ต้องแก้เพื่อเพิ่ม/ปรับฟอร์ม** มี comment อธิบาย key ทุกตัว
  ไว้ในไฟล์แล้ว (header_rows, skip_rows, identity_cols, identity_fields, value_type, value_label,
  value_split_last, carry_identity_fields)
* `src/XlsxReader.php` — อ่าน .xlsx ด้วย ZipArchive+DOM ล้วน คืนค่าเป็น grid พร้อม merge-fill
  เซลล์ที่ถูก merge จริงในไฟล์ Excel และรายการแถว/คอลัมน์ที่ถูกซ่อนไว้ (hiddenRows/hiddenCols)
* `src/Importer.php` — ตัวนำเข้าข้อมูลแบบ generic: ต่อ header หลายแถวเป็น "column_path" เดียว
  ต่อคอลัมน์ (เช่น `ข้าราชการ / ชาย`), ตรวจสอบค่าตัวเลข (`-`→0, `/`→1), ตรวจจับ/ข้ามแถวตัวอย่าง
  จากแม่แบบเปล่า, ข้ามแถว/คอลัมน์ที่ถูกซ่อนไม่นำเข้า, ตรวจโครงสร้างคอลัมน์เทียบกับต้นฉบับก่อน
  นำเข้าทุกครั้ง (บล็อกถ้าไม่ตรง), จัดการ upsert เมื่ออัปโหลดไฟล์ซ้ำ
* `reference_templates/` — ไฟล์แม่แบบเปล่าต้นฉบับ (commit ขึ้น git จริง ไม่เหมือน `exelform/`)
  ใช้เทียบโครงสร้างคอลัมน์กับไฟล์ที่อัปโหลดทุกครั้งก่อนนำเข้า ต้องมีไฟล์ตรงกับค่า `source_file`
  ของทุกฟอร์มใน registry.php — ถ้าเพิ่มฟอร์มใหม่ต้องคัดลอกไฟล์ต้นฉบับมาไว้ที่นี่ด้วย ไม่งั้นจะ
  ข้ามการตรวจสอบโครงสร้างไปเงียบ ๆ (ไม่ error แต่ก็ไม่ได้ป้องกันอะไร)
* `src/Reporting.php` — ประกอบข้อมูลกลับเป็นตาราง 2 แบบ: `pivot()` (ตารางกว้างเหมือนไฟล์ต้นฉบับ
  สำหรับหน้าดูข้อมูล/export.php) และ `tidyRows()` (ตารางเรียงยาว 1 แถว/1 ค่า สำหรับทำ Excel
  PivotTable ผ่าน export_tidy.php)
* หน้าเว็บ: `index.php` (แดชบอร์ด — จัดกลุ่มแถวตามฟอร์มด้วย rowspan), `upload.php`,
  `view.php` (ดู/ลบ/แก้ไขรายแถว), `edit_submission.php`, `uploads_history.php`,
  `review.php` (ค่าที่ต้องตรวจสอบ), `export.php`/`export_tidy.php`

### DB schema (ดูรายละเอียดเต็มใน `migrations/schema.sql`, `002_needs_review.sql`,
`003_school_code_check.sql`, `004_academic_year.sql` — ต้องรันตามลำดับเลข ทุกไฟล์)
เก็บข้อมูลแบบ **generic key-value** ไม่ใช่ 1 ตารางต่อ 1 ฟอร์ม:
* `uploads` — ไฟล์ที่อัปโหลดแต่ละครั้ง มี `academic_year` (จาก migration 004)
* `submissions` — 1 แถว = 1 หน่วยงาน/สถานศึกษาต่อฟอร์ม/ชีท/**ปีการศึกษา** มีฟิลด์มาตรฐาน (seq_no,
  school_code, school_code_issue, agency_name, school_name, amphoe, tambon, academic_year) +
  `extra_identity` (JSON) สำหรับฟิลด์ที่ไม่เข้าชุดมาตรฐาน (เช่น "age_group" ของฟอร์ม 13,
  "center_type"/"center_source" ของฟอร์ม 14) — ค่าพวกนี้จะไม่โผล่เป็นคอลัมน์จริงในตาราง แต่
  `Reporting`/`view.php`/`export*.php` จะดึงมาแสดงเป็นคอลัมน์เสริมให้อัตโนมัติ
* `submission_values` — ค่ารายคอลัมน์ (column_path => value) + `needs_review` flag
* `schools_master` — ทำเนียบโรงเรียนอ้างอิง เก็บแยกตามปี (คีย์หลัก `(academic_year, school_code)`)
* `app_settings` — ค่าตั้งค่าระบบแบบ key-value ทั่วไป (ตอนนี้มีแค่ `current_academic_year`)

### วิธีเพิ่มฟอร์มใหม่ (ทำตามนี้ทุกครั้ง)
1. ใช้ `unzip`/`ZipArchive` ดู `xl/workbook.xml` หาชื่อชีทที่แน่นอน (ระวัง: ชื่อชีทที่มี `&` จะถูก
   escape เป็น `&amp;` ในไฟล์ XML ดิบ ต้อง decode กลับเป็น `&` จริงก่อนใส่ใน registry.php)
2. อ่านโครงสร้าง header: มีกี่แถว (รวมแถวชื่อตาราง/แถวว่างคั่น), คอลัมน์แรกกี่คอลัมน์เป็นข้อมูล
   ระบุตัวตน (ระวัง: บางฟอร์มลำดับคอลัมน์ไม่เหมือนกัน เช่น ฟอร์ม 12 ตำบลมาก่อนอำเภอ, ฟอร์ม 15
   (ถ้ากลับมาทำ) ไม่มีคอลัมน์สังกัด/ลำดับที่เลย)
3. เพิ่ม entry ใน `forms/registry.php` — ไม่ต้อง map ทีละคอลัมน์ตัวเลข เพราะ Importer รวม header
   หลายแถวเป็น path อัตโนมัติ
4. ถ้า header มีแถวที่ซ้ำซ้อน/ไม่ช่วยแยกแยะ (เช่น "จำนวนนักเรียน" ซ้ำทุกคอลัมน์) ใช้ `skip_rows`
   ตัดออกได้ — ดูตัวอย่างในฟอร์ม 3, 5.3, 8.2, 9.3-9.5, 10.3, 10.4

### จุดที่พลาดมาก่อน (สำคัญ อย่าทำผิดซ้ำ)
* **การส่งออกแบบ pivot ต้องรวม header ทุกระดับเป็น "คอลัมน์เดียว"** ไม่ใช่แยกเป็นหลายคอลัมน์
  (ระดับ/ชั้นปี/ฯลฯ) — เคยลองแยกแล้วผู้ใช้ไม่ชอบ เพราะแต่ละกลุ่มในชีทเดียวกันมีความลึกของ header
  ไม่เท่ากันจริง (เช่น "ประถมศึกษา/ปีที่ 1" มี 2 ระดับ แต่ "มัธยมต้น/มัธยมศึกษา/ปี 1" มี 3 ระดับ)
  แยกคอลัมน์ตายตัวจะทำให้ข้อมูลผิดตำแหน่งข้ามกลุ่ม ให้ใช้ `value_label` (คอลัมน์เดียว) และ
  `value_split_last` (ถ้ามีมิติอิสระจริง เช่น เพศ ถึงจะแยกออกมาต่างหาก) — ดู `Reporting::joinLabel`
  และ `Reporting::tidyRows`
* **การเติมข้อความ header ที่ว่าง (merge แบบไม่ได้ merge จริงในไฟล์) ต้อง "หยุด" ทันทีที่แถวตื้น
  กว่าเปลี่ยนกลุ่ม** ไม่งั้นจะลากค่าจากกลุ่มก่อนหน้าที่ไม่เกี่ยวข้องมาใส่ผิด (เช่น คอลัมน์ว่างติดกับ
  คอลัมน์ "รวม" เดี่ยว ๆ) — ดู scoped-carry logic ใน `Importer::importSheet` (ตัวแปร `$scopeBroken`)
* **คีย์ upsert ตอนอัปโหลดไฟล์ซ้ำ**: ถ้าไม่มี school_code ต้องจับคู่ agency_name + school_name +
  extra_identity ร่วมกันแบบ NULL-safe (`<=>` ใน MySQL) ห้ามใช้ agency_name อย่างเดียว เพราะฟอร์มที่
  มีหลายแถวต่อหน่วยงานเดียวกัน (เช่น 1 อปท. มีหลายศูนย์เด็กเล็ก, 1 หน่วยงานมีหลายช่วงอายุ) จะลบ
  แถวก่อนหน้าทิ้งระหว่างนำเข้าไฟล์เดียวกัน — ดู `Importer::deleteExistingSubmission`
* **แถวตัวอย่างในแม่แบบ**: ปกติตรวจจับด้วย pattern `...`, `10 หลัก`, `(ตัวอย่าง)`, `หมายเหตุ`
  (ดู `Importer::isRealDataRow`) แต่ไฟล์ 15 (ตอนที่เคยพิจารณา) มีข้อมูลตัวอย่างจริงของ จ.ปัตตานี
  ฝังในแม่แบบ ไม่ใช่ placeholder ทั่วไป ทำให้ตรวจจับไม่ได้ — เป็นเหตุผลหนึ่งที่ไม่รองรับไฟล์นั้น
* **การซ่อนแถว/คอลัมน์ กับการตรวจโครงสร้างคอลัมน์ต้องแยกกันคนละเรื่อง**: ตอนแรกทำให้ column_path
  ข้ามคอลัมน์ที่ถูกซ่อนไปเลยตอนสร้าง path ผลคือคอลัมน์ที่ถูกซ่อนจะไปทำให้ผลเทียบโครงสร้างกับ
  reference_templates/ ผิดพลาด (นับจำนวนคอลัมน์ไม่ตรงทั้งที่จริง ๆ โครงสร้างเดิมทุกอย่าง แค่ซ่อนไว้)
  แก้โดยแยก 2 หน้าที่ออกจากกัน: `buildColumnPaths()` สร้าง path ครบทุกคอลัมน์เสมอ (ใช้เทียบ
  โครงสร้าง) ส่วนการข้าม "ไม่นำเข้าข้อมูล" ของคอลัมน์ที่ถูกซ่อน ย้ายไปทำตอน insert ค่าจริงแทน
* **บล็อกหมายเหตุใต้ตารางมักมีหลายแถว แต่ขึ้นต้นด้วย "หมายเหตุ" แค่แถวแรกแถวเดียว**: ของเดิมเช็ก
  ทีละแถวว่าขึ้นต้นด้วย "หมายเหตุ" หรือไม่ ทำให้แถวอธิบาย/รายการย่อยถัดไป (เช่น "(1) ครูที่
  ปฏิบัติการสอน หมายถึง...") หลุดเข้าไปเป็นข้อมูลปลอมในระบบ (พบในฟอร์ม 2, 3, 10 ทุกชีท) แก้โดยพอ
  เจอแถวที่ขึ้นต้นด้วย "หมายเหตุ" ให้ `break` ออกจากลูปนำเข้าทั้งชีททันที (ถือว่าตั้งแต่จุดนั้นถึง
  ท้ายชีทเป็นคำอธิบายทั้งหมด ไม่ใช่ข้อมูล) — ดู `Importer::isNoteMarkerRow`

### ชีทที่ "ไม่รองรับ" ตอนนี้ (มี comment อธิบายไว้ใน registry.php แล้ว)
* **9.สรุป** (ในไฟล์ 9_ข้อมูลผู้สำเร็จการศึกษา.xlsx) — กรอกครั้งเดียวต่อหน่วยงาน มีช่อง
  "หน่วยงาน...." ให้พิมพ์เอง ไม่ใช่คอลัมน์ตาราง และมีแค่ 3 แถวคงที่ ไม่ใช่ 1 แถวต่อสถานศึกษา
* **13_จำนวนนักเรียนแยกรายชั้น.xlsx** — รองรับแล้ว (ต่างจากชีทข้างบนที่ยังไม่รองรับ) แต่มี
  โครงสร้างพิเศษ (1 หน่วยงานหลายแถว, ไม่มี school_code) ใช้ `carry_identity_fields` — ดูตัวอย่าง
  ใน registry.php ถ้าจะทำฟอร์มอื่นที่มีรูปแบบคล้ายกัน (1 หน่วยงานกรอกหลายแถว)
* ไฟล์ 15 เคยไม่รองรับ (ตัดออกตามคำขอผู้ใช้งาน 2026-08-22) แต่**รองรับแล้วตั้งแต่ 2026-08-27**
  หลังผู้ใช้งานส่งไฟล์เวอร์ชันใหม่มา — ดูหัวข้อ "อัปเดตแบบฟอร์ม 10/13/14/15" ด้านล่าง

### การรวมหลายฟอร์มต้นฉบับเป็นชุดข้อมูลเดียว (merged form — เคยใช้กับฟอร์ม 14ก+14ข เดิม)
บางครั้งไฟล์ต้นฉบับ 2 ไฟล์แยกกันมีโครงสร้างคอลัมน์เหมือนกันทุกประการ (แค่สำรวจคนละกลุ่ม) และ
ผู้ใช้อยากให้ **นับ/ดู/ส่งออกรวมกันเป็นชุดเดียว** ไม่ใช่แค่โชว์คู่กันแยกชุด — กลไกนี้ยังอยู่ในระบบ
ใช้ได้กับฟอร์มอื่นในอนาคต แต่**ฟอร์ม 14 เลิกใช้แพทเทิร์นนี้แล้วตั้งแต่ 2026-08-27** (เปลี่ยนมาใช้ไฟล์
ต้นฉบับเดียว — ดูหัวข้อถัดไป) ตัวอย่างที่เหลืออยู่ในโค้ดตอนนี้จึงเป็นแค่กลไกทั่วไป ไม่ผูกกับฟอร์มไหน
โดยเฉพาะ — รูปแบบที่ใช้ตอนยังมี 2 ไฟล์:
* 1 `form_key` มี `sheets` หลายรายการ รายการละ 1 sheetDef ต่อไฟล์ต้นฉบับ 1 ไฟล์ แต่ละ sheetDef
  ประกาศ `source_file` ของตัวเอง (แทนที่จะประกาศที่ระดับฟอร์ม) และตั้ง `'optional' => true` เพื่อ
  บอกว่า "อัปโหลดไฟล์นี้ *หรือ* ไฟล์อื่นในกลุ่มก็ได้ ไม่ต้องมีครบทุกไฟล์พร้อมกัน" — ถ้าไม่ตั้ง
  `optional` ระบบจะรายงาน error "ไม่พบชีท" ทุกครั้งที่อัปโหลดไฟล์ใดไฟล์หนึ่งในกลุ่ม
* ทุก sheetDef ในกลุ่มตั้ง `db_sheet_name` เป็นค่าเดียวกัน — นี่คือชื่อที่ใช้เก็บจริงในคอลัมน์
  `sheet_name` ของตาราง `uploads`/`submissions` (แยกจาก `sheet_name` ซึ่งยังคงเป็นชื่อชีทจริงใน
  ไฟล์ Excel ใช้แค่หาไฟล์/เทียบโครงสร้างกับ reference_templates/) ทำให้ข้อมูลจากทั้งสองไฟล์ไป
  ตกอยู่ใน submissions bucket เดียวกัน หน้า view/export/dashboard จึงนับรวมกันอัตโนมัติ
* ใส่ `fixed_extra_identity => ['center_source' => '...']` (metadata คงที่ ไม่ได้อ่านจากคอลัมน์ใด)
  เพื่อให้ยังรู้ว่าแต่ละแถวมาจากไฟล์ต้นฉบับไหน โผล่เป็นคอลัมน์เสริมอัตโนมัติเหมือน extra_identity
  ทั่วไป (ผ่าน `extra_identity_label()` ใน bootstrap.php)
* **ผลกระทบที่ต้องแก้ทุกจุดที่ทำ `$sd['sheet_name'] === $sheetName`** (จับคู่ sheetDef จาก GET/DB):
  ต้องเปลี่ยนเป็น `($sd['db_sheet_name'] ?? $sd['sheet_name']) === $sheetName` เสมอ มิเช่นนั้นจะหา
  sheetDef ไม่เจอ — จุดที่ต้องแก้: `view.php`, `export_tidy.php`, `edit_submission.php`,
  ลิงก์ "ดูข้อมูลที่รวบรวม" ใน `upload.php`, และ dashboard (`index.php`) ต้อง dedupe แถวที่มี
  `db_sheet_name` ซ้ำกันด้วย ไม่งั้นจะโชว์ 2 แถวซ้ำสำหรับฟอร์มเดียวกัน

### ฟีเจอร์ตรวจสอบความถูกต้องของไฟล์ที่อัปโหลด (เพิ่มเมื่อ 2026-08-22)
1. **ซ่อนแถว/คอลัมน์ในไฟล์ Excel** → ข้อมูลจากแถว/คอลัมน์นั้นจะไม่ถูกนำเข้าเลย (อ่านจาก
   `hidden="1"` ใน `<row>`/`<col>` ของไฟล์จริง ไม่ใช่การเดา) แจ้งจำนวนที่ข้ามในข้อความผลอัปโหลด
2. **โครงสร้างคอลัมน์ไม่ตรงกับต้นฉบับ** (เพิ่ม/ลบ/แทรก/สลับคอลัมน์) → **บล็อกการนำเข้าทั้งชีทนั้น**
   ก่อนเขียนอะไรลงฐานข้อมูลเลย เทียบกับไฟล์ใน `reference_templates/` โดยใช้ฟังก์ชันสร้าง
   column_path ตัวเดียวกับตอนนำเข้าจริง (ไม่ได้พิมพ์ข้อมูลอ้างอิงเองด้วยมือ เพื่อกัน false
   positive จากการพิมพ์ผิด) — ดู `Importer::assertStructureMatches`
3. **รหัสสถานศึกษาว่างหรือไม่ตรงกับทำเนียบ** (เพิ่มเมื่อ 2026-08-22) → **ไม่บล็อก** แค่แปะ flag ไว้
   ให้ไปตรวจสอบทีหลัง (คนละแนวกับข้อ 2 ที่บล็อกทันที เพราะเรื่องนี้ไม่กระทบความเข้าใจข้อมูลของแถว
   นั้น แค่กันเผื่อพิมพ์รหัสผิด) — ดูรายละเอียดสถาปัตยกรรมในหัวข้อถัดไป

### การตรวจสอบรหัสสถานศึกษาเทียบทำเนียบโรงเรียน (เพิ่มเมื่อ 2026-08-22)
ผู้ใช้มีไฟล์ทำเนียบโรงเรียนทั้งจังหวัด (ตัวอย่าง: `exelform/school69.xlsx` — ไม่ใช่แบบฟอร์มสำรวจ
เป็นตารางแบนแถวเดียว 1 แถว/1 โรงเรียน คอลัมน์ header ภาษาอังกฤษ เช่น `SchoolCode`, `SchoolName`)
นำมาใช้เป็นข้อมูลอ้างอิงตรวจสอบรหัสสถานศึกษาที่กรอกมาในไฟล์อัปโหลดของฟอร์มอื่น ๆ:
* `migrations/003_school_code_check.sql` — สร้างตาราง `schools_master` (school_code เป็น PK) และ
  เพิ่มคอลัมน์ `submissions.school_code_issue` ENUM('missing','not_found') NULL (NULL = ไม่มีปัญหา)
* `schools_master.php` — หน้าอัปโหลดทำเนียบ (login แล้วเข้าได้ทุกคน ไม่มีระบบ role แยก) จับคู่
  คอลัมน์ด้วย**ชื่อ header แถวแรก** ไม่ใช่ตำแหน่ง เพราะไฟล์ปีอื่นอาจสลับลำดับคอลัมน์ อัปโหลดใหม่ =
  **แทนที่ทำเนียบทั้งชุด** (TRUNCATE แล้ว insert ใหม่) ไม่ใช่ upsert ทีละแถว เพราะถือเป็น snapshot
  ของปีการศึกษาปัจจุบัน ใช้ชีทแรกในไฟล์เสมอ (ชื่อชีทผูกกับปีการศึกษา เช่น "2569_School" เปลี่ยนทุกปี)
* `Importer::knownSchoolCodes()` cache รายการรหัสจาก `schools_master` ไว้ในเมมโมรีของ Importer
  instance เดียว (query ครั้งเดียวต่อการอัปโหลด 1 ไฟล์ ไม่ query ซ้ำทุกแถว) — **ถ้าทำเนียบยังว่าง
  เปล่า (ยังไม่เคยอัปโหลด) จะข้ามการตรวจสอบทั้งหมดไปเงียบ ๆ ไม่ flag อะไรเลย** เพราะเทียบกับ
  ทำเนียบว่างจะ flag ทุกแถวเป็น "ไม่พบ" ผิดความจริง — เจตนาให้ฟีเจอร์นี้ "เปิดใช้งานเอง" เมื่อมีคน
  อัปโหลดทำเนียบครั้งแรกเท่านั้น
* ตรวจเฉพาะฟอร์มที่มีคอลัมน์ `school_code` ใน `identity_fields` จริง (ฟอร์ม 1, 13, 14 ไม่มีคอลัมน์
  นี้ตั้งแต่ต้น ข้าม) ผลตรวจสอบ 2 แบบ: `missing` (ช่องว่าง) กับ `not_found` (มีค่าแต่ไม่ตรงทำเนียบ)
  ทั้งสองแบบ**ไม่บล็อกการนำเข้า** แค่บันทึกแถวลง DB ตามปกติแล้วแปะ flag ไว้ตรวจทีหลัง
* `review.php` มี section แยกต่างหาก "รหัสสถานศึกษาที่ต้องตรวจสอบ" คู่กับ section เดิม "ค่าตัวเลข
  ที่ไม่แน่ใจ" — แก้ไขแล้วบันทึกจะ set `school_code = ค่าใหม่` และเคลียร์ flag ทันทีโดยไม่ตรวจซ้ำ
  ว่าค่าใหม่ตรงทำเนียบจริงไหม (เชื่อการแก้ไขของแอดมิน เหมือน pattern เดิมของค่าตัวเลข)
* ทุก query ที่แตะ `schools_master`/`school_code_issue` ห่อด้วย try/catch แล้ว fallback เป็น
  "ไม่มีข้อมูล" เสมอ เพื่อไม่ให้หน้าเว็บพังถ้ายังไม่ได้รัน migration 003 บนเครื่อง server

### การรองรับหลายปีการศึกษา (เพิ่มเมื่อ 2026-08-23)
ระบบเก็บข้อมูลทุกปีการศึกษาไว้พร้อมกัน ไม่ทับกัน เลือกดูย้อนหลังได้ — สถาปัตยกรรมหลัก:
* **`migrations/004_academic_year.sql` ต้องรันก่อนใช้งาน ไม่ใช่ optional เหมือน 002/003** —
  ต่างจากฟีเจอร์ก่อนหน้าที่ออกแบบให้ fallback ได้ถ้ายังไม่ migrate เพราะฟีเจอร์นี้เปลี่ยนคีย์ upsert
  หลักของระบบ (เพิ่ม `academic_year` เข้าไปในคีย์อัปเดตทับข้อมูลเดิม) — ถ้ายังไม่รัน migration
  การอัปโหลดไฟล์ใด ๆ จะ error ทันที (INSERT ใส่คอลัมน์ที่ไม่มีอยู่จริง) ไม่ใช่แค่ฟีเจอร์เสริมหายไป
  หน้าที่อ่านอย่างเดียว (`index.php`, `view.php`, `uploads_history.php`) ยัง fallback ได้ (ซ่อน
  ตัวเลือกปีไปเงียบ ๆ) แต่การอัปโหลดจะพังจนกว่าจะรัน 004
* **`app_settings`** (key-value ทั่วไป) เก็บ `current_academic_year` — ปีที่จะแปะให้ทุกไฟล์ที่
  อัปโหลด**ใหม่**โดยอัตโนมัติ เพราะไฟล์ฟอร์มสำรวจ 1-15 **ไม่มีคอลัมน์ปีอยู่ในไฟล์เลย** (เช็กแล้ว
  หลายไฟล์) ต้องตั้งเป็นค่ากลางของระบบแทน เปลี่ยนได้ที่ `settings.php` — อ่าน/เขียนผ่าน class
  `Settings` (`src/Settings.php`, `Settings::currentAcademicYear($db)`)
* **`uploads`, `submissions` มีคอลัมน์ `academic_year`** ใส่ตอน import จากค่า current setting
  (ไม่ได้อ่านจากไฟล์) — คีย์ upsert ใน `Importer::deleteExistingSubmission` เพิ่ม `academic_year`
  เข้าไปด้วย ดังนั้นอัปโหลดไฟล์ปีใหม่จะ**ไม่ทับ**ข้อมูลปีเก่าของหน่วยงาน/โรงเรียนเดียวกันเด็ดขาด
  (อัปโหลดซ้ำในปีเดียวกันยังทับของเดิมตามปกติ)
* **`schools_master` เก็บแยกตามปีด้วย** (คีย์หลักเปลี่ยนจาก `school_code` เดี่ยว ๆ เป็นคู่
  `(academic_year, school_code)`) — ต่างจากฟอร์มสำรวจ ไฟล์ทำเนียบโรงเรียน**มีคอลัมน์ปีอยู่ในไฟล์จริง
  (`YearEdu`)** จึง**อ่านปีจากไฟล์อัตโนมัติ**ใน `schools_master.php` แทนที่จะใช้ current setting —
  ถ้าตรวจจับจากคอลัมน์ไม่ได้ มีช่องกรอกปีเองสำรองไว้ อัปโหลดใหม่จะแทนที่**เฉพาะทำเนียบของปีนั้น**
  (`DELETE ... WHERE academic_year = :y` ก่อน insert) ไม่ใช่ TRUNCATE ทั้งตารางเหมือนก่อนหน้านี้
  แล้ว `Importer::knownSchoolCodes(int $academicYear)` จะเทียบกับทำเนียบของปีเดียวกับที่กำลังนำเข้า
  เท่านั้น (cache แยกเป็น array ต่อปีในตัวแปร `$knownSchoolCodesByYear`)
* **ทุกจุดที่ query ข้อมูลรวม/ดู/ส่งออก** (`index.php`, `view.php`, `Reporting::pivot()`,
  `Reporting::tidyRows()`, `export.php`, `export_tidy.php`, `uploads_history.php`) รับ
  `academic_year` เป็น filter ทางเลือก (`null`/`'all'` = ทุกปีรวมกัน) — ตอนแสดงผลรวมหลายปี ต้อง
  โชว์คอลัมน์ "ปีการศึกษา" เพิ่มด้วยไม่งั้นแยกไม่ออกว่าแถวไหนของปีไหน (ดู `$showAllYears` ใน
  `view.php`/`export.php`/`export_tidy.php`)
* หน้า `index.php` (แดชบอร์ด) มี dropdown เลือกปี ค่า default = current setting — เปลี่ยนปีแล้ว
  ลิงก์ "ดูข้อมูล" ของทุกแถวจะพาไปที่ `view.php` พร้อม `&year=` เดียวกันอัตโนมัติ

### ตารางสรุปยอดรวมทั้งจังหวัดในหน้าดูข้อมูล (เพิ่มเมื่อ 2026-08-23)
บาง sheetDef (ตอนนี้มีแค่ฟอร์ม 13) อยากโชว์ตารางสรุปยอดรวมทั้งจังหวัด แยกตาม `extra_identity`
field หนึ่งตัว (เช่น ช่วงอายุ) อยู่เหนือตารางรายหน่วยงานเดิมใน `view.php`:
* เพิ่ม key `summary_group_field` (ชื่อฟิลด์ใน `extra_identity` ที่จะใช้แยกกลุ่ม) และ
  `summary_group_field_label` (label ที่แสดงบนหัวตาราง) ใน sheetDef — ดูตัวอย่างฟอร์ม 13
  (`age_group`) ใน `forms/registry.php`
* `view.php` จะรวมยอดทุกคอลัมน์ตัวเลขข้ามหน่วยงานทั้งหมด แยกเป็นแถวตามค่าของฟิลด์นั้น โดยใช้
  `$pivot['rows']` ชุดเดียวกับที่กรองปีการศึกษาไว้อยู่แล้ว (ไม่ query ซ้ำ) มีแถวรวมทั้งหมดท้ายตาราง
  (`$pivot['totals']`)
* เป็น key ที่เป็น optional — ไม่ตั้งไว้ก็ไม่มีผลอะไร (ตารางสรุปไม่แสดง) ถ้าจะทำฟอร์มอื่นที่มี
  แนวคิดคล้ายกัน (1 หน่วยงานหลายแถวแยกตาม extra_identity field) ก็เพิ่ม 2 key นี้ได้เลยไม่ต้องแก้
  `view.php`

### อัปเดตแบบฟอร์ม 10/13/14/15 (2026-08-27) — ผู้ใช้งานส่งไฟล์ต้นฉบับเวอร์ชันใหม่มา
ผู้ใช้งานแจ้งว่าฟอร์ม 10, 13, 14 มีการแก้ไข และฟอร์ม 15 เพิ่มเข้ามาใหม่ (ไฟล์ .xlsx จริงอยู่ใน
`exelform/` ซึ่งไม่ commit ขึ้น git — ต้องรอผู้ใช้งานบอกทุกครั้งว่าไฟล์ไหนอัปเดต หรือเทียบชื่อ/ขนาด
ไฟล์กับ `reference_templates/`+`source_file` ใน registry.php ดูเป็นสัญญาณเบื้องต้นได้ แต่ไม่แม่นยำ
100% เพราะจับได้แค่ชื่อ/ขนาดเปลี่ยน ไม่ใช่เนื้อหาข้างในเปลี่ยนแบบขนาดไฟล์เท่าเดิม)
* **ตัดสินใจสำคัญที่ถามผู้ใช้งานก่อนแก้**: registry.php ใช้ค่าเดียวกันกับข้อมูลทุกปีการศึกษา ไม่ได้
  แยกโครงสร้างตามปี — ถ้าแทนที่ sheetDef เดิมด้วยโครงสร้างใหม่ทั้งหมด ข้อมูลปีเก่าที่เคยนำเข้าด้วย
  โครงสร้างเดิมจะไม่โผล่ในหน้าเว็บอีกต่อไป (ยังอยู่ใน DB แต่ดึงมาแสดงไม่ได้เพราะไม่มี sheetDef เดิม
  ให้จับคู่ sheet_name แล้ว) — ผู้ใช้งานเลือก **"แทนที่ทั้งหมด ไม่ต้องห่วงข้อมูลเก่า"** จึงลบ
  sheetDef เดิมทิ้งตรง ๆ ไม่ได้ทำ versioning ตามปีการศึกษาให้ (ระบบยังไม่รองรับ point ทำแบบนั้น)
* **ฟอร์ม 10** — ปรับโครงสร้างใหม่ทั้งฟอร์ม จาก 5 ชีท (10.1-10.4) เป็น 6 ชีท (10.1-10.6): ชีท 10.3
  เดิม (แยก "ทุกสังกัดยกเว้นสาธิต&อุดม" กับ "เฉพาะสาธิต&อุดมฯ") ถูกยุบเป็น 10.3 เดียว (แยกตาม
  อันดับ/วิทยฐานะ) + แยก 10.4 ใหม่สำหรับสาย อว. โดยเฉพาะ (ตำแหน่งทางวิชาการ) + เพิ่ม 10.5 ใหม่
  (แยกวุฒิการศึกษา) + 10.4 เดิม (วิชาเอก) เลื่อนไปเป็น 10.6 — ไฟล์ต้นฉบับเปลี่ยนชื่อเป็น
  `10_ข้อมูลครูV2.xlsx` แล้ว
* **ฟอร์ม 13** — โครงสร้างเหมือนเดิมทุกอย่าง (skip_rows/header_rows/identity ไม่เปลี่ยน) แค่ไฟล์เก่า
  มีคอลัมน์ "ม.1-ม.3 ชาย/หญิง" ซ้ำกัน 2 ชุดโดยไม่ตั้งใจ (44 คอลัมน์) ไฟล์ใหม่ตัดชุดซ้ำออกเหลือ
  38 คอลัมน์ — ไม่ต้องแก้ registry.php เลย แค่แทนที่ reference_templates/ เท่านั้น
* **ฟอร์ม 14** — เลิกใช้แพทเทิร์น merged-form (14ก+14ข คนละไฟล์) เปลี่ยนมาใช้ไฟล์ต้นฉบับเดียว
  `14_ข้อมูลสถานพัฒนาเด็กปฐมวัย_V2.xlsx` แทน โครงสร้างคอลัมน์ระบุตัวตนเปลี่ยนเป็นมาตรฐาน
  (`$stdIdentity`) ด้วย ไม่มีคอลัมน์ "เป็นศูนย์ถ่ายโอน/ตั้งเอง" (center_type) อีกต่อไป จึงไม่ต้องใช้
  extra_identity/fixed_extra_identity แบบเดิมแล้ว — **แต่ยังต้องใช้ db_sheet_name/optional อยู่**
  (ดูหัวข้อถัดไป ปรับกลับมาใช้อีกครั้งเมื่อ 2026-08-29 ด้วยเหตุผลคนละเรื่องกับตอนที่มี 2 ไฟล์เดิม)
* **ฟอร์ม 14 — ชื่อแท็บชีทเก่ายังหลุดมาในไฟล์จริง (เพิ่มเมื่อ 2026-08-29)** หลังเปลี่ยนมาใช้ไฟล์
  เดียวข้างบน พบว่าหลายหน่วยงานยังส่งไฟล์ที่**เนื้อหาข้างในเป็นโครงสร้างใหม่แล้ว** (identity columns
  ตรงกับ `$stdIdentity` ทุกตัว) **แต่ชื่อแท็บชีท (sheet tab) ยังเป็น "14.สถานพัฒนาเด็กปฐมวัย"**
  ชื่อเดิมจากไฟล์ 14ข เก่า (ไม่ใช่ "14.ข้อมูลศูนย์พัฒนาเด็กเล็ก" ตามไฟล์ต้นฉบับใหม่) — น่าจะเพราะ
  หน่วยงานแก้ไขเนื้อหาในไฟล์เดิมที่เคยมีอยู่แทนที่จะโหลดไฟล์ต้นฉบับใหม่มาทั้งไฟล์ ตรวจสอบไฟล์จริง
  จากหลายหน่วยงานแล้วพบทั้ง 2 แบบปนกันอยู่ (บางไฟล์ใช้ชื่อแท็บใหม่ บางไฟล์ใช้ชื่อเก่า) จึงกลับมาใช้
  แพทเทิร์น merged-form (`db_sheet_name`/`optional`) อีกครั้ง — คราวนี้ไม่ใช่เพราะไฟล์ต้นฉบับ 2 ไฟล์
  แยกกัน (เหลือไฟล์เดียว) แต่เพราะ**ชื่อแท็บชีทภายในไฟล์เดียวกันมี 2 ชื่อที่เป็นไปได้** — ทั้งสอง
  sheetDef ต้องตั้ง `optional => true` ทั้งคู่ (ไม่ใช่แค่อันที่ 2) ไม่งั้นจะมี error message ปลอม
  "ไม่พบชีทที่ชื่อ..." โผล่มาคู่กับผลสำเร็จเสมอ (ดู `Importer::importFile` เช็กทีละ sheetDef อิสระ
  กัน) — และต้องเพิ่มชีท "14.สถานพัฒนาเด็กปฐมวัย" (คัดลอกโครงสร้างเดียวกัน ไม่มีข้อมูลจริง) เข้าไป
  ใน `reference_templates/14_ข้อมูลสถานพัฒนาเด็กปฐมวัย_V2.xlsx` ด้วย ไม่งั้นการตรวจสอบโครงสร้างจะ
  ถูกข้ามไปเงียบ ๆ สำหรับไฟล์ที่ใช้ชื่อแท็บนี้ (แก้ไฟล์ .xlsx ตรง ๆ ด้วย python ผ่าน zipfile — เพิ่ม
  `<sheet>` ใน workbook.xml, relationship ใน workbook.xml.rels, Override ใน [Content_Types].xml,
  แล้วคัดลอก worksheets/sheet1.xml เป็น sheet2.xml)
* **ฟอร์ม 15** — เพิ่มเข้ามาใหม่ ครบทั้ง 5 ชีท (15.1-15.3 + 2 ชีทสุดท้ายที่เปลี่ยนชื่อเป็น
  "สช.วิชาชีพ" กับ "สช.วิชาชีพ-ครู-นร.") — ปัญหาเดิมที่เคยทำให้ไม่รองรับ (แถวหัวข้อคั่นหมวดปนกับ
  ข้อมูลจริง + มีข้อมูลตัวอย่างจริงฝังในแม่แบบ) **ตรวจสอบแล้วไม่มีในไฟล์เวอร์ชันใหม่** เป็นแม่แบบว่าง
  สะอาดปกติ จึงรองรับได้ครบ:
  * ชีท "สช.วิชาชีพ" ไม่มีคอลัมน์ตัวเลขเลย (เหมือนฟอร์ม 12 — ทุกคอลัมน์เป็น identity_fields ทั้งหมด)
  * ชีท "สช.วิชาชีพ-ครู-นร." มีมิติสุดท้ายเป็น ชาย/หญิง/**รวม** (ไม่ใช่แค่ 2 เพศ) — ตั้งใจไม่ใช้
    `value_split_last` เพราะ "รวม" เป็นผลรวมที่คำนวณแล้ว ไม่ใช่มิติอิสระจริง (ตามหลักการใน "จุดที่
    พลาดมาก่อน" ด้านบน)
  * 15.1-15.3 มีคอลัมน์ identity ที่ไม่ใช่ field มาตรฐาน (ที่อยู่, รหัสไปรษณีย์, ผู้รับใบอนุญาต,
    ผู้จัดการ, ครูใหญ่, โต๊ะครู, ขนาดสถาบันศึกษาปอเนาะ ฯลฯ) ไปเก็บใน extra_identity ตามปกติ — เพิ่ม
    label ภาษาไทยให้ครบใน `extra_identity_label()` (bootstrap.php) แล้ว รวมถึง `admin_name`/`phone`
    ที่ฟอร์ม 1 เคยใช้แต่ไม่เคยมี label มาก่อน (ช่องโหว่เดิม แก้พร้อมกันไปด้วย)
* เครื่องมือช่วยตรวจ header ระหว่างทำงานนี้: เขียน python script อ่าน .xlsx ตรงด้วย
  `zipfile`+`xml.etree.ElementTree` (คล้าย `src/XlsxReader.php` แต่เป็น python) ใช้แทน Perl ที่เคย
  ใช้ก่อนหน้านี้ — ไม่ได้เก็บสคริปต์ไว้ใน repo (อยู่ใน scratchpad ของ session)

### แม่แบบเปล่ากับไฟล์จริงที่หน่วยงานส่งกลับมาไม่เหมือนกันเป๊ะเสมอไป (เจอซ้ำหลายครั้ง 2026-08-27/29)
บทเรียนสำคัญจากการแก้บั๊ก "อัปโหลดไม่ผ่าน" หลายรอบ: **`exelform/`/`reference_templates/` ที่ AI
ใช้วิเคราะห์คือไฟล์แม่แบบเปล่า ไม่ใช่ไฟล์ที่หน่วยงานจริงส่งกลับมา** — เจอ 2 รูปแบบปัญหาที่ต่างกัน:
1. **คอลัมน์ผีท้ายไฟล์** (ฟอร์ม 10.1) — ไฟล์จริงมีคอลัมน์ว่างเปล่าสนิท (ไม่มีทั้ง header/data) ต่อท้าย
   เกินจากแม่แบบ เพราะมีคนเลือก/จัดรูปแบบเซลล์เกินขอบเขตข้อมูลจริงตอนแก้ไฟล์ ทำให้ Excel นับเป็น
   "used range" เกิน — แก้ที่ `XlsxReader::readGrid()` ให้ตัดคอลัมน์ท้ายที่ไม่มีค่าในทุกแถวออกก่อน
   คืนค่า `maxCol` เสมอ (ไม่ใช่แก้เฉพาะฟอร์ม 10 — เป็น fix ระดับ reader กลาง ป้องกันทุกฟอร์ม)
2. **ชื่อแท็บชีทไม่ตรงกับแม่แบบใหม่** (ฟอร์ม 14) — หน่วยงานแก้ไขเนื้อหาในไฟล์เดิมที่เคยมีอยู่ให้ตรง
   โครงสร้างใหม่ แต่ไม่ได้เปลี่ยนชื่อแท็บชีทตาม — ดูหัวข้อ "ฟอร์ม 14 — ชื่อแท็บชีทเก่า" ด้านบน
**ข้อคิดสำหรับ AI ครั้งต่อไป**: เวลาผู้ใช้งานแจ้งว่า "อัปโหลดไม่ผ่าน" **ต้องขอไฟล์จริงที่ใช้อัปโหลด
มาตรวจ อย่าเดาจากแม่แบบเปล่าอย่างเดียว** เพราะไฟล์จริงอาจมี edge case ที่แม่แบบไม่มี (คอลัมน์ผี,
ชื่อแท็บเพี้ยน, ฯลฯ) ไฟล์จริงของหน่วยงานต่าง ๆ มักอยู่ใต้โฟลเดอร์ `EDU-ปัตตานี/.../ตอบกลับ/` (คนละ
ที่กับ `exelform/`) — ถ้าผู้ใช้งานให้ path มาแค่ไฟล์เดียว แนะนำเช็คไฟล์อื่นในโฟลเดอร์ `ตอบกลับ` ที่
เป็นฟอร์มเดียวกันเพิ่มด้วย (เช่นหาไฟล์จากหน่วยงานอื่น ๆ) เพื่อดูว่าปัญหาที่เจอเป็นเคสเดียวหรือเป็น
รูปแบบที่เกิดซ้ำหลายหน่วยงาน — ถ้าซ้ำหลายหน่วยงานควรแก้ที่ registry.php/โค้ดกลางให้รองรับไปเลย
ไม่ใช่ไปบอกให้หน่วยงานเดียวแก้ไฟล์ใหม่ (ไฟล์จากหน่วยงานอื่น ๆ ที่ยังไม่ upload ก็จะมีปัญหาเดียวกัน)

### seq_no (ลำดับที่) เรียงใหม่เองเสมอ ไม่ใช้ค่าที่อัปโหลดมา (เพิ่มเมื่อ 2026-08-28)
ค่า "ลำดับที่" ที่หน่วยงานพิมพ์เองในไฟล์ไม่น่าเชื่อถือ (ไม่ต่อเนื่อง ซ้ำกันได้ โดยเฉพาะพอรวมข้อมูล
จากหลายไฟล์/หลายหน่วยงานเข้าด้วยกัน) — `Reporting::pivot()`/`tidyRows()` (จุดเดียวที่ป้อนข้อมูลให้
`view.php`/`export.php`/`export_tidy.php` ทั้งหมด) **เรียงเลข 1,2,3... เองเสมอ** ตามลำดับที่
แสดงผลจริง แทนค่า `seq_no` ที่เก็บไว้ใน DB จากไฟล์อัปโหลด — ค่าเดิมใน DB ไม่ถูกแก้ไข แค่ไม่เอามา
แสดงผลอีกต่อไป (หน้า `edit_submission.php` ยังแก้ไขค่านี้ได้แต่จะไม่มีผลต่อการแสดงผลแล้ว) — ใน
`tidyRows()` (1 submission ออกได้หลายแถว) ทุกแถวของ submission เดียวกันใช้เลขเดียวกัน

### แทนที่สังกัด/อำเภอ/ตำบล + เพิ่ม "ต้นสังกัด" ด้วยข้อมูลจากทำเนียบโรงเรียนเสมอ (เพิ่มเมื่อ 2026-08-28, ต่อ 2026-08-29)
ค่า สังกัด/หน่วยงาน, อำเภอ, ตำบล ที่หน่วยงานพิมพ์เองในไฟล์สำรวจก็คลาดเคลื่อนได้เหมือนกัน — ถ้าแถว
นั้นมี `school_code` ที่จับคู่กับ**ทำเนียบโรงเรียน (`schools_master`) ของปีการศึกษาเดียวกัน**ได้
`Reporting::schoolMasterOverrides()` (เรียกจาก `pivot()`/`tidyRows()` และ `review.php`) จะดึงค่า
`area_name`/`amphoe`/`tambon` จากทำเนียบมาแทนค่าที่อัปโหลดมาเสมอ — **อ่านอย่างเดียว ไม่แก้ไขข้อมูล
ใน `schools_master` เลย** (เป็นไฟล์อ้างอิงที่ต้องรักษาความถูกต้องไว้) ถ้าจับคู่ไม่ได้ (ไม่มี
school_code ในฟอร์มนั้น เช่นฟอร์ม 1/13/14, ทำเนียบยังว่าง, หรือรหัสไม่ตรง) fallback เป็นค่าที่
อัปโหลดมาตามเดิม — เมธอดนี้เป็น `public` โดยตั้งใจ เผื่อหน้าอื่นที่ query `submissions` ตรง ๆ (ไม่ผ่าน
`Reporting`) ต้องเรียกใช้ด้วย (ตอนนี้มีแค่ `review.php`) — **ถ้าเพิ่มหน้าใหม่ที่โชว์ agency_name/
amphoe/tambon จาก submissions ต้องเรียก `schoolMasterOverrides()` ด้วยเสมอ ไม่งั้นค่าจะไม่ตรงกับ
หน้าอื่น**
* **เพิ่ม "ต้นสังกัด" (คอลัมน์ DB/ตัวแปรยังชื่อ `department` ตามเดิม แค่ label ที่แสดงผลเปลี่ยน — เดิม
  ใช้ label "กระทรวง" ตอนเพิ่มฟีเจอร์นี้ครั้งแรก ผู้ใช้งานขอเปลี่ยนคำเป็น "ต้นสังกัด" ทันทีหลังจากนั้น
  2026-08-29)** — คนละแบบกับ 3 ฟิลด์ข้างบน: ไฟล์สำรวจฟอร์มไหนก็ไม่มีคอลัมน์นี้เลยตั้งแต่ต้น (ไม่ใช่
  การ "แทนที่" ค่าที่อัปโหลดมา แต่เป็นการ**เพิ่มฟิลด์ใหม่ที่ไม่เคยมี**) มาจาก `schools_master.department`
  ล้วน ๆ (คอลัมน์นี้มีอยู่ในตาราง/มีการแม็ปจาก `DepartmentNameThai` อยู่แล้วตั้งแต่ migration 003 —
  แค่ไม่เคยถูกดึงมาใช้แสดงผลที่ไหนมาก่อน) เพิ่ม `department` เข้าไปใน `SELECT` ของ
  `schoolMasterOverrides()` แล้วเพิ่มคีย์ `department` ในทุกจุดที่สร้าง `$row` ใน `pivot()`/
  `tidyRows()` — ที่หน้าเว็บแค่เพิ่ม `'department' => 'ต้นสังกัด'` เข้าไปใน `$identityLabels` ของ
  `view.php`/`export.php`/`export_tidy.php` (ตัวตารางเป็น loop ทั่วไปตาม `$identityLabels` อยู่แล้ว
  ไม่ต้องแก้ HTML/CSV structure เพิ่ม) — **ไม่แตะ `edit_submission.php`** เพราะฟิลด์นี้ไม่ได้เก็บอยู่
  ใน `submissions` เลย แก้ไขในฟอร์มแก้ไขรายแถวไม่ได้ (เหมือนกับ agency_name/amphoe/tambon ตอน
  override — ฟอร์มแก้ไขยังโชว์ค่าดิบที่เก็บจริงไว้แก้เท่านั้น)
* **มีผลกับข้อมูลที่นำเข้าไปแล้วทันที ไม่ต้อง migrate/backfill อะไรเลย** เพราะคำนวณตอนแสดงผล
  (query `schools_master` ด้วย `school_code` ที่มีอยู่แล้วทุกแถว) ไม่ได้เขียนเพิ่มลง `submissions`
  — เงื่อนไขเดียวที่ต้องมี: **ทำเนียบโรงเรียนที่เคยอัปโหลดไว้ต้องมีคอลัมน์ `DepartmentNameThai` มาด้วย**
  ถ้าทำเนียบเก่าที่อัปโหลดไว้ไม่มีคอลัมน์นี้ (ไฟล์ต้นฉบับบางเวอร์ชันอาจไม่มี) ต้นสังกัดจะว่างไปจนกว่า
  จะอัปโหลดทำเนียบใหม่ที่มีคอลัมน์นี้ (ดูหัวข้อ "การตรวจสอบรหัสสถานศึกษาเทียบทำเนียบโรงเรียน")

### ดาวน์โหลดรวมทุกชีทของฟอร์มเดียวกัน (เพิ่มเมื่อ 2026-08-28)
ฟอร์มที่มีหลายชีท (เช่นฟอร์ม 12 — 13 โครงการ) เดิมต้องเข้าดู/ดาวน์โหลดทีละชีท — `export.php`/
`export_tidy.php` รองรับ `sheet=__all__` เพื่อรวมทุกชีทของฟอร์มนั้นเข้าไฟล์เดียว (union คอลัมน์/
extra_identity fields ข้ามชีท, เพิ่มคอลัมน์ "ชีท" บอกที่มาแต่ละแถว) มีปุ่มที่หน้าแดชบอร์ด
(`index.php`) โผล่อัตโนมัติเมื่อฟอร์มมีมากกว่า 1 ชีท (หลัง dedupe ด้วย `db_sheet_name`) — เขียนให้
เป็น**กลไกทั่วไป ใช้ได้กับฟอร์มไหนก็ได้ที่มีหลายชีท** ไม่ผูกกับฟอร์ม 12 โดยเฉพาะ
* **บั๊กแฝงที่เจอระหว่างทำ**: `tidyRows()` เดิมจะไม่คืนแถวเลยสำหรับชีทที่ไม่มี value column (เช่น
  ฟอร์ม 12 ที่แค่ "ปรากฏในชีท" ก็คือข้อมูลแล้ว ไม่มีคอลัมน์ตัวเลข) เพราะวนอ่านจาก
  `submission_values` ที่ไม่เคยมีแถวถูกเขียนไว้เลยสำหรับ sheet แบบนี้ — ไฟล์ "สำหรับ Pivot Table"
  ของฟอร์มแบบนี้จึงว่างเปล่ามาตลอด แก้แล้วให้เพิ่มแถวสำรอง 1 แถวต่อ submission ที่ไม่มีค่าเลย
  (คอลัมน์ค่าว่างไว้ แต่ identity/extra_identity เช่น "ชื่อโครงการ" ยังโผล่ครบ)
* **`fixed_extra_identity` ใช้ดึงข้อความจากแม่แบบมาผูกกับข้อมูลได้ด้วย ไม่ใช่แค่บอกที่มาไฟล์**:
  ฟอร์ม 12 มี 13 ชีท แต่ละชีทมีชื่อโครงการเต็มอยู่ในแถว 1 ของแม่แบบ (อ่านง่ายกว่า sheet_name เอง)
  เพิ่ม `fixed_extra_identity => ['project_name' => '...ข้อความจากแถว 1...']` ให้ทุก sheetDef —
  ค่านี้ hard-code จากแม่แบบตรง ๆ ไม่ได้อ่านจากไฟล์ที่อัปโหลดจริงทุกครั้ง ถ้าแม่แบบเปลี่ยนข้อความ
  ต้องแก้ registry.php ตามด้วย

### รวมคอลัมน์ที่แทรก/แทนที่เพิ่มเข้ามาเข้ากับคอลัมน์ "อื่นๆ" (เพิ่มเมื่อ 2026-08-28, แก้ให้ทั่วไปขึ้น 2026-08-29)
บาง sheetDef อนุญาตให้หน่วยงานแทรกคอลัมน์ของตัวเองเพิ่มได้ (เช่นฟอร์ม 10.6 วิชาเอก ที่แม่แบบมี
หมายเหตุ "สามารถแทรกคอลัมน์วิชาเอกเพิ่มเติมได้") — เดิมระบบจะบล็อกทั้งชีทถ้าจำนวนคอลัมน์ไม่ตรงกับ
ต้นฉบับ ไม่ว่าจะแทรกตรงไหนก็ตาม เพิ่ม key `merge_extra_columns_into` (ตั้งเป็น column_path ของ
คอลัมน์ปลายทาง เช่น `'อื่นๆ'`) ให้ sheetDef เพื่อให้ **`Importer::tryResolveExtraColumns()`** รวมค่า
คอลัมน์ที่ไม่รู้จักเข้ากับคอลัมน์ปลายทางแทนการ error
* **เวอร์ชันแรก (2026-08-28) จับคู่แบบตำแหน่งตายตัว** (คอลัมน์ก่อนหน้าปลายทางทุกคอลัมน์ต้องตรงเป๊ะ
  รวมถึงตัวปลายทางเอง แล้วเอาเฉพาะคอลัมน์ที่ต่อท้ายมาบวกรวม) — **พังจริงกับไฟล์จริง** เพราะหน่วยงาน
  ไม่ได้แค่ต่อท้ายอย่างเดียว: บางไฟล์แทรกคอลัมน์ใหม่แทรกกลางรายชื่อวิชาเดิม (ทำให้ทุกคอลัมน์หลังจุด
  แทรกเลื่อนตำแหน่ง ไม่ตรงกับต้นฉบับอีกต่อไป), บางไฟล์ลบ/เปลี่ยนชื่อ "อื่นๆ" ทิ้งไปเลยแล้วใส่ชื่อวิชา
  ของตัวเองแทนหลายคอลัมน์รวด (ไม่มีข้อความ "อื่นๆ" หลงเหลืออยู่ในไฟล์เลย)
* **เวอร์ชันแก้ (2026-08-29) จับคู่แบบ subsequence**: ตัดคอลัมน์ปลายทาง ("อื่นๆ") ออกจากรายการ
  คอลัมน์ที่ต้องตรวจสอบ เหลือแค่รายชื่อวิชาที่เหลือ (24 รายการ) แล้วไล่คอลัมน์ที่อัปโหลดมาทีละคอลัมน์
  ตามลำดับ — คอลัมน์ไหนตรงกับวิชาตัวถัดไปที่รอ (ตามลำดับ ไม่ใช่ตำแหน่ง) ก็ข้ามไป คอลัมน์ไหนไม่ตรง
  (ไม่ว่าจะเป็น "อื่นๆ" ตัวเดิม หรือวิชาที่แทรกเข้ามา หรือคอลัมน์ท้ายไฟล์) ถือเป็น "extra" ทั้งหมด
  รวมเข้า "อื่นๆ" หมด — ถ้าไล่จนจบแล้วยังมีวิชาที่ไม่เจอเลย (หายไปจริง ไม่ใช่แค่ย้ายที่) ถึงจะ error
* **ปัญหาความสม่ำเสมอของ col_index ข้ามไฟล์**: ตำแหน่งคอลัมน์ "อื่นๆ" ที่รวมแล้วไม่เท่ากันทุกไฟล์
  (เพราะขึ้นกับว่าคอลัมน์แรกที่ไม่รู้จักอยู่ตรงไหน) แต่ `Reporting::pivot()` ต้องจับคู่ col_index
  ข้ามหลาย submission ให้เป็นคอลัมน์เดียวกัน (ผ่าน `column_path`) — ถ้า col_index ไม่คงที่จะได้
  คอลัมน์ "อื่นๆ" ซ้ำกันหลายคอลัมน์ในตารางแสดงผล แก้โดยใช้ **`Importer::MERGED_COLUMN_INDEX`
  (ค่าคงที่ 999999)** เป็น col_index ของคอลัมน์ที่รวมแล้วเสมอ ทุกไฟล์ ไม่ชนกับคอลัมน์จริงเพราะไม่มี
  ไฟล์ไหนกว้างขนาดนั้น และพลอยได้ผลข้างเคียงที่ดี: คอลัมน์ "อื่นๆ" จะเรียงอยู่ท้ายสุดเสมอตอน export
  (ตรงกับตำแหน่งเดิมในแม่แบบ) — ใช้ได้เฉพาะ `value_type => 'numeric'` เท่านั้น (เพราะต้อง "บวก" ค่า
  เข้าด้วยกันผ่าน `Importer::mergeNumericCells`)

### สถิติการศึกษาจังหวัดปัตตานี — หน้าสาธารณะไม่ต้อง login (เพิ่มเมื่อ 2026-08-29)
`public_report.php` เป็นหน้าเดียวในระบบที่**ไม่เรียก `Auth::requireLogin()`** (ตั้งใจ — สำหรับ
ศึกษาธิการจังหวัด/ผู้ว่าราชการจังหวัด/สาธารณะทั่วไป) แสดงตัวเลขหลักสรุประดับจังหวัดเท่านั้น
(ไม่มีข้อมูลรายสถานศึกษา/รายบุคคล) แยกได้ 3 มิติ: สังกัด/หน่วยงาน (`agency_name`), ต้นสังกัด
(`department`), อำเภอ (`amphoe`) — ทั้ง 3 ฟิลด์นี้ผ่าน `Reporting::pivot()` ซึ่ง override ด้วย
ทำเนียบโรงเรียนอยู่แล้ว (ดูหัวข้อ "แทนที่สังกัด/อำเภอ/ตำบล...") จึงตรงกับหน้าที่ต้อง login เสมอ
* **เมธอดใหม่ใน `Reporting.php`**: `groupedTotal()` (รวมยอดทุกคอลัมน์ตัวเลขของ 1 ชีท แยกตามมิติ),
  `groupedTotalForColumns()` (เหมือน `groupedTotal()` แต่รวมยอดเฉพาะ column_path ที่ระบุ — ใช้กับ
  ชีทที่มีหลายหัวข้อปนกัน เช่นฟอร์ม 14 ที่มีทั้งจำนวนเด็กและจำนวนครูในตารางเดียวกัน),
  `groupedCount()` (นับจำนวนแถว แยกตามมิติ — ใช้กับชีทที่ 1 แถว = 1 หน่วยนับพอดี), และ
  `schoolCountByDimension()` (นับจำนวนสถานศึกษาตรงจาก `schools_master` เอง ไม่พึ่งความครบถ้วน
  ของการอัปโหลดฟอร์ม 2 — แม่นยำกว่าถ้าบางหน่วยงานยังไม่ส่งฟอร์ม 2 มา)
* **ตัวเลขหลักที่เลือกมาแสดง (`$metrics` ใน `public_report.php`) ยังไม่ครบทั้ง 15 ฟอร์มโดยตั้งใจ** —
  เลือกเฉพาะชีทที่ตรวจสอบกับ `reference_templates/` ด้วยมือแล้วว่า**ไม่มีคอลัมน์ "รวม" ปนอยู่กับ
  คอลัมน์ย่อยในชีทเดียวกัน** (ถ้ามีแล้วเอาไป sum ทุกคอลัมน์รวมกัน จะรวมยอดซ้ำสอง — เจอเคสแบบนี้แล้ว
  ในฟอร์ม 8.2 "ประเภทความพิการ" กับฟอร์ม 14 ที่มีทั้งจำนวนเด็กและจำนวนครูปนอยู่ในชีทเดียวกัน จึง
  **ไม่ใช้ฟอร์ม 8.2/9/11/12/13/15 เป็นตัวเลขหลัก** ตอนนี้ — ถ้าจะเพิ่มฟอร์มอื่นเข้ามาต้องตรวจ
  header ของชีทนั้นก่อนทุกครั้งว่าไม่มีคอลัมน์สรุปยอดปนอยู่ ก่อนจะเพิ่มเข้า `$metrics` (หรือถ้ามีปนอยู่
  แต่ระบุคอลัมน์เฉพาะที่ต้องการได้ ให้ใช้ `groupedTotalForColumns()` แทนแบบที่ทำกับฟอร์ม 14 ด้านล่าง)
* ตัวเลขที่เลือกไว้ตอนนี้: จำนวนสถานศึกษา (จาก `schools_master`), **จำนวนนักเรียน (ฟอร์ม 4 + เด็กเล็ก
  จากฟอร์ม 14 คอลัมน์ "รวมทั้งสิ้น" อย่างเดียว — เพิ่มเมื่อ 2026-08-29 ตามคำขอผู้ใช้งาน เพราะศูนย์
  พัฒนาเด็กเล็ก/สถานพัฒนาเด็กปฐมวัย (สังกัด ศพด., ต้นสังกัด สถ.) ไม่ได้กรอกฟอร์ม 4 เลย + ผู้เรียน
  จากฟอร์ม 15 (โรงเรียนเอกชนนอกระบบ — สังกัด ศธจ. เช่น "สช นอกระบบ ตาดีกา", "สช นอกระบบ สถาบัน
  ศึกษาปอเนาะ" ฯลฯ ก็ไม่ได้กรอกฟอร์ม 4 เช่นกัน — เพิ่มเมื่อ 2026-08-29 เหมือนกัน) ถ้าไม่รวมฟอร์ม
  14/15 เข้ามาตัวเลขจะขาดไปทั้งกลุ่ม)**, จำนวนห้องเรียน (ฟอร์ม 3), **จำนวนครู/บุคลากร (ฟอร์ม 10.1
  + ครู/ผู้ดูแลเด็กจากฟอร์ม 14 เฉพาะ 2 คอลัมน์ "จำนวนครู/ผู้ดูแลเด็ก (คน) / ชาย" กับ "/ หญิง" +
  ผู้สอน/โต๊ะครูจากฟอร์ม 15 เหตุผลเดียวกัน — ไม่รวมคอลัมน์แยกวุฒิการศึกษาของครูในฟอร์ม 14 เพราะเป็น
  การแจกแจงซ้ำของ 2 คอลัมน์นี้อยู่แล้ว ถ้ารวมด้วยจะนับซ้ำสอง)**, นักเรียนออกกลางคัน (ฟอร์ม 7 รวม
  6 ชีท), นักเรียนพิการ (ฟอร์ม 8.1), นักเรียนด้อยโอกาส (ฟอร์ม 5.1)
* **ฟอร์ม 15 มี 5 ชีท แต่ละชีทตั้งชื่อคอลัมน์ตัวเลขไม่เหมือนกัน** ต้องระบุ column_path ให้ตรงเป๊ะ
  ทีละชีท: 15.1/15.3 ใช้ "จำนวนผู้สอน"/"จำนวนผู้เรียน", 15.2 ใช้ "จำนวนโต๊ะครู"/"จำนวนผู้เรียน" (คำ
  เฉพาะของสถาบันปอเนาะ), "สช.วิชาชีพ-ครู-นร." ใช้ "จำนวนนักเรียน"/"จำนวนครู" ที่มีคอลัมน์ย่อย ชาย/
  หญิง/**รวม** อยู่ในตัว (ไม่ได้ split_last แยกเพศแบบฟอร์มอื่น — ดู forms/registry.php) ต้องใช้แค่
  คอลัมน์ "รวม" เท่านั้น ไม่งั้นจะนับซ้ำสอง — ชีท "สช.วิชาชีพ" (value_type=text) ไม่มีคอลัมน์ตัวเลข
  เลย จึงไม่เกี่ยวกับตัวเลขหลักใด ๆ ในหน้านี้
* ลิงก์เข้าถึงจากหน้าแดชบอร์ด (`index.php`) มีปุ่ม "สถิติการศึกษาจังหวัด (สาธารณะ)" เปิดแท็บใหม่ ไม่ต้อง
  login ก็เข้าได้ตรง ๆ ผ่าน URL `public_report.php`

### กราฟในหน้าสถิติสาธารณะ (เพิ่มเมื่อ 2026-08-29, ขยายเพิ่มรอบเดียวกัน)
เพิ่มกราฟชุดแรก 3 ตัวใน `public_report.php` ตามคำขอผู้ใช้งาน: สัดส่วนนักเรียนชาย:หญิง (stacked bar
แนวนอน), อัตราส่วนนักเรียนต่อครู (stat tile), กราฟแท่งจำนวนนักเรียนแยกตามต้นสังกัด/อำเภอ (bar
chart แนวนอน 2 อัน ตรึงมิติไว้ตายตัว ไม่ขึ้นกับ dropdown บนสุด) — ใช้แนวทางจาก skill `dataviz`
(โหลด skill แล้วอ่าน `references/choosing-a-form.md`/`palette.md`/`marks-and-anatomy.md` ก่อนเขียน
โค้ดกราฟทุกครั้ง ตามที่ skill กำหนด)
* **สีที่ใช้**: ชุดสี validated ของ skill ตรง ๆ ไม่ได้ปรับเอง — slot 1 (`#2a78d6` น้ำเงิน) กับ slot 2
  (`#eb6834` ส้ม) ตามลำดับ fixed order ที่ระบุไว้ใน `references/palette.md` (ผ่านทุก gate การมองเห็น
  สีที่ตาบอดสีอยู่แล้วตามที่ skill validate มาให้ในเอกสาร ไม่ต้องรัน validator ซ้ำเพราะไม่ได้แก้ค่าสี
  เอง — เครื่องนี้ไม่มี `node` ติดตั้งด้วย รันสคริปต์ validator ไม่ได้จริง ๆ ถ้าจะเปลี่ยนสีในอนาคตต้อง
  รัน `node scripts/validate_palette.js` จากเครื่องที่มี node ก่อนเสมอ)
* **กราฟแท่งไม่ได้ใช้ SVG** — ใช้ `<div>` + CSS `width:%` ธรรมดา (bar-fill ปัดมุมด้านขวา สี่เหลี่ยม
  ด้านซ้าย/ฐาน ตาม mark spec ของ skill) เพราะป้ายชื่อ (ต้นสังกัด/อำเภอ) เป็นข้อความไทยยาว จัด layout
  ด้วย HTML ง่ายกว่าคำนวณตำแหน่ง text ใน SVG มาก — มี `title` attribute บนแท่งเป็น tooltip พื้นฐาน
  (ไม่ใช้ JS เลยทั้งหน้า คงสไตล์เดิมของเว็บที่เป็น server-rendered PHP ล้วน)
* **สัดส่วนชาย/หญิงคำนวณจากคอลัมน์แยกเพศดิบ ไม่ใช่คอลัมน์ "รวม"**: ใช้ column_path ชุดเดียวกับ
  metric "จำนวนนักเรียน" ทุกแหล่งข้อมูล (ฟอร์ม 4, 14, 15) แต่สลับจาก whitelist ที่ใช้คอลัมน์ "รวม"/
  "รวมทั้งสิ้น" (กันนับซ้ำตอนรวมยอด) มาใช้คอลัมน์ ชาย/หญิง ดิบแทน (เพิ่ม
  `Reporting::genderTotalsForColumns()` — จับคู่ด้วย regex `/ชาย$/`/`/หญิง$/` ที่ปลาย column_path
  ใช้ได้ทุกฟอร์มเพราะทุกฟอร์มลงท้ายคอลัมน์แยกเพศด้วยคำนี้เสมอ ไม่ว่าจะมีกี่ระดับ header ก่อนหน้า)
* **Refactor**: ดึง logic คำนวณยอดต่อ metric+มิติ ออกมาเป็นฟังก์ชัน `compute_metric_totals()` แยก
  ต่างหาก เพื่อเรียกซ้ำได้ทั้งตารางหลัก (ตามมิติที่เลือก) และกราฟแท่ง (ตรึงมิติ department/amphoe)
  โดยไม่ query ซ้ำโค้ด — อัตราส่วนนักเรียนต่อครูใช้ผลรวมที่คำนวณไว้แล้วสำหรับตารางหลัก (`array_sum`)
  ไม่ query เพิ่มเพราะยอดรวมทั้งจังหวัดเท่ากันไม่ว่าจะแยกตามมิติไหน
* หน้าเว็บทั้งเว็บนี้**ไม่มีโหมดมืด** (ดู `public/assets/style.css`) กราฟจึงทำแค่โหมดสว่างเท่านั้น
  ตั้งใจไม่ตาม dark-mode pattern ที่ skill กำหนดสำหรับ Artifact เพราะหน้านี้ไม่ใช่ Artifact
* **ขยายเพิ่มในคำขอเดียวกัน**: ย้ายกราฟทั้งหมดขึ้นไว้เหนือตารางสรุปยอดรวม (เดิมกราฟแท่งอยู่ใต้ตาราง)
  แล้วเพิ่มอีก 5 กราฟ: สัดส่วนครูชาย:หญิง (ใช้ `Reporting::genderTotalsForColumns()` ตัวเดิม แค่
  สลับชุดชีทเป็นของ metric "จำนวนครู/บุคลากร"), จำนวนนักเรียนรายปีการศึกษา (bar-per-year แทน line
  chart — เจตนา เพราะระบบเพิ่งเริ่มเก็บข้อมูล อาจมีแค่ 1-2 ปี เส้นที่มี 1-2 จุดดูไม่มีความหมาย ใช้
  bar component เดิมที่ทนกับข้อมูลน้อยได้ดีกว่า พอมีหลายปีจะเห็นเป็นแนวโน้มได้เองไม่ต้องแก้โค้ด),
  นักเรียนออกกลางคันแยกตามสาเหตุ, นักเรียนพิการแยกตามประเภทความพิการ, และ 5 อำเภอที่มีอัตรา
  ออกกลางคันสูงสุด/ต่ำสุด (คำนวณเป็น % ไม่ใช่จำนวนดิบ — หารด้วยจำนวนนักเรียนในอำเภอเดียวกัน กันอำเภอ
  ใหญ่ดูน่ากังวลเกินจริงเทียบกับอำเภอเล็ก)
  * **เพิ่ม `Reporting::sumByColumnPathParts($formKey, $sheetName, $dropFirst, $dropLast, $year)`**
    — ตัดคอลัมน์ path (เช่น "ชั้นปี / สาเหตุ / เพศ" ของฟอร์ม 7 หรือ "ประเภท / เพศ" ของฟอร์ม 8.2)
    ออกจากหัว/ท้ายตามจำนวนที่ระบุ แล้วรวมยอดตามส่วนที่เหลือ — คอลัมน์ที่เหลือ 0 ส่วนหลังตัด (เช่น
    "รวม" ของฟอร์ม 8.2 ซึ่งมีแค่ 1 ระดับ) จะถูกข้ามอัตโนมัติ ไม่ต้องเช็กชื่อคอลัมน์ "รวม" เองเลย —
    **ต้องตรวจโครงสร้างคอลัมน์จริงก่อนใช้ทุกครั้ง** (ฟอร์ม 7 มี "สาเหตุ" บางอันแตกเป็น 2 ระดับ เช่น
    "มีปัญหา / ครอบครัว" ไม่ใช่แค่ 1 ระดับเสมอไป — ถ้าเดาโครงสร้างผิดจะตัดผิดตำแหน่งเงียบ ๆ)
  * **`render_bar_chart()`** (ฟังก์ชันใน `public_report_data.php` — ดูหัวข้อแยกหน้าด้านล่างว่าย้าย
    มาจากไหน) รวม logic เรนเดอร์กราฟแท่งที่เคยซ้ำกันทุกกราฟให้เหลือจุดเดียว รับ `callable
    $formatValue` เพื่อ format หน่วยต่างกันได้ (คน vs %) — ถ้าจะเพิ่มกราฟแท่งใหม่ในหน้านี้อีก เรียก
    ฟังก์ชันนี้แทนการก็อปโค้ด HTML เดิม
* **เปลี่ยนโทนสีเป็นฟ้าพาสเทล/ชมพูพาสเทล (แก้เมื่อ 2026-08-29)** — ผู้ใช้ขอเปลี่ยนจากชุดสี validated
  เดิม (น้ำเงินเข้ม `#2a78d6` / ส้ม `#eb6834`) เป็นฟ้าพาสเทล (ชาย/series-1) กับชมพูพาสเทล (หญิง/
  series-2) โดยตรง — แก้แค่ค่า `--series-1: #7fb3e0` / `--series-2: #f4a6c6` ใน `.viz-root` ที่
  `public/assets/public_report.css` (จุดเดียว มีผลกับทุกกราฟเพราะทุกกราฟอ้างผ่าน CSS variable นี้)
  **เครื่องนี้ยังไม่มี `node` รัน `scripts/validate_palette.js` ของ skill `dataviz` ไม่ได้จริง ๆ** จึง
  เลือกคู่สีที่ hue ห่างกันมาก (ฟ้า ~210° กับชมพู ~340°) ซึ่งไม่ได้พึ่งแกนแดง-เขียวที่คนตาบอดสีชนิด
  deuteranopia/protanopia แยกยาก และทุกจุดที่ใช้สีนี้แยกเพศมี legend + ตัวเลขกำกับเป็นข้อความอยู่แล้ว
  (ไม่ได้ใช้สีเป็นตัวบ่งชี้เดี่ยว ๆ) — **ถ้ามี node ในอนาคตควรรัน validator ยืนยันอีกครั้ง** ก่อนใช้งาน
  จริงจังในระยะยาว

### แยกหน้ากราฟกับหน้าตารางสรุป + เมนูด้านซ้าย (เพิ่มเมื่อ 2026-08-29)
เดิม `public_report.php` มีทั้งกราฟและตารางสรุปยอดรวมอยู่ในหน้าเดียวยาวมาก ผู้ใช้งานขอให้แยกเป็น
2 หน้า พร้อมเมนูสลับหน้าด้านซ้าย — โครงสร้างไฟล์ใหม่:
* **`public_report_data.php`** — ไฟล์ข้อมูล/ฟังก์ชันร่วม (ไม่มี HTML ของเนื้อหาเฉพาะหน้า) ดึง logic
  คำนวณข้อมูลทั้งหมดที่เคยอยู่ในไฟล์เดียวมารวมไว้ที่นี่ (`$metrics`, `compute_metric_totals()`,
  ข้อมูลกราฟทุกตัว, `fmt_num()`, `render_bar_chart()`) **ทั้ง 2 หน้าที่ `require_once` ไฟล์นี้
  ไม่ query ซ้ำกัน** เพราะเรียก `Reporting`/`schools_master` ครั้งเดียวต่อการโหลดหน้า (คนละหน้าคนละ
  request จึงคำนวณแยกกันตามปกติ แต่ไม่ได้ query ซ้ำ "ภายใน" หน้าเดียวกันเหมือนก่อนแยก)
  * มีฟังก์ชัน `render_report_start(string $activePage)` / `render_report_end()` เปิด/ปิด
    layout ร่วม (head, topbar, เมนูด้านซ้าย, การ์ดแนะนำ+ตัวกรองปี/มิติ, footer) — `$activePage`
    รับค่า `'charts'` หรือ `'table'` ใช้ไฮไลต์เมนูที่กำลังเปิดอยู่ **ฟังก์ชันเหล่านี้เปิด/ปิด div ไม่
    ครบคู่ในตัวเอง** (เปิด `.container`/`.report-layout`/`.report-main` ค้างไว้ให้หน้าที่เรียกใส่
    เนื้อหาต่อ แล้วปิดทั้งหมดใน `render_report_end()`) — ถ้าจะเพิ่มหน้าที่ 3 ในอนาคต ต้องเรียกคู่นี้
    ให้ครบทั้งสองฟังก์ชันเสมอ ไม่งั้น HTML จะเพี้ยน (div ไม่ปิด)
  * `$navQuery` (query string ของปี+มิติที่เลือกอยู่) คำนวณไว้ในไฟล์นี้ ใช้ต่อท้าย URL ตอนสลับเมนู
    เพื่อไม่ให้ตัวกรองที่เลือกไว้หายไปตอนเปลี่ยนหน้า
* **`public_report.php`** — เหลือแค่เนื้อหากราฟ (สัดส่วนชาย/หญิงนักเรียน+ครู, อัตราส่วนนักเรียนต่อครู,
  กราฟแท่งทั้งหมด) ยังเป็น URL หลักที่ลิงก์จากแดชบอร์ด (`index.php`) เหมือนเดิม
* **`public_report_table.php`** — หน้าใหม่ มีแค่ตารางสรุปยอดรวม (ที่เคยอยู่ท้ายหน้าเดียวกัน)
* **`public/assets/public_report.css`** — แยก CSS เฉพาะหน้าสถิติ (กราฟ, เมนูด้านซ้าย, ตาราง) ออกจาก
  `<style>` inline เดิม เป็นไฟล์ CSS แยกต่างหาก โหลดคู่กับ `style.css` หลักในทั้ง 2 หน้า — เพิ่ม
  `.report-layout`/`.report-sidebar`/`.report-main` สำหรับ layout เมนูด้านซ้าย (responsive: จอแคบ
  กว่า 720px เมนูจะเปลี่ยนจากแนวตั้งด้านซ้ายเป็นแถวปุ่มแนวนอนด้านบนแทน)
* **dropdown "แยกตามมิติ" โชว์เฉพาะหน้าตาราง (ตัดออกจากหน้ากราฟเมื่อ 2026-08-29)** — กราฟส่วนใหญ่
  ตรึงมิติไว้ตายตัว (ต้นสังกัด/อำเภอ) อยู่แล้ว ไม่ได้ขึ้นกับ dropdown นี้เลย ผู้ใช้งานบอกว่ามี
  dropdown ไว้ที่หน้ากราฟทำให้สับสนว่าเปลี่ยนแล้วกราฟจะเปลี่ยนตามไหม — `render_report_start()` เช็ก
  `$activePage === 'table'` ก่อนโชว์ dropdown มิติ ถ้าเป็นหน้ากราฟจะใส่ `<input type="hidden"
  name="dim">` แทน (คงค่ามิติที่เคยเลือกไว้จากหน้าตารางไว้เผยแพร่ต่อ เผื่อผู้ใช้งานเปลี่ยนปีที่หน้า
  กราฟแล้วสลับกลับไปหน้าตาราง จะได้ไม่รีเซ็ตมิติที่เคยเลือก) — ถ้าจะเพิ่มหน้าที่ 3 ที่ใช้มิตินี้ด้วย
  ต้องเพิ่มเงื่อนไขให้ตรงกับ `$activePage` ของหน้านั้น
* **ตารางสรุปยอดรวมกว้างเต็มจอ + scrollbar แนวนอนใช้งานง่ายขึ้น (แก้เมื่อ 2026-08-29)** — ปัญหาเดิม
  2 อย่าง: (1) `.container` ของทั้ง 2 หน้าจำกัดไว้แค่ 1300px ตารางที่มีหลายคอลัมน์เลยแคบเกินไป ต้อง
  scroll แนวนอนทั้งที่จอกว้างพอ แก้โดยเปลี่ยนเป็น `max-width: 98vw` ให้กว้างเกือบเต็มจอ (มีผลทั้ง
  หน้ากราฟและหน้าตารางเพราะใช้ `render_report_start()` ร่วมกัน) (2) **`.table-scroll` มาตรฐานของ
  ระบบ (ใช้ทั่วทั้งเว็บ) scroll ได้แค่แนวนอนอย่างเดียว** ถ้าตารางมีแถวเยอะจนสูงเกินจอ scrollbar
  แนวนอนจะไปโผล่ท้ายตาราง (ท้ายสุดของหน้าเว็บ) ผู้ใช้งานต้องเลื่อนหน้าจอลงไปสุดก่อนถึงจะเห็น/ใช้
  scrollbar ได้ — **ไม่ได้แก้ `.table-scroll` ตัวกลาง** (จะกระทบทุกหน้าที่ใช้ เช่น view.php,
  review.php) แต่เพิ่มคลาสใหม่เฉพาะ `.report-table-scroll` (ใน `public/assets/public_report.css`)
  ใส่ `max-height: 75vh; overflow: auto;` (ทั้ง 2 แกน ไม่ใช่แค่ overflow-x) ให้กรอบตารางเองมีขนาด
  คงที่ scroll ได้ในตัวเอง scrollbar เลยอยู่ในตำแหน่งที่มองเห็นเสมอไม่ต้องเลื่อนหน้าเว็บลงไปหา — ผล
  พลอยได้: `th { position: sticky; top: 0; }` ที่มีอยู่แล้วใน style.css จะ sticky ตาม scrolling
  ancestor ที่ใกล้ที่สุด กลายเป็น sticky กับกรอบตารางเองแทนหน้าเว็บ (หัวตารางค้างอยู่ด้านบนเวลา
  scroll ดูแถวข้อมูลในกรอบ อ่านง่ายขึ้นไปด้วย) — ใช้เฉพาะ `public_report_table.php` เท่านั้น ไม่ใช้
  `.table-scroll` เดิม

### รวมข้อมูลฟอร์ม 11 (กศน.) เข้าตารางสรุป/กราฟ (เพิ่มเมื่อ 2026-08-29)
ผู้ใช้ขอให้เอาข้อมูลตารางที่ 11 (`11_nfe`) มารวมในหน้าสถิติสาธารณะด้วย โดยอิงรหัสสถานศึกษาเหมือนแหล่ง
ข้อมูลอื่น (ฟอร์ม 11 เป็นสังกัด/หน่วยงาน กศน. ซึ่งไม่ได้กรอกฟอร์ม 4/8 เหมือนโรงเรียนสังกัดอื่น เลยไม่
เคยถูกนับในยอดรวม "จำนวนนักเรียน"/"นักเรียนพิการ" มาก่อน):
* **ชีท 11.1 → บวกเข้า metric `students`** — ตรวจ `reference_templates/11_ข้อมูลการศึกษา_กศน.xlsx`
  ด้วย `xlsx_dump.py` แล้ว โครงสร้างเป็นคอลัมน์ กิจกรรมการศึกษา (13 กิจกรรม) × เพศ (ชาย/หญิง) **ไม่มี
  คอลัมน์ "รวม"/"รวมทั้งสิ้น" ปนอยู่เลย** ต่างจากฟอร์ม 14/15 ที่ต้องระบุ `onlyColumns` เพื่อกันชนกับ
  คอลัมน์อื่นในชีทเดียวกัน — 11.1 จึงส่ง `null` (รวมทุกคอลัมน์) ได้เลยเหมือนฟอร์ม 4/10.1
* **ชีท 11.2 → บวกเข้า metric `disability`** — โครงสร้างคอลัมน์เหมือน 11.1 ทุกประการ (กิจกรรม×เพศ
  ไม่มี "รวม") ต่างแค่หัวเรื่องเป็น "จำนวนผู้เรียนพิการ" แทน — ส่ง `null` เช่นกัน ไม่กระทบกราฟ
  "นักเรียนพิการแยกตามประเภทความพิการ" เพราะกราฟนั้นดึงจากชีท 8.2 ตรง ๆ คนละตัวแปรกัน (ฟอร์ม 11.2
  แยกตาม "กิจกรรมการศึกษา" ไม่ใช่ "ประเภทความพิการ" เอาไปรวมกับ 8.2 ไม่ได้เพราะมิติคนละแบบ)
* **เพิ่ม 11.1 เข้า `$genderSheets` ด้วย** (ตัวแปรสัดส่วนนักเรียนชาย:หญิง) ให้ยอดรวมตรงกับ metric
  `students` เสมอ — ไม่ได้เพิ่มเข้า `$teacherGenderSheets`/metric `teachers` เพราะฟอร์ม 11 (11.1-11.5)
  ไม่มีชีทไหนเก็บจำนวนครู/บุคลากรเลย (เก็บแต่จำนวนผู้เรียน) และผู้ใช้ก็ไม่ได้ขอ
* `forms/registry.php` ของ `11_nfe` มี `identity_fields` เป็น `$stdIdentity` (มี `school_code`) อยู่แล้ว
  ไม่ต้องแก้ registry — งานนี้แก้แค่ `public_report_data.php` (เพิ่มรายการชีทใน `$metrics['students']`,
  `$metrics['disability']`, `$genderSheets`)

### หน้าค้นหารหัสสถานศึกษา (เพิ่มเมื่อ 2026-08-29, ย้ายไปหน้าสาธารณะวันเดียวกัน)
เพิ่มหน้าให้ค้นหารหัสสถานศึกษาจากชื่อสถานศึกษาได้ตอนกรอกฟอร์มไม่รู้รหัส — query ตรงจาก
`schools_master` (ทำเนียบโรงเรียน) ด้วย `school_name LIKE '%คำค้น%'`
* **เริ่มแรกสร้างเป็น `school_search.php` ฝั่ง backend (ต้อง login)** แล้วผู้ใช้ขอให้ย้ายไปหน้า
  สาธารณะในคำขอถัดมาทันที ให้ผู้ใช้ทั่วไป (เช่น เจ้าหน้าที่หน่วยงานที่ยังไม่มีบัญชีเข้าระบบหลังบ้าน)
  ค้นหาเองได้โดยไม่ต้องขอสิทธิ์ก่อน — **ย้ายไฟล์เป็น `public_school_search.php`** (ตัด
  `Auth::requireLogin()` ออก) ตั้งใจไม่ทิ้งไฟล์เดิมไว้คู่กัน (ย้ายจริง ไม่ได้สร้างซ้ำ) ต้องไล่แก้ลิงก์
  เมนูฝั่ง backend ทุกไฟล์ที่เคยชี้ไป `school_search.php` ให้ชี้มาที่ `public_school_search.php`
  แทน (เปิด tab ใหม่ด้วย `target="_blank"` เพราะเป็นหน้าคนละกลุ่มสิทธิ์กับหน้า backend ที่คลิกออกมา)
* **ใช้ layout ร่วมกับหน้าสถิติสาธารณะ** (`public_report.php`/`public_report_table.php`) — เมนู
  sidebar ด้านซ้าย 3 รายการ (ภาพรวม/ตารางสรุป/ค้นหารหัสสถานศึกษา) โดยเพิ่มลิงก์ที่ 3 เข้าไปใน
  `render_report_start()` ที่ `public_report_data.php` โดยตรง (ใช้ `$activePage === 'search'`
  เป็นตัวไฮไลต์เมนูที่หน้านี้เอง แต่หน้านี้เขียน HTML เอง ไม่ได้เรียก `render_report_start()`/
  `render_report_end()` เพราะการ์ดหัวหน้า/ฟอร์มกรองของฟังก์ชันนั้นผูกกับ "ปีการศึกษาของข้อมูลสถิติ"
  (metrics/dimensions) ไม่ใช่ปีของทำเนียบโรงเรียน+ช่องค้นหาชื่อ ผสมกันจะสับสน — คัดลอกแค่โครง
  topbar/sidebar/container/footer มาเขียนเองในไฟล์นี้ ใช้คลาส CSS ชุดเดียวกัน
  (`public/assets/public_report.css`) จึงหน้าตาเหมือนกันสนิท) ใช้ `.report-table-scroll` ตัว
  เดียวกับตารางสรุปยอดรวมด้วย (ตารางผลค้นหาอาจมีหลายแถวเหมือนกัน)
* **ต้องเลือกปีการศึกษาก่อนค้นเสมอ** (`<select name="year">` ดึงจาก `SELECT DISTINCT academic_year
  FROM schools_master`) เพราะทำเนียบเก็บแยกตามปี (`migrations/004_academic_year.sql` — คีย์หลักคือ
  `(academic_year, school_code)`) โรงเรียนเดียวกันอาจเปลี่ยนสังกัด/อำเภอข้ามปีได้ ค้นข้ามปีพร้อมกัน
  ไม่ได้เพราะรหัสเดียวกันคนละปีอาจมีข้อมูลไม่ตรงกัน — เจตนาเดียวกับ `Reporting::schoolMasterOverrides()`
* **แยกชื่อซ้ำด้วยการแสดงคอลัมน์ สังกัด/หน่วยงาน (`area_name`), ต้นสังกัด (`department`), อำเภอ, ตำบล
  กำกับทุกแถวผลค้นหา** (คอลัมน์เดียวกับที่ `Reporting::schoolMasterOverrides()` ใช้) พร้อมนับจำนวนชื่อ
  ที่ซ้ำกันในผลค้นหาที่แสดงอยู่ (`$nameCounts`) แปะ badge "ชื่อซ้ำ" สีแดงเตือนแถวที่มีชื่อสถานศึกษา
  ซ้ำกับแถวอื่นในผลลัพธ์เดียวกัน ให้ผู้ใช้สังเกตง่ายว่าต้องเช็คสังกัด/อำเภอให้ตรงก่อนคัดลอกรหัสไปใช้
* หน้านี้ query `schools_master` ตรง ๆ ไม่ผ่าน `Reporting` class (ไม่เกี่ยวกับ `submissions`/pivot
  เลย) จึงไม่ต้องห่อ try/catch แบบ `schoolMasterOverrides()` (ถ้ายังไม่รัน migration 003/004 หน้านี้
  จะ error ตรง ๆ แทนที่จะเงียบ — ยอมรับได้เพราะหน้านี้ไม่มีประโยชน์อะไรถ้าไม่มีทำเนียบโรงเรียนอยู่แล้ว)
* หน้านี้เป็นหน้าสาธารณะไม่ต้อง login เหมือน `public_report*.php` — แสดงแค่ข้อมูลทำเนียบโรงเรียน
  (รหัส/ชื่อ/สังกัด/อำเภอ/ตำบล) ไม่มีข้อมูลการรายงานผลรายสถานศึกษาใด ๆ ปนอยู่ ปลอดภัยที่จะเปิดเผย
* **ย้ายเมนูสลับ 3 หน้าสาธารณะจาก sidebar ด้านซ้ายขึ้นไปไว้ในแถบบนสุดแทน (แก้เมื่อ 2026-08-29 คำขอ
  ถัดมาทันที)** — ผู้ใช้ขอให้ย้ายเมนูไปด้านบน วางไว้ทางซ้ายของลิงก์ "เข้าสู่ระบบเจ้าหน้าที่" (ลิงก์
  เดิมที่มีอยู่แล้วในแถบบนสุดของทุกหน้าสาธารณะ) แก้ที่ 2 จุด: (1) `render_report_start()` ใน
  `public_report_data.php` — ย้าย `<nav class="report-sidebar">` (3 ลิงก์: ภาพรวม/ตารางสรุป/ค้นหา
  รหัสสถานศึกษา) เข้าไปอยู่ใน `<nav>` ของ `.topbar` เดิม **ก่อน** ลิงก์ "เข้าสู่ระบบเจ้าหน้าที่" (ลำดับ
  ใน HTML กำหนดตำแหน่งซ้าย-ขวา เพราะ `.topbar` เป็น `display:flex` ปกติ) เอา `.report-layout` (ตัวห่อ
  flex สำหรับ sidebar+main) ออกไปเลย เหลือแค่ `.report-main` เป็นตัวห่อเนื้อหาเดียว (2) แก้
  `public_school_search.php` แบบเดียวกัน (หน้านี้เขียน topbar/nav เองไม่ได้เรียก
  `render_report_start()` ตามที่บันทึกไว้ด้านบน จึงต้องแก้ 2 ที่แยกกัน)
  * ลบ CSS `.report-sidebar`/`.report-layout` (รวม media query มือถือของมัน) ออกจาก
    `public/assets/public_report.css` ทั้งหมด ใช้คลาส `.topbar nav a` มาตรฐานของระบบที่มีอยู่แล้วแทน
    (ดู `public/assets/style.css`) เพิ่มแค่กฎใหม่ `.topbar nav a.active` (สีน้ำเงิน/ตัวหนา) ให้ยังเห็น
    ได้ว่ากำลังอยู่หน้าไหนในเมนูบนเหมือนที่ sidebar เดิมเคยไฮไลต์ไว้
  * **ตรวจ div ปิด/เปิดสมดุลแบบเดิม** (นับ `<div>`/`</div>` ของ `render_report_start()`+
    `render_report_end()` รวมกับเนื้อหาของแต่ละหน้าที่เรียกมัน) หลังตัด `.report-layout` ออกไป 1 ชั้น
    ต้องตัด `</div>` ที่เคยปิดมันออกด้วย ไม่งั้น HTML จะปิด tag เกิน — ตรวจแล้วสมดุลทั้ง 3 หน้า
    (shell เปิด/ปิด div 6/6 เอง, รวมกับ `public_report.php` = 27/27, กับ `public_report_table.php`
    = 8/8, และ `public_school_search.php` เขียนเองสมดุลในตัว 9/9)

### หน้าแรกของเว็บ (root "/") เปลี่ยนเป็นหน้าสถิติสาธารณะ (เพิ่มเมื่อ 2026-08-29)
ผู้ใช้แจ้งว่าเข้า URL รากเว็บ (`/` ไม่ระบุชื่อไฟล์) แล้วยังเจอหน้า login อยู่ — สาเหตุคือ Apache ใช้
`index.php` เป็นไฟล์ default ของ directory ตามค่ามาตรฐาน (ไม่มี `.htaccess` มาก่อนเลย) และ
`index.php` เป็นแดชบอร์ดฝั่งเจ้าหน้าที่ที่เรียก `Auth::requireLogin()` เจอไม่ได้ login ก็ redirect ไป
`login.php` ทันที ผู้ใช้อยากให้หน้าแรกเป็น `public_report.php` (สถิติการศึกษาจังหวัด) แทน
* **เพิ่ม `.htaccess` ที่ root โปรเจกต์เป็นครั้งแรก** (ไม่เคยมีมาก่อนเลยทั้งโปรเจกต์) ใส่แค่บรรทัด
  `DirectoryIndex public_report.php index.php` — บอก Apache ว่าถ้าเข้ามาแบบ directory (ไม่ระบุไฟล์)
  ให้ลองหา `public_report.php` ก่อน ถ้าไม่มีค่อย fallback ไป `index.php` **ไม่ได้ย้าย/ลบ/เปลี่ยนชื่อ
  ไฟล์ใดเลย** — `index.php` (แดชบอร์ดฝั่งเจ้าหน้าที่) ยังเข้าได้ปกติทุกประการผ่าน URL ตรง ๆ
  (`/index.php`) การ login ผ่าน `login.php` ก็ redirect ไป `index.php` เหมือนเดิมไม่กระทบ
* **ข้อควรระวัง — ต้องพึ่งการตั้งค่า `AllowOverride` ของ Apache ฝั่งเซิร์ฟเวอร์** `.htaccess` จะมีผล
  ก็ต่อเมื่อ vhost/httpd config อนุญาต override คำสั่ง `DirectoryIndex` ในไดเรกทอรีนั้น (ปกติ Plesk
  จะเปิดให้ไว้แล้วสำหรับ subscription ทั่วไป แต่ไม่ได้ตรวจสอบจริงจากเซิร์ฟเวอร์ เพราะเครื่อง dev นี้
  ไม่มี Apache/PHP รันจริง) **ถ้า deploy แล้วเข้า root ยังเป็นหน้า login เหมือนเดิม ให้ตรวจสอบว่า
  Plesk อนุญาต `.htaccess` override `DirectoryIndex` หรือไม่** (อาจต้องตั้งค่าผ่านหน้า Plesk เอง แทน
  การพึ่ง `.htaccess` อย่างเดียว)

### รวมข้อมูลฟอร์ม 9 (ผู้สำเร็จการศึกษา) เข้าตารางสรุป/กราฟ (เพิ่มเมื่อ 2026-08-29)
ผู้ใช้ถามแนวทางนำตารางที่ 9 มาเผยแพร่ (คำถามเชิงสำรวจ) แนะนำ 2 อย่างแล้วผู้ใช้ให้ลงมือทำเลย:
* **เพิ่ม metric `graduates` ("จำนวนผู้สำเร็จการศึกษา")** เข้าตารางสรุปยอดรวม ดึงจากชีท
  `9.1ผู้สำเร็จการศึกษา` เท่านั้น — ตรวจ `reference_templates/9_ข้อมูลผู้สำเร็จการศึกษา.xlsx` แล้ว
  ชีทนี้เป็นคอลัมน์ระดับชั้น×เพศ ล้วน ไม่มีคอลัมน์ "รวม" ปนอยู่เลย จึงส่ง `null` รวมทุกคอลัมน์ได้เหมือน
  ฟอร์ม 4/10.1/11.1 — ไม่ใช้ 9.2 (สายอาชีพ, ข้อมูลคนละมิติ) หรือ 9.6 (มีงานทำ, identity ไม่ครบ
  `$stdIdentity` มีแค่ `school_code`/`school_name` หั่นตามมิติอื่นไม่ได้ — ดูเหตุผลที่ให้คำแนะนำไว้ก่อน
  ลงมือทำ)
* **เพิ่มกราฟ "สถานะหลังจบการศึกษา" 3 กราฟย่อย (ป.6/ม.3/ม.6 — ชีท 9.3/9.4/9.5)** — **ต่างจาก 8.2
  (นักเรียนพิการแยกตามประเภท) ตรงที่ชีทกลุ่มนี้มีคอลัมน์ "รวม" ปนอยู่ที่ต้น ๆ ตาราง ไม่ใช่ท้ายตาราง**:
  "ทั้งหมด" (= ที่จบ+ที่ไม่จบ) และ "ที่จบการศึกษา"/"ที่ไม่จบการศึกษา" (= ผลรวมของคอลัมน์ปลายทางย่อยที่
  ตามมาทั้งหมด) — `Reporting::sumByColumnPathParts()` ตัดได้แค่ระดับ "เพศ" ท้ายสุดตามพารามิเตอร์เดิม
  ตัดคอลัมน์เฉพาะเจาะจงกลางตารางไม่ได้ จึงต้อง **`unset()` ทั้ง 3 คีย์นี้เองหลังเรียกฟังก์ชัน** (ไม่ได้
  แก้ `sumByColumnPathParts()` เพิ่มพารามิเตอร์ใหม่ เพราะใช้แค่ 3 ที่นี้ที่เดียว เพิ่มความซับซ้อนให้
  ฟังก์ชันกลางไม่คุ้ม) ตรวจคอลัมน์รวมทั้ง 3 ชีทแล้วชื่อคอลัมน์เหมือนกันทุกชีท ("ทั้งหมด"/"ที่จบ
  การศึกษา"/"ที่ไม่จบการศึกษา") แต่คอลัมน์ปลายทางย่อยที่เหลือหลัง unset ไม่เหมือนกัน (ป.6/ม.3/ม.6 มี
  ปลายทางศึกษาต่อ/ประกอบอาชีพต่างกันตามธรรมชาติของแต่ละช่วงชั้น) **จึงแสดงเป็น 3 กราฟแยกกัน (การ์ด
  เดียว 3 คอลัมน์แบบ `.kpi-row`/`.kpi-col` เหมือนกราฟ 5 อันดับออกกลางคันสูงสุด/ต่ำสุด) ไม่รวมเป็นกราฟ
  เดียว** เพราะหมวดหมู่ไม่ตรงกันข้ามชีท รวมกันจะสับสน
* ไม่ได้เพิ่ม 9.1 เข้า `$genderSheets`/`$teacherGenderSheets` เพราะผู้ใช้ไม่ได้ขอ และ metric นี้เป็น
  ตัวเลขใหม่ที่ไม่เคยมีมาก่อน ไม่จำเป็นต้องผูกกับตัวเลข "จำนวนนักเรียน" เดิมให้ยอดตรงกันแบบฟอร์ม 11

### ตารางผู้เรียนรายชั้น หน้าที่ 4 หน้าสาธารณะ (เพิ่มเมื่อ 2026-08-29)
ผู้ใช้ขอตารางแบบ "1 แถวต่อสถานศึกษา 1 คอลัมน์ต่อระดับชั้น" (คำย่อ อ.1, อ.2, ..., ป.เอก) พร้อมเพิ่ม
คอลัมน์ "ผู้เรียน สกร." จากฟอร์ม 11 และ "ผู้เรียนนอกระบบ" จากฟอร์ม 15 ต้องส่งออก CSV ได้ด้วย —
เพิ่ม 3 ไฟล์ใหม่ (แพทเทิร์นเดียวกับ public_report_data.php/public_report.php/public_report_table.php):
* **`public_school_grade_table_data.php`** — ไฟล์ข้อมูลร่วม ระหว่างหน้าเว็บกับ CSV export
  * **แหล่งคอลัมน์ระดับชั้น = ฟอร์ม 4 (`4_students`) เท่านั้น** เป็นฟอร์มเดียวที่แยกระดับชั้นละเอียด
    ขนาดนี้ (37 ระดับ: อ.เตรียม, อ.1-3, เด็กเล็ก, ป.1-6, ม.1-6, ปวช.1-3, อนุป.1-3, ปวส.1-3, ป.ตรี1-6,
    ป.บัณฑิต, ป.โท, ป.บัณฑิตชั้นสูง, ป.เอก, การศึกษาพิเศษ) — **สร้างสคริปต์ Python ใหม่
    (`colpaths.py` ในเครื่อง dev เท่านั้น ไม่ได้ commit เข้า repo) จำลอง
    `Importer::buildColumnPaths()` เป๊ะ ๆ** (รวม logic inherit ค่าจากคอลัมน์ก่อนหน้าตอนเซลล์ว่าง,
    ตัดข้อความซ้ำติดกัน, join ด้วย `' / '`) รันกับ `reference_templates/4_จำนวนนักเรียน.xlsx` จริง
    ได้ column_path ทั้ง 74 คอลัมน์ (37 ระดับ × 2 เพศ) แล้ว **hardcode เป็น `$gradeGroups` ในไฟล์นี้
    ตรง ๆ ทีละคู่ (ชาย, หญิง)** — ปลอดภัยที่จะ hardcode เพราะฟอร์ม 4 ไม่มี
    `merge_extra_columns_into` (ดู `forms/registry.php`) `Importer::assertStructureMatches()` บังคับ
    ทุกไฟล์ที่อัปโหลดผ่านมีต้องมี column_path ตรงเป๊ะกับแม่แบบเสมอ ค่าที่ import เข้า DB จริงจึงตรงกับ
    hardcode นี้เสมอ (แพทเทิร์นเดียวกับ `$genderSheets`/`$teacherGenderSheets` ที่มีอยู่แล้ว) —
    **ตรวจทาน 74 สตริงด้วยสคริปต์เทียบอัตโนมัติ** (diff ค่าจาก `colpaths.py` กับค่าที่ hardcode ไว้
    ทีละคู่ ตรงกันครบ 100%) ไม่ได้ตรวจด้วยตาเปล่าเพราะเสี่ยงพิมพ์ผิด/สลับตำแหน่งได้ง่ายมากกับ string
    ยาวขนาดนี้ — สังเกต 2 คอลัมน์แปลก: "อ.2"/"อ.3" มี column_path ที่มีข้อความซ้อนแปลก ๆ
    (`"ก่อนประถมศึกษา / อนุบาล 2(สช.) / /อนุบาล 1 / ชาย"` มี `//` ซ้อนกัน) เพราะแม่แบบต้นฉบับเขียน
    "อนุบาล 2(สช.)" คู่กับ "/อนุบาล 1" ในคนละแถว header (โรงเรียนรัฐเรียก "อนุบาล 2" แต่เอกชนเรียก
    ปีเดียวกันว่า "อนุบาล 1" นับปีต่างกัน) — คัดลอก column_path ตามที่แม่แบบเขียนจริงเป๊ะ ไม่ได้แก้ไข
  * **ผู้เรียน กศน. (11.1) และผู้เรียนนอกระบบ (15.1-15.3, สช.วิชาชีพ-ครู-นร.) เป็นสถานศึกษาคนละกลุ่ม
    กับฟอร์ม 4 เลย** (ไม่กรอกฟอร์ม 4) จึงต้อง **union แถว (school_code)** จาก 3 แหล่งเข้าด้วยกัน ไม่ใช่
    แค่ union คอลัมน์ — วน `pivot()` ของฟอร์ม 4 ก่อนเป็นฐาน แล้ววนฟอร์ม 11/15 เติมแถวใหม่เฉพาะ
    school_code ที่ยังไม่เจอ (ค่าระดับชั้นเป็น 0 ทั้งหมดสำหรับแถวที่เติมทีหลัง เพราะไม่ได้กรอกฟอร์ม 4)
  * เรียก `pivot()` แค่ครั้งเดียวต่อชีท แล้ว sum คอลัมน์ในลูปเดียวกับที่ดึง identity เอง (ไม่เรียก
    `Reporting::groupedTotal()`/`groupedTotalForColumns()` ซึ่งจะเรียก `pivot()` ซ้ำข้างในอีกที)
  * **ไม่ include `public_report_data.php`** แม้จะมีของซ้ำ (`fmt_num()`, การคำนวณ `$selectedYear`/
    `$availableYears`/`$navQuery`) เพราะไฟล์นั้นไม่ได้ออกแบบให้ include ซ้อนกับไฟล์อื่นที่ต้องการ
    ตัวแปรชุดเดียวกันคนละความหมาย (เช่น `$dimensions`/`$selectedDimension` ที่หน้านี้ไม่ใช้เลย) —
    ยอมก็อป `fmt_num()` มาซ้ำ (ฟังก์ชัน 3 บรรทัด) แทนความซับซ้อนของการแยก shared file ให้ generic ขึ้น
* **`public_school_grade_table.php`** — หน้าเว็บ (ไม่ login) เขียน topbar/nav เองแบบเดียวกับ
  `public_school_search.php` (เหตุผลเดียวกัน: การ์ดหัวหน้า/คำอธิบายของ `render_report_start()` ผูก
  กับเนื้อหาสถิติภาพรวม ไม่ใช่ตารางรายสถานศึกษา อีกทั้งประโยคนึงในนั้นบอกว่า "ไม่มีข้อมูลรายสถานศึกษา"
  ซึ่งขัดกับเนื้อหาหน้านี้ตรง ๆ) ใช้ `.report-table-scroll`/`.stats-table` เดิมจาก
  `public/assets/public_report.css` (ตารางกว้างมาก ~43 คอลัมน์ ต้อง scroll ทั้ง 2 แกนในกรอบเดียว)
* **`public_school_grade_table_export.php`** — ดาวน์โหลด CSV รูปแบบ header/BOM เดียวกับ
  `export.php` ฝั่ง backend ใช้ข้อมูลชุดเดียวกับหน้าเว็บ ไม่คำนวณซ้ำ
* **เพิ่มลิงก์เมนู "ผู้เรียนรายชั้น" เป็นรายการที่ 4 ในเมนูบนสุด** ของทุกหน้าสาธารณะ (แก้
  `render_report_start()` ที่ `public_report_data.php` + แก้ `public_school_search.php` เอง) ต่อจาก
  "ค้นหารหัสสถานศึกษา" ก่อนลิงก์ "เข้าสู่ระบบเจ้าหน้าที่" เหมือนเดิม
* **แก้ข้อความแบรนด์มุมซ้ายบน (แก้เมื่อ 2026-08-29)** — เปลี่ยนจาก "สถิติการศึกษาจังหวัดปัตตานี" เป็น
  "ข้อมูลด้านการศึกษาจังหวัดปัตตานี" ในทุกหน้าสาธารณะที่มีลิงก์นี้ (`render_report_start()` ที่
  `public_report_data.php` ใช้ร่วมกับ `public_report.php`/`public_report_table.php` +
  `public_school_search.php` + `public_school_grade_table.php` เขียน topbar เอง 2 หน้าหลัง) —
  แก้เฉพาะข้อความลิงก์แบรนด์เท่านั้น ไม่ได้แตะ `<title>`/`<h1>` ของแต่ละหน้าที่ยังใช้คำว่า "สถิติ
  การศึกษาจังหวัดปัตตานี" อยู่ตามเดิม (ผู้ใช้ระบุจุดที่ต้องการแก้ชัดเจนแค่ตำแหน่งนี้)

### ค้นหา/กรอง + แถวรวมในตารางผู้เรียนรายชั้น (เพิ่มเมื่อ 2026-08-29)
เพิ่มใน `public_school_grade_table_data.php` (ใช้ร่วมทั้งหน้าเว็บและ CSV export ตามที่ออกแบบไว้แต่แรก):
* **dropdown 3 ตัว**: ค้นหาชื่อสถานศึกษา (text, substring, `stripos()` ธรรมดา — ไม่ใช้ `mb_stripos()`
  เพราะภาษาไทยไม่มีตัวพิมพ์เล็ก/ใหญ่ให้ต้องแปลง และไม่อยากเพิ่ม dependency กับ extension mbstring ที่
  ไม่เคยมีจุดไหนในระบบนี้ใช้มาก่อนเลย), สังกัด/หน่วยงาน (dropdown ค่าตรงเป๊ะ), อำเภอ (dropdown ค่าตรง
  เป๊ะ) — ตัวเลือกใน dropdown ดึงจากข้อมูล**ทั้งหมดก่อนกรอง** เสมอ (เก็บ `$agencyOptions`/
  `$amphoeOptions` ไว้ก่อนกรอง) ไม่งั้นพอเลือกกรองแล้วตัวเลือกอื่นจะหายไปเรื่อย ๆ สลับกลับไม่ได้
* **แถวรวมด้านบนสุดของตาราง** (`$gradeTotals`, class `.row-total`) คำนวณจากผลลัพธ์**หลัง**กรองเสมอ
  (เปลี่ยนตามสถานะการค้นหาตามที่ผู้ใช้งานขอ) — วางเป็นแถวแรกใน `<tbody>` (ไม่ใช่ `<tfoot>` ท้ายตาราง)
  เพราะผู้ใช้อยากเห็นยอดรวมทันทีไม่ต้องเลื่อนหาท้ายตารางที่อาจมีเป็นพันแถว สไตล์เหมือน `tfoot` เดิม
  (ตัวหนา พื้นเทาอ่อน) แค่ selector ต่างกัน — **ไม่ได้ทำให้ sticky ค้างจอตอน scroll** (แค่แถวแรกในตาราง
  เฉย ๆ เลื่อนตามเนื้อหาไปด้วย) เพราะ sticky ซ้อนกับ `th { position: sticky; top: 0 }` ที่มีอยู่แล้ว
  ต้องคำนวณความสูงหัวตารางเป๊ะเพื่อวาง `top` ให้ถูก เสี่ยงพังง่าย ไม่คุ้มความซับซ้อนสำหรับ scope นี้
* **CSV export กรองตามเดียวกับหน้าเว็บเสมอ** — ปุ่มดาวน์โหลดส่ง query string `q`/`agency`/`amphoe`
  ต่อไปให้ `public_school_grade_table_export.php` ด้วย (ไฟล์นั้น include ข้อมูลไฟล์เดียวกัน อ่าน
  `$_GET` ชุดเดียวกัน จึงกรองอัตโนมัติโดยไม่ต้องเขียน logic กรองซ้ำ) — ผู้ใช้ไม่ได้ขอเรื่องนี้ตรง ๆ แต่
  เป็นพฤติกรรมที่คาดหวังได้ตามธรรมชาติ (ดาวน์โหลดสิ่งที่กำลังดูอยู่)
* **dropdown "สังกัด/หน่วยงาน" เปลี่ยนเป็นเลือกได้หลายรายการ (แก้เมื่อ 2026-08-29 คำขอถัดมา)** —
  `<select name="agency[]" multiple size="6">` แทน single-select เดิม `$filterAgency` เปลี่ยนชนิดจาก
  string เป็น array (`array_filter(array_map('trim', (array)($_GET['agency'] ?? [])))` กันค่าว่าง/
  ช่องว่างหลุดเข้ามาปนในรายการ) กรองด้วย `in_array($r['agency_name'], $filterAgency, true)` แทน `===`
  เดิม — เอาตัวเลือก "— ทั้งหมด —" ออกเพราะ multi-select ไม่เลือกอะไรเลยก็คือ "ทั้งหมด" อยู่แล้วในตัว
  (array ว่าง) ไม่ต้องมีตัวเลือกพิเศษ — `http_build_query()` ตัวเดิมที่ส่งต่อไปให้ CSV export จัดการ
  array ให้อัตโนมัติอยู่แล้ว (ได้ `agency[0]=..&agency[1]=..`) ไม่ต้องแก้โค้ดส่วนนั้น — อำเภอยังเป็น
  single-select เหมือนเดิม (ผู้ใช้ขอเจาะจงแค่สังกัด/หน่วยงาน)

### แยกชาย/หญิงในตารางผู้เรียนรายชั้น (เพิ่มเมื่อ 2026-08-29)
ผู้ใช้ขอให้ตารางผู้เรียนรายชั้นแยกเป็นชาย/หญิงด้วย (เดิมรวมชาย+หญิงเป็นยอดเดียวต่อระดับชั้น):
* **ระดับชั้นทั้ง 37 คอลัมน์ (ฟอร์ม 4) แยกชาย/หญิงได้สะอาด** เพราะ `$gradeGroups` เก็บ column_path
  คู่ (ชาย, หญิง) แยกกันอยู่แล้วตั้งแต่แรก (แค่เคยเอามาบวกรวมกันตอนแสดงผล) เปลี่ยน
  `$row['grades'][$label]` จาก float เดี่ยว เป็น `['male'=>.., 'female'=>..]`
* **"ผู้เรียน สกร." (11.1) แยกได้สะอาดเหมือนกัน** เพราะทุกคอลัมน์ของชีทนี้ลงท้าย "ชาย"/"หญิง" เสมอ
  (ไม่มีคอลัมน์ "รวม" ปน) จับคู่ด้วย regex ปลาย column_path (`/ชาย$/u`, `/หญิง$/u`) แบบเดียวกับ
  `Reporting::genderTotalsForColumns()` แทนการรวมทุกคอลัมน์แบบเดิม
* **"ผู้เรียนนอกระบบ" (ฟอร์ม 15) ไม่แยก ยังเป็นยอดรวมเดียวเหมือนเดิม** — เพราะ 1 ใน 4 ชีทที่รวมกันอยู่
  ("สช.วิชาชีพ-ครู-นร.") มีแค่คอลัมน์ "จำนวนนักเรียน / รวม" อย่างเดียว ไม่มีคอลัมน์แยกเพศในต้นฉบับเลย
  ถ้าแยกเฉพาะอีก 3 ชีทที่ทำได้แล้วทิ้งชีทนี้ไป ยอดรวมจะไม่ตรงกับตัวเลขที่เคยแสดงไว้ก่อนหน้า —
  เลือกคงเป็นยอดรวมเดียวไว้ ไม่เดาแบ่งสัดส่วน (documented ไว้ในโค้ดเป็นข้อยกเว้นชัดเจน)
* **หัวตาราง 2 แถว** (แถวบน = ชื่อระดับชั้น กว้าง 2 คอลัมน์ `colspan="2"`, แถวล่าง = "ชาย"/"หญิง" ต่อ
  ระดับ) คอลัมน์ identity (รหัส/ชื่อ/สังกัด/อำเภอ) และ "ผู้เรียนนอกระบบ" ใช้ `rowspan="2"` แทน
  * **บั๊กที่ต้องระวัง**: `style.css` ตั้ง `th { position: sticky; top: 0 }` ไว้ใช้กับตารางหัวแถวเดียว
    ทั่วทั้งเว็บ ถ้าใช้กับตารางนี้ตรง ๆ (2 แถวหัวตาราง) แถวที่ 2 (ชาย/หญิง) จะ sticky top:0 ทับซ้อน
    แถวที่ 1 (ชื่อระดับชั้น) แทนที่จะเรียงต่อกันตามปกติเวลา scroll ในกรอบ `.report-table-scroll` —
    แก้โดยเพิ่มคลาส `.two-row-header` เฉพาะตารางนี้ ปิด sticky รายตัวของ `th`
    (`position: static`) แล้ว sticky ทั้ง `<thead>` เป็นก้อนเดียวแทน (เบราว์เซอร์สมัยใหม่รองรับ
    `position: sticky` บน `<thead>` ได้ ไม่ต้องคำนวณความสูงแถวเป็น px เอง) — ดู
    `public/assets/public_report.css`
* **CSV export** หัวคอลัมน์เปลี่ยนเป็น "ชื่อระดับชั้น (ชาย)"/"(หญิง)" แยกกัน เหมือนหน้าเว็บ
* **เพิ่มคอลัมน์ "รวม" ต่อจาก "อำเภอ" (แก้เมื่อ 2026-08-29 คำขอถัดมา)** — ผลรวมทุกอย่างของสถานศึกษา
  แถวนั้น (ทุกระดับชั้นชาย+หญิง + ผู้เรียน สกร. ชาย+หญิง + ผู้เรียนนอกระบบ) คำนวณเป็น `$row['grand_total']`
  **ในลูปเดียวหลังรวมข้อมูลทั้ง 3 แหล่ง (ฟอร์ม 4/11/15) เข้าแถวเดียวกันเสร็จสมบูรณ์แล้วเท่านั้น**
  (ไม่คำนวณแทรกในแต่ละ pass ที่ดึงข้อมูลจากฟอร์มต่าง ๆ เพราะเสี่ยงคำนวณตอนข้อมูลยังไม่ครบ/พลาดพวก
  แถวที่เพิ่มเข้ามาทีหลังจากฟอร์ม 11/15) แถวรวมด้านบนตาราง (`$gradeTotals['grand_total']`) ก็รวมค่านี้
  จากผลลัพธ์หลังกรองเหมือนคอลัมน์อื่น — วางเป็น `rowspan="2"` คู่กับคอลัมน์ identity (รหัส/ชื่อ/
  สังกัด/อำเภอ) ก่อนคอลัมน์ระดับชั้นชุดแรก ทั้งหน้าเว็บและ CSV export

### ตารางครูผู้สอนรายชั้น หน้าที่ 5 หน้าสาธารณะ (เพิ่มเมื่อ 2026-08-29)
ผู้ใช้ขอ "ทำตารางแบบนี้กับข้อมูล 'ครูผู้สอน' และเพิ่มไปในหน้าใหม่" — คือทำตารางแบบเดียวกับตารางผู้เรียน
รายชั้น (`public_school_grade_table*.php`) ทุกฟีเจอร์ (ค้นหา/กรอง 3 ตัว, แถวรวม, แยกชาย/หญิง, คอลัมน์
"รวม", CSV export) แค่สลับแหล่งข้อมูลเป็นฝั่งครู — เพิ่ม 3 ไฟล์ใหม่คู่กัน
(`public_teacher_grade_table_data.php`/`.php`/`_export.php`) โครงสร้างโค้ดเหมือนกันเป๊ะกับตาราง
ผู้เรียนรายชั้น (ก็อปแล้วแก้เฉพาะแหล่งข้อมูล ไม่ได้พยายามรวม 2 ตารางให้ใช้โค้ดชุดเดียวกันแบบ generic
เพราะโครงสร้างคอลัมน์กลาง [7 ระดับ vs 37 ระดับ] และจำนวนคอลัมน์เสริมท้ายตาราง [2 vs 2 แต่คนละที่มา]
ต่างกันมากพอที่ generic ไปจะซับซ้อนกว่าก็อปตรง ๆ):
* **แหล่งคอลัมน์ระดับชั้นหลัก = ฟอร์ม 10.2 (`10_teachers` / ชีท "10.2ทุกสังกัด")** เท่านั้น เป็นชีท
  เดียวในฟอร์ม 10 ที่แยกครู/ผู้สอนตามระดับชั้น (ชีทอื่นของฟอร์ม 10 แยกตามตำแหน่ง/อันดับ-วิทยฐานะ/
  วุฒิการศึกษา/วิชาเอกแทน คนละมิติ) **มีแค่ 7 ระดับ** (ก่อนอนุบาล, อนุบาล, ประถม, ม.ต้น, ม.ปลาย,
  ปวช., ปวส.) ไม่ละเอียดเท่าฟอร์ม 4 ของผู้เรียนที่มี 37 ระดับ เพราะครู 1 คนสอนได้หลายชั้นปีในระดับ
  เดียวกัน ฟอร์มนี้เลยรายงานเป็นช่วงชั้นกว้าง ๆ แทนที่จะแยกทีละปีเหมือนฟอร์ม 4 — ตรวจ column_path
  ทั้ง 14 คอลัมน์ (7×2 เพศ) เทียบ `reference_templates/10_ข้อมูลครูV2.xlsx` จริงด้วยสคริปต์อัตโนมัติ
  ก่อน hardcode เหมือนตารางผู้เรียนรายชั้น (`identity_cols=6, header_rows=5, skip_rows=[1,2]`
  ตามที่ระบุไว้ใน `forms/registry.php`)
* **คอลัมน์เสริมท้ายตาราง**: "ครู ศพด." จากฟอร์ม 14 (คอลัมน์ `จำนวนครู/ผู้ดูแลเด็ก (คน) / ชาย,หญิง`
  — แยกชาย/หญิงได้สะอาด รู้ชื่อคอลัมน์ตรง ๆ ไม่ต้องเดาด้วย regex) และ "ครูนอกระบบ" จากฟอร์ม 15
  (คอลัมน์ผู้สอน/โต๊ะครู 4 ชีท — **ไม่แยกชาย/หญิง** เหตุผลเดียวกับ "ผู้เรียนนอกระบบ" ของตารางผู้เรียน
  รายชั้น: ชีท "สช.วิชาชีพ-ครู-นร." มีแค่ "จำนวนครู / รวม" ไม่แยกเพศในต้นฉบับเลย) — ทั้งคู่คัดลอก
  column_path มาจาก metric "จำนวนครู/บุคลากรทางการศึกษา" ที่ตรวจสอบไว้แล้วใน `public_report_data.php`
  ตรง ๆ ไม่ต้องตรวจซ้ำ
* **ไม่มี "ครู สกร." จากฟอร์ม 11** (ต่างจากตารางผู้เรียนรายชั้นที่มี "ผู้เรียน สกร.") เพราะฟอร์ม 11
  (11.1-11.5) เก็บแต่ข้อมูลผู้เรียน กศน. ไม่มีชีทไหนเก็บจำนวนครู/บุคลากรเลย — ไม่ได้ลืมใส่ แต่ไม่มี
  ข้อมูลนี้อยู่จริงในระบบ
* เพิ่มลิงก์เมนู "ครูผู้สอนรายชั้น" เป็นรายการที่ 5 ในเมนูบนสุดของทุกหน้าสาธารณะ (แก้
  `render_report_start()` + `public_school_search.php` + `public_school_grade_table.php` เอง) ต่อจาก
  "ผู้เรียนรายชั้น" ก่อนลิงก์ "เข้าสู่ระบบเจ้าหน้าที่"
* **ปรับโครงสร้างใหม่ทันทีในคำขอถัดมา (แก้เมื่อ 2026-08-29)** — ผู้ใช้เปลี่ยนใจจากคอลัมน์หลักแยกตาม
  ระดับชั้น (ฟอร์ม 10.2 ข้างบน) เป็น **คอลัมน์เดียว "ครูผู้สอน" แยกชาย/หญิง จากฟอร์ม 10.1 แทน** —
  เฉพาะกลุ่ม "ครูที่ปฏิบัติการสอน(1)" 4 ตำแหน่งย่อย (ข้าราชการ(2), พนักงาน(3), ครูประจำตามสัญญา(4),
  ครูพิเศษ(5)) รวมกัน **ไม่รวมผู้บริหาร** (ผอ.รร./รอง ผอ.รร.) **และไม่รวมบุคลากรสนับสนุน** (ลูกจ้าง
  ประจำ/พนักงานราชการ/ลูกจ้างชั่วคราว/พนักงานจ้างภารกิจ/พนักงานจ้างทั่วไป/จ้างเหมาบริการ/พนักงานจ้าง
  ผู้เชี่ยวชาญพิเศษ) ตามที่ผู้ใช้ระบุชัดเจน — ตรวจ column_path ทั้ง 8 คอลัมน์ (4 ตำแหน่งย่อย×2 เพศ)
  เทียบ `reference_templates/10_ข้อมูลครูV2.xlsx` ชีท "10.1ทุกสังกัด" จริงด้วยสคริปต์อัตโนมัติก่อน
  hardcode (จับคู่ด้วย regex ปลาย column_path `/ชาย$/u`/`/หญิง$/u` แบบเดียวกับที่ใช้กับฟอร์ม 11.1)
  * **ผลคือตารางนี้ไม่มีคอลัมน์แยกตามระดับชั้นอีกต่อไปแล้ว** (ฟอร์ม 10.1 ไม่มีมิติระดับชั้นให้แยก)
    เหลือแค่ 1 ตัวเลข "ครูผู้สอน" ต่อสถานศึกษา แยกเพศ — **เอา 2-row header/colspan/rowspan/คลาส
    `.two-row-header` ที่ลอกมาจากตารางผู้เรียนรายชั้นออกทั้งหมด** เหลือหัวตารางแถวเดียวธรรมดา
    (label รวมชื่อ+เพศไว้ในคอลัมน์เดียวกันเลย เช่น "ครูผู้สอน (ชาย)") เพราะไม่มีกลุ่มคอลัมน์ย่อยให้
    ต้องคร่อมหัวตารางอีกต่อไป
  * คอลัมน์เสริมท้ายตาราง ("ครู ศพด." จากฟอร์ม 14, "ครูนอกระบบ" จากฟอร์ม 15) และคอลัมน์ "รวม"/แถวรวม/
    ค้นหา-กรอง/CSV export **ไม่เปลี่ยน** ยังทำงานเหมือนเดิมทุกประการ แค่ตัดส่วนระดับชั้นออกไป

### การ์ดยอดรวม "จำนวนผู้เรียน"/"จำนวนผู้สอน" บนสุดหน้าภาพรวม (เพิ่มเมื่อ 2026-08-29)
เพิ่ม `.kpi-row` ใหม่ 1 การ์ดไว้บนสุดของ `public_report.php` (ก่อนการ์ดสัดส่วนชาย/หญิงเดิม) แสดงตัวเลข
รวมทั้งจังหวัดใหญ่ ๆ 2 ตัว: "จำนวนผู้เรียนทั้งหมด" และ "จำนวนผู้สอนทั้งหมด" ใช้คลาส `.stat-value`/
`.stat-sub` แบบเดียวกับ tile "อัตราส่วนนักเรียนต่อครู/บุคลากร" ที่มีอยู่แล้วด้านล่าง
* **ใช้ตัวแปร `$totalStudents`/`$totalTeachers` ตัวเดิมที่คำนวณไว้แล้ว** ใน `public_report_data.php`
  (ผลรวมจาก metric `students`/`teachers` เดียวกับที่ใช้ในตารางสรุปยอดรวมและ tile อัตราส่วนที่มีอยู่
  แล้ว) ไม่ได้คำนวณใหม่
* **[แก้ไขในคำขอถัดมาทันที — ดูหัวข้อ "รวม metric teachers ให้ตรงกับตารางครูผู้สอน" ด้านล่าง]** ตอน
  แรกตั้งใจไม่ผูก `$totalTeachers` กับนิยาม "ครูผู้สอน" แคบ ๆ ของตารางครูผู้สอนรายชั้น แต่ผู้ใช้ขอกลับ
  ให้ตรงกันแทน — เก็บ bullet เดิมไว้เป็นบันทึกเหตุผลที่เคยคิดตอนแรก (เผื่อมีคนสงสัยว่าทำไมเคยแยกกัน)

### รวม metric `teachers` ให้ตรงกับตารางครูผู้สอน (แก้เมื่อ 2026-08-29)
ผู้ใช้แจ้งว่ายอดรวม "จำนวนผู้สอนทั้งหมด" (การ์ดที่เพิ่งเพิ่มบนหน้าภาพรวม) ต้องตรงกับตัวเลขในตาราง
ครูผู้สอน (`public_teacher_grade_table.php`) — เดิมไม่ตรงกันเพราะ metric `teachers` ของ
`public_report_data.php` (ใช้ร่วมทั้งตารางสรุปยอดรวม, tile อัตราส่วนนักเรียนต่อครู, และการ์ดใหม่)
ยังรวมทุกตำแหน่งของฟอร์ม 10.1 (ผู้บริหาร+ครูผู้สอน+บุคลากรสนับสนุน) อยู่ ขณะที่ตารางครูผู้สอนใช้แค่
กลุ่ม "ครูที่ปฏิบัติการสอน" เท่านั้น (แก้ไปแล้วในคำขอก่อนหน้า) — แก้ให้ตรงกันโดย:
* **ย้าย `$teachingColumns` (8 คอลัมน์: 4 ตำแหน่งย่อย × 2 เพศ ของกลุ่ม "ครูที่ปฏิบัติการสอน(1)")**
  จาก `public_teacher_grade_table_data.php` มาประกาศซ้ำใน `public_report_data.php` ด้วย (ก่อน
  `$metrics` เพื่อให้ `$metrics['teachers']` เรียกใช้ได้) — ก็อปสตริงตรง ๆ ไม่ต้องตรวจซ้ำเพราะตรวจ
  เทียบ `reference_templates/` แล้วตอนสร้างตารางครูผู้สอน
* **`$metrics['teachers']['sheets'][0]`** เปลี่ยนจาก `['10_teachers', '10.1ทุกสังกัด', null]` (รวม
  ทุกคอลัมน์) เป็น `['10_teachers', '10.1ทุกสังกัด', $teachingColumns]` (เฉพาะครูผู้สอน) — เปลี่ยน
  label metric จาก "จำนวนครู/บุคลากรทางการศึกษา (คน)" เป็น "จำนวนครูผู้สอน (คน)" ให้ตรงกับนิยามใหม่
  (label นี้ไหลไปที่หัวคอลัมน์ตารางสรุปยอดรวม/CSV export อัตโนมัติเพราะดึงจาก `$metrics` โดยตรง)
* **`$teacherGenderSheets`** (ใช้ทำกราฟสัดส่วนครูชาย:หญิง) ก็เปลี่ยน sheet แรกจาก `null` เป็น
  `$teachingColumns` เช่นกัน ไม่งั้นผลรวมชาย+หญิงของกราฟจะไม่เท่ากับ `$totalTeachers` ที่ใช้ที่อื่น
  ในหน้าเดียวกัน
* คอลัมน์ฟอร์ม 14/15 ใน metric `teachers` **ไม่เปลี่ยน** (childcare + private nonformal teacher —
  ตรงกับตารางครูผู้สอนอยู่แล้วตั้งแต่แรก)
* ผลลัพธ์: `$totalTeachers` (การ์ด+tile อัตราส่วน), แถวตาราง "จำนวนครูผู้สอน" ในตารางสรุปยอดรวม, และ
  `$gradeTotals['grand_total']` ในตารางครูผู้สอน ตอนนี้คำนวณจากชุดคอลัมน์เดียวกันทั้งหมด ควรได้ตัวเลข
  รวมทั้งจังหวัดตรงกัน (ไม่ได้รันทดสอบจริงเพราะเครื่อง dev นี้ไม่มี PHP — ตรวจแค่โค้ด/เหตุผลตรงกัน)

### เปลี่ยนชื่อเมนู "ครูผู้สอนรายชั้น" เป็น "ครูผู้สอน" (แก้เมื่อ 2026-08-29)
แก้ลิงก์เมนูบนสุดของทุกหน้าสาธารณะ (4 จุด: `render_report_start()`, `public_school_search.php`,
`public_school_grade_table.php`, `public_teacher_grade_table.php` เอง) จาก "ครูผู้สอนรายชั้น" เป็น
"ครูผู้สอน" — ไม่ได้แตะชื่อไฟล์/`<title>`/`<h1>` ของหน้า (ยังคงคำว่า "ตารางครูผู้สอน" อยู่ในเนื้อหา
หน้านั้นเอง) แก้แค่ข้อความลิงก์เมนูตามที่ผู้ใช้ระบุ

### ย้ายเมนู "ค้นหารหัสสถานศึกษา" ไปหลัง "ครูผู้สอน" (แก้เมื่อ 2026-08-29)
สลับลำดับลิงก์เมนูบนสุดจาก ภาพรวม → ตารางสรุปยอดรวม → **ค้นหารหัสสถานศึกษา** → ผู้เรียนรายชั้น →
ครูผู้สอน → เข้าสู่ระบบ เป็น ภาพรวม → ตารางสรุปยอดรวม → ผู้เรียนรายชั้น → ครูผู้สอน →
**ค้นหารหัสสถานศึกษา** → เข้าสู่ระบบ แก้ทั้ง 4 จุดที่มีเมนูนี้ซ้ำกัน (`render_report_start()` ที่
`public_report_data.php`, `public_school_search.php`, `public_school_grade_table.php`,
`public_teacher_grade_table.php` เขียน topbar เอง) ให้ลำดับตรงกันทุกหน้า

### แก้ไขความเข้าใจผิด: "ครูนอกระบบ" (ฟอร์ม 15) แยกชาย/หญิงได้จริง (แก้เมื่อ 2026-08-29)
**บันทึกความผิดพลาดที่เคยเขียนไว้ก่อนหน้านี้ผิด** — ตอนสร้างตารางครูผู้สอน (`public_teacher_grade_table*.php`)
และ metric `teachers` เคยเขียนไว้ว่าชีท "สช.วิชาชีพ-ครู-นร." ของฟอร์ม 15 "มีแค่คอลัมน์ 'จำนวนครู / รวม'
อย่างเดียว ไม่ได้แยกเพศไว้เลยในต้นฉบับ" — **ผิด** ผู้ใช้ทักท้วงว่าน่าจะแยกเพศได้ ตรวจซ้ำด้วย
`colpaths.py` เทียบ `reference_templates/15_ข้อมูลโรงเรียนเอกชนนอกระบบ.xlsx` จริงแล้วพบว่าชีทนี้มีครบ
ทั้ง 3 คอลัมน์: `"จำนวนครู / ชาย"`, `"จำนวนครู / หญิง"`, `"จำนวนครู / รวม"` (เหมือนชีท 15.1-15.3 ทุก
ประการ — "รวม" เป็นผลบวกของอีก 2 คอลัมน์ ไม่ใช่มิติอิสระ ตรงกับที่ `forms/registry.php` บันทึกเหตุผล
ไว้อยู่แล้วว่าทำไมไม่ใช้ `value_split_last`) **สาเหตุที่เข้าใจผิด**: ตอนออกแบบ metric `teachers`/
`students` ครั้งแรกเลือกใช้แค่ "รวม" อย่างเดียวไป (ถูกต้องสำหรับยอดรวมเฉย ๆ เพราะ รวม=ชาย+หญิง) แต่
ตอนคัดลอกคอมเมนต์มาอธิบายที่ตารางครูผู้สอนใหม่ กลับเขียนสรุปเกินจริงไปว่า "ไม่มีคอลัมน์แยกเพศในต้น
ฉบับเลย" ทั้งที่ไม่เคยตรวจสอบจริงจากไฟล์ (สันนิษฐานจากที่ตัวเองเคยใช้ "รวม" มาก่อน) — **บทเรียน**:
ต้องตรวจ column_path จริงทุกครั้งก่อนสรุปว่าฟิลด์ไหน "มี"/"ไม่มี" ไม่ใช่เดาจากโค้ดเก่าที่เคยเขียนไว้
* แก้ 3 จุดให้ใช้ `['จำนวนครู / ชาย', 'จำนวนครู / หญิง']` แทน `['จำนวนครู / รวม']`: metric `teachers`
  ใน `public_report_data.php` (บรรทัดที่ 106 เดิม), `$privateNonformalTeacherSheets` ใน
  `public_teacher_grade_table_data.php` (`$teacherGenderSheets` ใช้แบบนี้อยู่แล้วตั้งแต่แรก ไม่ต้อง
  แก้) — ผลรวมไม่เปลี่ยน (รวม=ชาย+หญิงอยู่แล้ว) แต่ตอนนี้ตัวเลข "ครูนอกระบบ" ในตารางครูผู้สอนแยกเป็น
  ชาย/หญิง 2 คอลัมน์ได้แล้ว (`private_nonformal_total` เปลี่ยนจาก float เดี่ยวเป็น
  `['male'=>..,'female'=>..]` ทั้งใน row และ `$gradeTotals` — อัปเดตหน้าเว็บ+CSV export ตาม)
* **ตรวจแล้ว: ยอดรวม "จำนวนผู้สอนทั้งหมด" (การ์ดหน้าภาพรวม), แถวตารางสรุป "จำนวนครูผู้สอน", สัดส่วน
  ครูชาย:หญิง (การ์ดหน้าภาพรวม), และผลรวมในตารางครูผู้สอน ตอนนี้คำนวณจากชุดคอลัมน์เดียวกันทั้งหมด
  ทั้ง 4 จุด** ควรได้ตัวเลขรวมทั้งจังหวัดตรงกันเป๊ะ (ไม่ได้รันทดสอบจริงเพราะเครื่อง dev ไม่มี PHP —
  ตรวจแค่โค้ด/แหล่งข้อมูลตรงกัน)
* **พบจุดเดียวกันฝั่งผู้เรียนด้วย** (`$metrics['students']` ใน `public_report_data.php` เคยใช้
  `['จำนวนนักเรียน / รวม']` สำหรับชีทนี้เหมือนกัน) — แก้ metric `students` ให้ใช้ ชาย/หญิง แทนแล้ว
  (สอดคล้องกับ `$genderSheets` ที่ใช้แบบนี้อยู่แล้ว ผลรวมไม่เปลี่ยน) — ตอนนั้นยังไม่ได้แก้คอลัมน์
  "ผู้เรียนนอกระบบ" ในตารางผู้เรียนรายชั้นเอง แต่ผู้ใช้ขอให้ทำต่อทันทีในคำขอถัดมา ดูหัวข้อถัดไป

### แยกชาย/หญิงคอลัมน์ "ผู้เรียนนอกระบบ" ในตารางผู้เรียนรายชั้น (แก้เมื่อ 2026-08-30)
ทำแบบเดียวกับที่แก้ "ครูนอกระบบ" ในตารางครูผู้สอนไปก่อนหน้านี้ทุกประการ (คัดลอกแพทเทิร์นตรง ๆ) ใน
`public_school_grade_table_data.php`:
* `$privateNonformalSheets` sheet สุดท้าย ("สช.วิชาชีพ-ครู-นร.") เปลี่ยนจาก `['จำนวนนักเรียน / รวม']`
  เป็น `['จำนวนนักเรียน / ชาย', 'จำนวนนักเรียน / หญิง']`
* `private_nonformal_total` เปลี่ยนจาก float เดี่ยว เป็น `['male'=>..,'female'=>..]` ทั้งใน
  `grade_table_blank_row()`, การสะสมยอด (จับคู่ด้วย regex ปลาย column_path `/ชาย$/u`/`/หญิง$/u`),
  `grand_total` ต่อแถว, และ `$gradeTotals`
* หน้าเว็บ (`public_school_grade_table.php`): กลุ่มหัวตาราง "ผู้เรียนนอกระบบ" เปลี่ยนจาก
  `rowspan="2"` 1 คอลัมน์ เป็น `colspan="2"` (แถวบน) + ชาย/หญิง (แถวล่าง) แบบเดียวกับกลุ่ม
  "ผู้เรียน สกร." ที่อยู่ข้าง ๆ กัน — แถวข้อมูล/แถวรวมเพิ่มเป็น 2 คอลัมน์ต่อแถวตามไปด้วย
* CSV export (`public_school_grade_table_export.php`): หัวคอลัมน์แยกเป็น "ผู้เรียนนอกระบบ (ชาย)"/
  "(หญิง)" 2 คอลัมน์เหมือนกัน
* ตอนนี้ทุกคอลัมน์ในตารางผู้เรียนรายชั้นแยกชาย/หญิงได้ครบหมดแล้ว ไม่มีคอลัมน์ไหนเหลือเป็นยอดรวมเดียว
  อีกต่อไป (เหมือนตารางครูผู้สอนที่แก้ไปก่อนหน้านี้)

### เพิ่มฟอร์ม 16 — ข้อมูล พมจ. (เพิ่มเมื่อ 2026-08-30)
ผู้ใช้ส่งไฟล์แม่แบบใหม่ `16_ข้อมูล_พมจ_V2.xlsx` (2 ชีท) ให้เพิ่มระบบ upload — **ต่างจากฟอร์ม 1-15 ทุก
ฟอร์มตรงที่ไม่มีข้อมูลรายสถานศึกษาเลย** เป็นข้อมูลจากสำนักงานพัฒนาสังคมและความมั่นคงของมนุษย์จังหวัด
(พมจ.) คนละหน่วยงานกับสถานศึกษา:
* **ชีท "พมจ-16.1"** (จำนวนผู้พิการช่วงอายุ 6-11 ปี แยกตามความพิการ/เพศ) — **ไม่มีทั้งรหัสสถานศึกษา
  และชื่อหน่วยงานเลย** เป็นข้อมูลระดับพื้นที่ล้วน ๆ (1 แถวต่อ 1 ตำบล) `identity_cols=2,
  identity_fields=['tambon','amphoe']` (ตรวจ column_path ด้วย `colpaths.py` เทียบไฟล์จริงแล้ว 16
  คอลัมน์ = 8 ประเภทความพิการ × 2 เพศ ไม่มี "รวม" ปน)
  * **ปัญหาที่เจอ**: `Importer::deleteExistingSubmission()` เดิมมีคีย์กันข้อมูลซ้ำตอน reupload แค่ 2
    แบบ — `school_code` (ถ้ามี) หรือ `agency_name`+`school_name`+`extra_identity` (ถ้ามี
    agency_name) — **ชีทนี้ไม่มีทั้งคู่เลย** ถ้าปล่อยไว้แบบเดิม เงื่อนไข `elseif ($agencyName)` จะ
    เป็นเท็จเสมอ ไม่มีการลบแถวเก่าก่อน insert แถวใหม่เลย ทำให้ reupload ไฟล์เดิมซ้ำจะสะสมแถวซ้ำ
    เพิ่มขึ้นเรื่อย ๆ ไม่มีวันถูกแทนที่ (ต่างจากทุกฟอร์มอื่นที่ reupload แล้วแทนที่ข้อมูลเดิมสะอาด) —
    **แก้โดยเพิ่ม fallback ที่ 3 ใน `deleteExistingSubmission()` (src/Importer.php)**: ถ้าไม่มีทั้ง
    `school_code` และ `agency_name` แต่มี `tambon` ให้ใช้ `tambon`+`amphoe` (NULL-safe `<=>`) เป็น
    คีย์กันซ้ำแทน — เปลี่ยน signature ฟังก์ชันเพิ่มพารามิเตอร์ `$tambon`/`$amphoe` (จุดเรียกใช้งานจุด
    เดียวในไฟล์ อัปเดตแล้ว) **การเปลี่ยนนี้ไม่กระทบฟอร์มเดิมทั้ง 15 ฟอร์มเลย** เพราะทุกฟอร์มเดิมมีอย่าง
    น้อย school_code หรือ agency_name อยู่แล้ว จะเข้า 2 branch แรกเหมือนเดิมเป๊ะ ไม่มีทางตกไปถึง
    branch ที่ 3 ใหม่นี้ได้
* **ชีท "พมจ-16.2"** (สถานรับเลี้ยงเด็กเอกชน ผู้เรียนปฐมวัย+ครู/ผู้สอน สังกัด พมจ.) — แม่แบบกรอกรายชื่อ
  สถานรับเลี้ยงเด็กที่ทราบอยู่แล้วไว้ล่วงหน้าในคอลัมน์ "สังกัด" (5 แห่งคงที่ ไม่ใช่รหัสสถานศึกษา)
  หน่วยงานกรอกแค่ตัวเลขแต่ละแถว — ใช้ `identity_cols=1, identity_fields=['agency_name']` ตรงไปตรงมา
  เหมือนฟอร์ม 1 (ไม่มีรหัสสถานศึกษาเหมือนกัน) ไม่ต้องแก้ Importer เพิ่มเพราะ `agency_name` มีค่าอยู่
  แล้ว เข้า branch เดิมที่มีอยู่แล้วได้ปกติ
* คัดลอกไฟล์แม่แบบไปไว้ที่ `reference_templates/16_ข้อมูล_พมจ_V2.xlsx` (สำหรับ
  `Importer::assertStructureMatches()` เทียบโครงสร้างตอนอัปโหลดจริง) — ไม่ต้องแก้ไฟล์อื่นเลย
  (`index.php`/`upload.php`/`view.php`/`export.php` ทั้งหมดอ่านจาก `form_registry()` แบบ generic
  อัตโนมัติอยู่แล้ว) **ไม่ได้เพิ่มฟอร์มนี้เข้า `$metrics`/`public_report_data.php`** เพราะไม่ใช่ข้อมูล
  รายสถานศึกษา ไม่เข้ากับโครงสร้างหน้าสถิติสาธารณะที่มีอยู่ (แยกตามสังกัด/หน่วยงาน/อำเภอของสถานศึกษา)
  — ถ้าจะเอาไปแสดงต้องออกแบบหน้าใหม่แยกต่างหาก (ไม่ได้ขอในงานนี้)

### ฟอร์ม 16.2 — จับคู่ชื่อศูนย์พัฒนาเด็กกับทำเนียบโรงเรียนหารหัสสถานศึกษาให้อัตโนมัติ (แก้เมื่อ 2026-08-30)
ผู้ใช้ขอปรับฟอร์ม 16.2 (สถานรับเลี้ยงเด็กเอกชนสังกัด พมจ.) เพิ่มเติมจากที่เพิ่งเพิ่มไป — เดิมเก็บชื่อ
ศูนย์ไว้ในคอลัมน์ `agency_name` เพราะไม่มีรหัสสถานศึกษาให้กรอกอยู่แล้ว ผู้ใช้อยากให้ (1) ชื่อศูนย์ไป
อยู่ในคอลัมน์ "ชื่อสถานศึกษา" แทน (2) จับคู่ชื่อนั้นกับทำเนียบโรงเรียน (`schools_master`) หารหัส
สถานศึกษาให้เอง — **เพิ่มความสามารถใหม่ในระดับ engine กลาง (`src/Importer.php`) ไม่ใช่แค่แก้
registry.php** เพราะเดิมระบบไม่เคยรองรับการ "หารหัสจากชื่อ" มาก่อนเลย (ทุกฟอร์มเดิมที่มี school_code
ต้องกรอกรหัสมาในไฟล์ตรง ๆ เท่านั้น):
* **`identity_fields`** ของ `พมจ-16.2` เปลี่ยนจาก `['agency_name']` เป็น `['school_name']`
* **เพิ่ม registry key ใหม่ `match_school_code_by_name => true`** (เฉพาะชีทที่ต้องใช้ฟีเจอร์นี้ —
  ฟอร์มอื่นทั้งหมดไม่ตั้ง flag นี้ ไม่มีผลอะไรกับฟอร์มเดิมเลย)
* **`Importer::schoolCodesByName(int $academicYear): array`** เมธอดใหม่ (cached ต่อปีเหมือน
  `knownSchoolCodes()` ที่มีอยู่แล้ว) — สร้าง map `school_name (trim) => school_code` จาก
  `schools_master` ของปีนั้น **ถ้าชื่อซ้ำกันมากกว่า 1 รหัส ตั้งใจ map เป็น `null` (ไม่เดา)** ดีกว่าจับคู่
  ผิดโรงเรียนแบบเงียบ ๆ
* ใน `importSheet()` (จุดที่ประกอบ `$standard` เสร็จแล้ว ก่อนเช็ก `school_code_issue`) — ถ้า sheet
  ตั้ง `match_school_code_by_name` และ `school_code` ยังว่าง ให้ค้นด้วยชื่อจาก `schoolCodesByName()`
  แล้วเซ็ต `$standard['school_code']` ถ้าเจอ **เงื่อนไขตรวจ `school_code_issue` ก็ขยายให้ครอบคลุมชีท
  แบบนี้ด้วย** (`in_array('school_code', $identityFields, true) || $matchByName`) จับคู่ไม่ได้จะขึ้น
  `school_code_issue = 'missing'` ให้ไปแก้ที่หน้า "รายการที่ต้องตรวจสอบ" เหมือนฟอร์มอื่นทุกประการ
  * **ผลพลอยได้**: พอมี `school_code` แล้ว `Reporting::schoolMasterOverrides()` (ใช้อยู่ใน
    `pivot()`/`review.php` อยู่แล้ว) จะเติม `agency_name`/`amphoe`/`tambon`/`department` ให้เองจาก
    ทำเนียบโรงเรียนอัตโนมัติทันที ไม่ต้องเขียนโค้ดเพิ่มตรงไหนอีกเลย — จุดนี้คือเหตุผลหลักที่คุ้มค่าที่จะ
    implement เป็นการ "หารหัส" แทนที่จะปล่อยให้กรอกชื่อหน่วยงานลอย ๆ แบบเดิม
* **แก้ `Importer::deleteExistingSubmission()` เพิ่ม fallback ที่ 4** — `elseif ($schoolName)` ใช้
  `school_name` เดี่ยว ๆ เป็นคีย์กันข้อมูลซ้ำ (เมื่อ `school_code`/`agency_name`/`tambon` ว่างหมด) —
  จำเป็นเพราะถ้าจับคู่ชื่อไม่สำเร็จ (`school_code` ยังว่างหลัง resolve) แถวนั้นจะไม่มีคีย์กันซ้ำเหลือเลย
  ถ้าไม่มี fallback นี้ reupload ไฟล์เดิมซ้ำจะสะสมแถวที่จับคู่ไม่ได้ไปเรื่อย ๆ — เรียงลำดับ fallback
  ตอนนี้เป็น `school_code` → `agency_name` → `tambon`+`amphoe` → `school_name` (ตัวไหนมีก็ใช้ตัวนั้น
  เป็นคีย์ ทีละขั้น ไม่กระทบฟอร์มเดิมเลยเพราะทุกฟอร์มเดิมมี school_code หรือ agency_name อยู่แล้วเข้า
  2 branch แรกเสมอ)

### รวมข้อมูลฟอร์ม 16.2 เข้า "จำนวนผู้เรียน" + คอลัมน์ "พมจ." ในตารางผู้เรียนรายชั้น (เพิ่มเมื่อ 2026-08-30)
ผู้ใช้ขอเอาข้อมูลเด็กจากชีท พมจ-16.2 มารวมในยอด "จำนวนผู้เรียน" ทั้งจังหวัดด้วย พร้อมแยกเป็นคอลัมน์
"พมจ." ต่างหาก — ใช้เฉพาะคอลัมน์ **"จำนวนเด็ก..."** (2 ช่วงอายุ × 2 เพศ = 4 คอลัมน์) **ไม่รวมคอลัมน์
"จำนวนผู้ดูแลเด็ก..."** ในชีทเดียวกัน (นั่นเป็นข้อมูลครู/ผู้ดูแล ไม่ใช่ผู้เรียน — เหตุผลเดียวกับที่ฟอร์ม
14 ต้องระบุ `onlyColumns` กันไม่ให้จำนวนครูปนกับจำนวนเด็ก)
* **`public_report_data.php`**: เพิ่ม `['16_pmj', 'พมจ-16.2', [4 คอลัมน์ "จำนวนเด็ก..."]]` เข้าทั้ง
  `$metrics['students']['sheets']` (ยอดรวมตารางสรุป/การ์ดหน้าภาพรวม) และ `$genderSheets` (กราฟสัดส่วน
  นักเรียนชาย:หญิง) เพื่อให้ยอดรวมตรงกันเหมือนแหล่งอื่นทุกแหล่ง
* **`public_school_grade_table_data.php`**: เพิ่ม section 4) ใหม่ (ต่อจากฟอร์ม 4/11/15) join ด้วย
  `pivot('16_pmj', 'พมจ-16.2', $selectedYear)` แล้วจับคู่ school_code เหมือนฟอร์ม 11/15 — เพิ่มฟิลด์
  `pmj_total` (`['male'=>..,'female'=>..]`) เข้า `grade_table_blank_row()`, `grand_total`, และ
  `$gradeTotals`
  * **แถวที่จับคู่ school_code จากชื่อไม่สำเร็จ (ดูหัวข้อก่อนหน้า) จะไม่ปรากฏในตารางนี้เลย** เพราะ
    loop `if ($code === '') continue;` ข้ามแถวไม่มี school_code เหมือนทุกแหล่งข้อมูลอื่นในตารางนี้ —
    ผู้ใช้ต้องไปแก้ที่หน้า "รายการที่ต้องตรวจสอบ" ให้จับคู่สำเร็จก่อนถึงจะเห็นในตารางผู้เรียนรายชั้น
* **หน้าเว็บ/CSV**: เพิ่มกลุ่มคอลัมน์ "พมจ." (colspan=2 ชาย/หญิง) ต่อท้ายหลัง "ผู้เรียนนอกระบบ" แบบ
  เดียวกับกลุ่มอื่น ๆ ในหัวตาราง 2 แถว — ตอนนี้ตารางผู้เรียนรายชั้นมี 4 แหล่งข้อมูลนอกฟอร์ม 4 แล้ว
  (สกร./นอกระบบ/พมจ. — ครบทุกแหล่งที่ระบบรู้จักผู้เรียนนอกฟอร์ม 4 ในตอนนี้)
* **ไม่ได้เพิ่มเข้าตารางครูผู้สอน** เพราะคอลัมน์ "จำนวนผู้ดูแลเด็ก..." ของชีทนี้ยังไม่ได้ถูกขอให้รวม —
  ถ้าต้องการทีหลังทำแพทเทิร์นเดียวกันได้เลย (คอลัมน์มีอยู่แล้ว แค่ยังไม่ได้เอามาใช้)
  * **[อัปเดต 2026-08-30]** ผู้ใช้ขอเพิ่มจริงในคำขอถัดมาทันที — ดูหัวข้อถัดไป

### รวมข้อมูล "ผู้ดูแลเด็ก" ฟอร์ม 16.2 เข้าจำนวนครูผู้สอน + คอลัมน์ "พมจ." ในตารางครูผู้สอน (เพิ่มเมื่อ 2026-08-30)
ทำแบบเดียวกับที่เพิ่งทำฝั่งผู้เรียนไปทุกประการ แต่ใช้คอลัมน์ **"จำนวนผู้ดูแลเด็ก..."** แทน "จำนวน
เด็ก..." (2 ช่วงอายุ × 2 เพศ = 4 คอลัมน์ — สังเกตคอลัมน์ "3-5 ปี" มีช่องว่าง 2 ตัวก่อน "3-5" ในชื่อจริง
ของแม่แบบ ต้องคัดลอกเป๊ะ ๆ ไม่งั้น column_path ไม่ตรงกันเงียบ ๆ):
* **`public_report_data.php`**: เพิ่ม `['16_pmj', 'พมจ-16.2', [4 คอลัมน์ "จำนวนผู้ดูแลเด็ก..."]]` เข้า
  ทั้ง `$metrics['teachers']['sheets']` และ `$teacherGenderSheets` (แพทเทิร์นเดียวกับที่ทำกับ
  "จำนวนเด็ก..." ในหัวข้อก่อนหน้า)
* **`public_teacher_grade_table_data.php`**: เพิ่ม section 4) ใหม่ join ด้วย
  `pivot('16_pmj', 'พมจ-16.2', $selectedYear)` เพิ่มฟิลด์ `pmj_total`
  (`['male'=>..,'female'=>..]`) เข้า `teacher_table_blank_row()`, `grand_total`, และ `$gradeTotals` —
  แถวที่จับคู่ school_code จากชื่อไม่สำเร็จจะไม่ปรากฏในตารางนี้เช่นกัน (เหตุผลเดียวกับฝั่งผู้เรียน)
* **หน้าเว็บ/CSV**: เพิ่มคอลัมน์ "พมจ. (ชาย)"/"(หญิง)" ต่อท้ายหลัง "ครูนอกระบบ" — ตารางครูผู้สอนไม่มี
  หัวตาราง 2 แถวแบบตารางผู้เรียนรายชั้น (ถูกตัดออกไปตอนเปลี่ยนคอลัมน์หลักจากแยกระดับชั้นเป็นยอดรวม
  เดียว) จึงเพิ่มแค่ `<th>`/`<td>` ธรรมดา 2 คอลัมน์ต่อท้าย ไม่ต้องทำ colspan/rowspan
* ตอนนี้ทั้งตารางผู้เรียนรายชั้นและตารางครูผู้สอนมีข้อมูลจากฟอร์ม 16.2 ครบทั้ง 2 ฝั่ง (เด็ก→ตาราง
  ผู้เรียน, ผู้ดูแลเด็ก→ตารางครูผู้สอน) สอดคล้องกับยอดรวมทั้งจังหวัดที่ `public_report_data.php`

### กราฟ "จำนวนนักเรียนแยกตามรูปแบบการศึกษา" + คอลัมน์ใหม่ในทำเนียบโรงเรียน (เพิ่มเมื่อ 2026-08-30)
ผู้ใช้ขอกราฟแยกจำนวนผู้เรียนตาม "รูปแบบการศึกษา" อ้างอิงทำเนียบโรงเรียน — **ตรวจแล้วว่า `schools_master`
เดิมไม่มีคอลัมน์นี้เลย และไฟล์ทำเนียบต้นฉบับที่เคยใช้ (`91_SchoolCode-Matching-V1.xlsx`) ก็ไม่มี** ถาม
ผู้ใช้แล้วได้ไฟล์ทำเนียบฉบับใหม่ `SchoolDetail_2569.xlsx` มาตรวจ พบคอลัมน์ "รูปแบบการศึกษา" (ค่าเช่น
"ในระบบ") อยู่ในชีท **"2569_School"** (ชีทที่ 3 ของไฟล์ ไม่ใช่ชีทแรก!) จึงเป็นงานที่ใหญ่กว่าแค่เพิ่ม
กราฟ — ต้องเพิ่มคอลัมน์ใหม่ในทำเนียบโรงเรียนทั้งระบบก่อน:
* **`migrations/005_school_education_form.sql`** — เพิ่มคอลัมน์ `education_form VARCHAR(120) NULL`
  ใน `schools_master` (รันผ่าน phpMyAdmin เหมือน migration อื่น ๆ — **ยังไม่ได้รันบน production
  จนกว่าแอดมินจะรันเอง**)
* **`schools_master.php`** — เพิ่ม `'education_form' => 'รูปแบบการศึกษา'` เข้า `$columnMap`, เก็บค่านี้
  ตอนอ่านแถวข้อมูล, เพิ่มเข้า `INSERT INTO schools_master` เป็น optional (ไฟล์ทำเนียบเก่าที่ไม่มี
  คอลัมน์นี้ยังอัปโหลดได้ปกติ ค่านี้จะเป็น `null`)
  * **แก้บั๊กที่เจอระหว่างตรวจไฟล์ใหม่**: โค้ดเดิม `readGrid($sheetNames[0])` ยึดว่าชีทแรกในไฟล์คือ
    ชีทข้อมูลเสมอ — **ไฟล์ `SchoolDetail_2569.xlsx` พิสูจน์ว่าไม่จริงเสมอไป** (ชีทแรกเป็น
    Pivot Table สรุป ชีทข้อมูลจริงอยู่ชีทที่ 3) ถ้าไม่แก้ อัปโหลดไฟล์นี้จะพังทันที (อ่านชีทผิด หา
    คอลัมน์ SchoolCode/SchoolName ไม่เจอ) — แก้เป็น **สแกนทุกชีทหาชีทที่แถวหัวตาราง (แถวที่ 1) มี
    ทั้ง "SchoolCode" และ "SchoolName" จริง ๆ** แทนการยึดตำแหน่งชีทตายตัว (loop เรียก `readGrid()`
    ทีละชีทจนกว่าจะเจอ — ไฟล์ทำเนียบมีไม่กี่พันแถว อัปโหลด 1 ครั้งไม่บ่อย ไม่ห่วงเรื่อง perf)
* **`src/Reporting.php`**:
  * `schoolMasterOverrides()` — เพิ่ม `education_form` เข้า SELECT **แต่ห่อ query นี้ด้วย try/catch
    ชั้นในแยกต่างหาก** (ไม่ใช่แค่ catch ใหญ่อันเดิมที่คลุมทั้งฟังก์ชัน) ถ้าคอลัมน์ยังไม่มี (migration
    005 ยังไม่รัน) จะ fallback ไป query เดิมแบบไม่มี `education_form` แทนที่จะปล่อยให้ query พังแล้ว
    ทำให้ override อื่น ๆ ที่ใช้งานได้อยู่แล้ว (amphoe/tambon/area_name/department) พังไปด้วยทั้งที่
    ตารางมีอยู่จริง — เจตนาป้องกัน regression ช่วงที่ deploy โค้ดไปแล้วแต่แอดมินยังไม่ได้รัน migration
    005 บน production (เป็นขั้นตอนคนละขั้นเสมอในระบบนี้ ต้องรันมือผ่าน phpMyAdmin)
  * `pivot()` — เพิ่มฟิลด์ `education_form` เข้าทุกแถวผลลัพธ์ (`$master['education_form'] ?? ''`)
    แพทเทิร์นเดียวกับ `department` ทุกประการ ทำให้ `compute_metric_totals(..., 'education_form', ...)`
    ใน `public_report_data.php` ใช้งานได้ทันทีโดยไม่ต้องแก้ฟังก์ชันกลุ่มอื่นเลย (`groupedTotal()`/
    `groupedTotalForColumns()` group ตาม `$row[$dimension]` อยู่แล้วแบบ generic)
  * **ตั้งใจไม่แก้ `tidyRows()`** (ใช้กับ `export_tidy.php`) แม้จะมี `$s['department']` แพทเทิร์นคล้าย
    กันอยู่ — เพิ่มแค่ตรงนั้นแล้วไม่เอาไปต่อใน `$row[]` ของฟังก์ชันเดียวกัน (ต้องแก้ทั้ง 2 จุด + เพิ่ม
    label ใน `export_tidy.php`) จะเป็น dead code เปล่า ๆ ไม่ได้ใช้จริง — ผู้ใช้ขอแค่กราฟ ยังไม่ได้ขอ
    ให้โผล่ในตาราง/export อื่น ถ้าต้องการทีหลังค่อยเพิ่ม
* **`public_report_data.php`/`public_report.php`** — เพิ่ม `$studentsByEducationForm` (ใช้
  `compute_metric_totals()` แบบเดียวกับ `$studentsByDept`/`$studentsByAmphoe` ตรึงมิติไว้ตายตัว ไม่ขึ้น
  กับ dropdown) เพิ่มเป็นกราฟแท่งที่ 3 ในกลุ่ม `$barCharts` ต่อจาก "แยกตามอำเภอ" — โรงเรียนที่ไม่มีค่า
  รูปแบบการศึกษาในทำเนียบ (หรือทำเนียบยังไม่มีคอลัมน์นี้เลย) จะรวมอยู่ใต้ "ไม่ระบุ" เหมือนมิติอื่นทุกมิติ
  * **ไม่ได้เพิ่มเข้า dropdown "แยกตามมิติ"** ของตารางสรุปยอดรวม (`$dimensions` มีแค่ สังกัด/หน่วยงาน,
    ต้นสังกัด, อำเภอ) เพราะผู้ใช้ขอแค่กราฟ ไม่ได้ขอเป็นมิติเลือกได้ในตาราง — เหมือน "อำเภอ"/"ต้นสังกัด"
    ที่มีกราฟตรึงมิติของตัวเองแยกจาก dropdown อยู่แล้วเป็นแบบอย่าง

### บั๊ก: ตารางผู้เรียนรายชั้นขาดฟอร์ม 14 ไปเลย ทำให้ยอดรวมไม่ตรงกับหน้าภาพรวม/ตารางสรุป (แก้เมื่อ 2026-08-30)
ผู้ใช้ส่งภาพหน้าจอมาถามว่าทำไม "จำนวนผู้เรียนทั้งหมด" (การ์ดหน้าภาพรวม, 119,848) ไม่ตรงกับตารางผู้เรียน
รายชั้น — **ไล่เทียบทีละแหล่งข้อมูลระหว่าง `public_report_data.php`'s `$metrics['students']['sheets']`
กับ `public_school_grade_table_data.php` แล้วพบว่าตารางผู้เรียนรายชั้น "ไม่เคยรวมข้อมูลฟอร์ม 14
(เด็กเล็ก ศพด./สถานพัฒนาเด็กปฐมวัย) เลยตั้งแต่สร้างตารางนี้มา"** — ทั้งที่ `$metrics['students']`
รวมฟอร์ม 14 อยู่แล้ว (คอลัมน์ "รวมทั้งสิ้น") มาตั้งแต่ session ก่อนหน้านี้มาก ๆ (จุดที่พลาด: ตอนสร้าง
ตารางผู้เรียนรายชั้นครั้งแรก คัดลอกแนวคิดมาจาก `$metrics['students']` แค่บางส่วน — เอาแค่ 11/15 ที่
"เห็นชัดว่าไม่มีในฟอร์ม 4" แต่ลืมฟอร์ม 14 ไปเฉย ๆ ไม่มีเหตุผลเชิงเทคนิคที่ตั้งใจข้าม)
* **สังเกตวิธีตรวจแบบนี้ไว้ใช้ครั้งหน้า**: เทียบ sheet list ทีละบรรทัดระหว่าง `$metrics[...]['sheets']`
  ใน `public_report_data.php` กับ pivot() calls ใน `public_*_grade_table_data.php` ที่เกี่ยวข้อง — ถ้า
  จำนวนไม่เท่ากันคือมีจุดหลุดแน่นอน (ฝั่งครูผู้สอนตรวจแล้วไม่มีปัญหานี้ เพราะสร้างตารางครูผู้สอนทีหลัง
  และคัดลอกครบทุกแหล่งจาก metric `teachers` ตั้งแต่แรก)
* **แก้**: เพิ่มคอลัมน์ใหม่ "เด็ก ศพด." ในตารางผู้เรียนรายชั้น (`childcare_total`, แยกชาย/หญิง) ใช้
  คอลัมน์ `เด็กเล็ก / อายุ 2-5 ปี / เพศ` (8 คอลัมน์ ไม่ใช่ "รวมทั้งสิ้น" ตรง ๆ เพราะ "รวมทั้งสิ้น" ไม่
  แยกเพศ — ผลรวม 8 คอลัมน์นี้ = "รวมทั้งสิ้น" อยู่แล้ว เป็นชุดเดียวกับที่ `$genderSheets` ใน
  `public_report_data.php` ใช้อยู่แล้วสำหรับกราฟสัดส่วนนักเรียนชาย:หญิง) ฟอร์ม 14 มี school_code ใน
  ตัวเอง (ใช้ `$stdIdentity`) ไม่ต้องพึ่งการจับคู่ชื่อแบบฟอร์ม 16.2
  * เพิ่มเป็น section 2) แทรกก่อนฟอร์ม 11 (เดิม 2,3,4 → เลื่อนเป็น 3,4,5) อัปเดต `grand_total` และ
    `$gradeTotals` ให้รวม `childcare_total` ด้วย เพิ่มกลุ่มคอลัมน์ "เด็ก ศพด." (colspan=2) ในหัวตาราง
    2 แถว ต่อจากกลุ่มระดับชั้น ก่อน "ผู้เรียน สกร." ทั้งหน้าเว็บและ CSV export
* **ตรวจแล้ว: ตารางครูผู้สอนไม่มีปัญหานี้** (`public_teacher_grade_table_data.php` มีฟอร์ม 14 อยู่แล้ว
  เป็น section 2 ตั้งแต่สร้างตาราง — "ครู ศพด." ครบถ้วนมาตลอด) มีแค่ฝั่งผู้เรียนที่ขาด
* **บทเรียนสำคัญ**: เวลาเพิ่มแหล่งข้อมูลใหม่ (metric sheet) เข้า `public_report_data.php` ต้องเช็ค
  เสมอว่ามี "ตารางรายสถานศึกษา" คู่ขนาน (`public_school_grade_table*.php` /
  `public_teacher_grade_table*.php`) ที่ต้องอัปเดตตามด้วยหรือไม่ ไม่งั้นยอดรวม 2 หน้าจะไม่ตรงกันแบบ
  เงียบ ๆ (ไม่มี error ให้เห็น แค่ตัวเลขไม่ตรงเฉย ๆ) — ผู้ใช้เป็นคนสังเกตเจอเองจากภาพหน้าจอ ไม่ใช่ตรวจ
  พบระหว่างพัฒนา
* **[ยืนยันเพิ่มเติม 2026-08-30]** ผู้ใช้ถามต่อว่าตารางครูผู้สอนรวมผู้ดูแลเด็กฟอร์ม 16.2 หรือยัง —
  ตรวจโค้ดแล้ว **มีอยู่แล้วครบทุกจุด** (เพิ่มไปตั้งแต่ commit `e3456d8` ก่อนหน้านี้ในวันเดียวกัน):
  `pmj_total` ใน `public_teacher_grade_table_data.php` section 4), รวมใน `grand_total`/`$gradeTotals`,
  คอลัมน์ "พมจ. (ชาย)"/"(หญิง)" ในหน้าเว็บและ CSV export, และอยู่ใน `$metrics['teachers']['sheets']`
  ของ `public_report_data.php` ด้วย — ไม่มีจุดไหนขาดเหมือนที่เจอฝั่งฟอร์ม 14 ข้างบน ไม่ต้องแก้อะไรเพิ่ม
  (บันทึกไว้กันถามซ้ำ/ตรวจซ้ำในอนาคต)

### แก้ต่อ: ยอดฟอร์ม 14 ยังไม่ตรง เพราะใช้ "ผลรวมส่วนย่อย" แทน "คอลัมน์รวมจริง" (แก้เมื่อ 2026-08-30)
หลังเพิ่มฟอร์ม 14 เข้าตารางผู้เรียนรายชั้นไปแล้ว (หัวข้อก่อนหน้า) ผู้ใช้แจ้งว่ายอดรวมยังไม่ตรงกับหน้า
ภาพรวม/ตารางสรุปอยู่ดี ต่างกัน 65 คน — **สาเหตุที่แท้จริง**: ตอนแก้รอบก่อนเลือกใช้ผลรวมของ 8 คอลัมน์
"เด็กเล็ก / อายุ 2-5 ปี / เพศ" แทนคอลัมน์ "รวมทั้งสิ้น" ตรง ๆ (เพราะอยากได้ตัวเลขแยกชาย/หญิงให้เข้าชุด
กับคอลัมน์อื่นในตาราง) โดยสมมติว่า 8 คอลัมน์นี้บวกกันแล้วต้องเท่ากับ "รวมทั้งสิ้น" เป๊ะเสมอ — **สมมติฐาน
นี้ผิดกับข้อมูลจริง** บางแถวที่หน่วยงานกรอกมา "รวมทั้งสิ้น" กับผลรวมของ 4 ช่วงอายุไม่เท่ากัน (กรอกไม่
ครบ/พิมพ์ผิดเป็นบางแถว เป็นเรื่องปกติของข้อมูลกรอกเอง) ในขณะที่ `$metrics['students']` ของ
`public_report_data.php` ใช้คอลัมน์ "รวมทั้งสิ้น" ตรง ๆ มาตั้งแต่ต้น (ก่อนตารางผู้เรียนรายชั้นจะมีอยู่
ด้วยซ้ำ) — **บทเรียน**: การ "ใช้คอลัมน์คนละตัวกัน แต่คาดหวังว่าผลรวมจะเท่ากันเพราะควรจะเป็น subtotal
ของกันและกัน" เป็นสมมติฐานที่พังได้ง่ายกับข้อมูลกรอกเองจากหลายหน่วยงาน ต้อง**ใช้คอลัมน์ต้นทางเดียวกัน
เป๊ะทั้ง 2 จุดเสมอ** ถ้าอยากให้ยอดรวมตรงกันแบบรับประกันได้จริง (ไม่ใช่แค่ "ควรจะตรง")
* **แก้**: เปลี่ยน `$childcareChildColumns` ใน `public_school_grade_table_data.php` จาก 8 คอลัมน์อายุ
  เป็น `['รวมทั้งสิ้น']` คอลัมน์เดียว — คอลัมน์เดียวกับที่ `$metrics['students']` ใช้เป๊ะ ๆ รับประกันยอด
  รวมตรงกันโดยโครงสร้าง ไม่ต้องพึ่งสมมติฐานเรื่องข้อมูลถูกต้องอีก
* **ผลข้างเคียง**: คอลัมน์ "เด็ก ศพด." ในตารางนี้**ไม่มีแยกชาย/หญิงอีกต่อไป** (กลับไปเป็นยอดรวมเดียว
  เหมือนก่อนแก้ครั้งแรก) เพราะ "รวมทั้งสิ้น" ไม่มีข้อมูลเพศให้ใช้ — เปลี่ยนหัวตารางจาก `colspan="2"`
  (กลุ่มชาย/หญิง) เป็น `rowspan="2"` (คอลัมน์เดี่ยวเหมือน "รวม") ทั้งหน้าเว็บและ CSV export — `$row`/
  `$gradeTotals['childcare_total']` เปลี่ยนชนิดจาก `['male'=>..,'female'=>..]` กลับเป็น float เดี่ยว
* **ไม่ได้แก้ตารางครูผู้สอน** เพราะ "ครู ศพด." ของตารางนั้นใช้คอลัมน์ `จำนวนครู/ผู้ดูแลเด็ก (คน) /
  ชาย,หญิง` (2 คอลัมน์ตรง ไม่ใช่ "รวมทั้งสิ้น" ที่ต้องคำนวณผลรวมส่วนย่อยแบบนี้) ตรงกับที่
  `$metrics['teachers']` ใช้อยู่แล้วเป๊ะ ๆ ไม่มีปัญหาแบบเดียวกันเกิดขึ้น

### กลับคำ: ห้ามใช้คอลัมน์ "รวม" เลยทั้งระบบ ต้องใช้ข้อมูลแยกชาย/หญิงเสมอ (แก้เมื่อ 2026-08-30 คำสั่งถัดมาทันที)
ผู้ใช้สั่งกลับหัวข้อก่อนหน้านี้ทันที — **ไม่ยอมรับการใช้คอลัมน์ "รวม"/"รวมทั้งสิ้น" ของฟอร์มไหนเป็น
แหล่งข้อมูลเด็ดขาด** เหตุผล: ตัวเลข "รวม" เป็นตัวเลขที่หน่วยงานพิมพ์บวกเลขเองมาในไฟล์ (ไม่ใช่ระบบคำนวณ
ให้) เสี่ยงบวกผิดได้ ต้องการใช้ข้อมูลแยกชาย/หญิงที่ระบบเก็บเป็นคอลัมน์แยกจริงเสมอ แม้ว่าจะมีความเสี่ยง
เรื่องยอดรวมไม่ตรงกัน 100% ระหว่างหน้าต่าง ๆ ถ้าหน่วยงานกรอกช่องอายุกับช่องรวมไม่ตรงกันก็ตาม (ยอมรับ
ความเสี่ยงนี้แทนที่จะเชื่อคอลัมน์ "รวม" ที่อาจผิดจากการบวกเลขเอง)
* **กลับคืนทั้ง 2 ไฟล์**: `public_report_data.php` (`$metrics['students']['sheets']` ฟอร์ม 14 กลับไป
  ใช้ 8 คอลัมน์ `เด็กเล็ก / อายุ 2-5 ปี / เพศ` แทน `['รวมทั้งสิ้น']`) และ
  `public_school_grade_table_data.php` (`$childcareChildColumns` กลับไปเป็น 8 คอลัมน์เดียวกัน,
  `childcare_total` กลับเป็น `['male'=>..,'female'=>..]`, หัวตาราง "เด็ก ศพด." กลับเป็น `colspan="2"`
  ทั้งหน้าเว็บและ CSV) — ตอนนี้ `$genderSheets` ก็ใช้คอลัมน์เดียวกันนี้อยู่แล้วอยู่แล้ว (ไม่เคยเปลี่ยน)
  ทั้ง 3 จุดจึงใช้คอลัมน์ชุดเดียวกันเป๊ะอีกครั้ง
* **ตรวจสอบยืนยันว่า "ไม่มีจุดไหนในระบบทั้งหมดอ้างอิงคอลัมน์ 'รวม'/'รวมทั้งสิ้น' ของฟอร์มไหนเป็นแหล่ง
  ข้อมูลโดยตรงอีกต่อไปแล้ว** — `grep "'รวม" ทุกไฟล์ public_report_data.php`,
  `public_school_grade_table_data.php`, `public_teacher_grade_table_data.php` ไม่เจอการใช้เป็น
  onlyColumns เหลืออยู่เลย (มีแต่คอมเมนต์อธิบายเหตุผลที่เลิกใช้)
* **ตรวจสอบยืนยันความสอดคล้องกันของ 4 จุดหลัก (sheet list เทียบทีละบรรทัด)**:
  - ผู้เรียน: `$metrics['students']['sheets']` = `$genderSheets` = pivot() calls ใน
    `public_school_grade_table_data.php` — ครบทั้ง 8 รายการ (4/14/15×4/11/16) ตรงกันทุกตัว ทุก
    onlyColumns เหมือนกันเป๊ะ
  - ครูผู้สอน: `$metrics['teachers']['sheets']` = `$teacherGenderSheets` = pivot() calls ใน
    `public_teacher_grade_table_data.php` — ครบทั้ง 7 รายการ (10.1/14/15×4/16) ตรงกันทุกตัวเช่นกัน
  - ทั้งหมดนี้แปลว่า: การ์ดหน้าภาพรวม, ตารางสรุปยอดรวม, กราฟสัดส่วนชาย:หญิง, และตารางรายสถานศึกษา
    (ผู้เรียน/ครูผู้สอน) **คำนวณจากชุดคอลัมน์เดียวกันเป๊ะทั้งหมดแล้ว ณ ตอนนี้** ไม่มีจุดไหนหลุดอีก
    (ไม่ได้รันทดสอบจริงเพราะเครื่อง dev ไม่มี PHP — ตรวจแค่โค้ด/รายการคอลัมน์ตรงกัน)
* **ถ้าจะเพิ่ม/แก้แหล่งข้อมูลใหม่ในอนาคต**: ต้องแก้ 3-4 จุดพร้อมกันเสมอ (metric หลัก + gender chart +
  grade table ที่เกี่ยวข้อง) และ**ห้ามใช้คอลัมน์ "รวม"/"รวมทั้งสิ้น" เป็นแหล่งข้อมูลเด็ดขาดตามคำสั่ง
  ผู้ใช้งาน** ให้ใช้คอลัมน์แยกชาย/หญิงเสมอแม้จะต้องรวมหลายคอลัมน์เอง

### เพิ่มคอลัมน์ "รวมชาย"/"รวมหญิง" ในตารางผู้เรียนรายชั้น + ตารางครูผู้สอน (เพิ่มเมื่อ 2026-08-30)
ผู้ใช้ขอเพิ่มคอลัมน์ยอดรวมแยกเพศ (คนละคอลัมน์กับ "รวม" ที่เป็นยอดรวมทั้งชาย+หญิงอยู่แล้ว) ในทั้ง 2
ตารางรายสถานศึกษา พร้อมรวมอยู่ใน CSV export ด้วย — ทำได้ตรงไปตรงมาเพราะทุกแหล่งข้อมูลในทั้ง 2 ตาราง
แยกชาย/หญิงอยู่แล้วทั้งหมด (ไม่มีแหล่งไหนเป็นยอดรวมเดี่ยวอีกต่อไป หลังแก้หัวข้อ "ห้ามใช้คอลัมน์รวม"
ด้านบน) แค่บวกส่วนชายทั้งหมดเข้าด้วยกัน บวกส่วนหญิงทั้งหมดเข้าด้วยกัน แยกกัน:
* **`public_school_grade_table_data.php`**: เพิ่ม `$gtRow['grand_total_male']`/`grand_total_female`
  (ผลรวม `grades[label]['male'/'female']` ทุกระดับชั้น + `childcare_total`/`nfe_total`/
  `private_nonformal_total`/`pmj_total` แต่ละฝั่งเพศ) คำนวณคู่กับ `grand_total` เดิมในลูปเดียวกัน (ไม่
  ต้องวนซ้ำ) เพิ่ม `$gradeTotals['grand_total_male']`/`grand_total_female` คู่กัน
* **`public_teacher_grade_table_data.php`**: แพทเทิร์นเดียวกันเป๊ะ (`teaching_total` แทน `grades`)
* **หน้าเว็บทั้ง 2 หน้า**: เพิ่มคอลัมน์ "รวมชาย"/"รวมหญิง" (`rowspan="2"` ในตารางผู้เรียนรายชั้นที่มี
  หัวตาราง 2 แถว, คอลัมน์ธรรมดาในตารางครูผู้สอนที่หัวตารางแถวเดียว) วางไว้**ทันทีหลังคอลัมน์ "รวม"**
  ก่อนคอลัมน์ระดับชั้น/ครูผู้สอนชุดแรก ทั้งแถวรวมบนสุดและแถวข้อมูลแต่ละสถานศึกษา
* **CSV export ทั้ง 2 ไฟล์**: เพิ่มคอลัมน์ "รวมชาย"/"รวมหญิง" ต่อจาก "รวม" ในหัวตารางเช่นกัน

### อัปโหลดฟอร์ม 4 ค้าง/timeout (ERR_TIMED_OUT) — แก้ที่ `src/Importer.php` (แก้เมื่อ 2026-08-30)
ผู้ใช้แจ้งว่าอัปโหลดฟอร์ม 4 (`upload.php?form=4_students`) บน dev server แล้วเบราว์เซอร์ค้างจนขึ้น
"ERR_TIMED_OUT" ส่วนหน้าอื่น ๆ ของเว็บเข้าใช้งานได้ปกติ (ไม่ใช่เซิร์ฟเวอร์ล่มทั้งเครื่อง) — สืบจากโค้ด
ล้วน (เครื่อง dev ไม่มี PHP รันจริง ไม่มีสิทธิ์เข้า log/shell ของเซิร์ฟเวอร์) พบสาเหตุที่เป็นไปได้มาก
ที่สุดใน `Importer::importSheet()`:

* `src/Db.php` ตั้ง `PDO::ATTR_EMULATE_PREPARES => false` (ปิด emulation) แปลว่าทุกครั้งที่เรียก
  `prepare()` คือ round-trip จริงไปเซิร์ฟเวอร์ DB (ไม่ใช่แค่ฝั่ง client อย่างที่ emulate ทำ)
* เดิมโค้ด `prepare()` statement ของตาราง `submissions` ใหม่ทุกรอบวนลูป (แถวเดิม ๆ) — สิ้นเปลืองแต่
  ไม่ใช่ตัวหลัก ย้ายมา `prepare()` ครั้งเดียวก่อนลูป ใช้ซ้ำผ่าน `$insSubmission->execute()`
* ตัวหลักจริง ๆ: `submission_values` (ค่าของแต่ละคอลัมน์ในแถว) เดิม `prepare()` ครั้งเดียวต่อแถว แต่
  `execute()` **1 ครั้งต่อ 1 เซลล์ที่ไม่ว่าง** — ฟอร์ม 4 มี 74 คอลัมน์ข้อมูล (37 ระดับชั้น × 2 เพศ) ถ้ามี
  ~500-600 สถานศึกษา รวมแล้วอาจถึง ~44,000 round-trip ใน 1 ครั้งอัปโหลด ซึ่งน่าจะเกิน timeout ของ
  web request ได้ง่าย ในขณะที่ฟอร์มอื่นไม่มีจำนวนคอลัมน์เยอะขนาดนี้ (จึงไม่เจอปัญหาเดียวกัน) — ตรง
  กับที่ผู้ใช้ยืนยันว่าเฉพาะฟอร์ม 4 เท่านั้นที่ค้าง
* **วิธีแก้**: รวบรวม tuple `[col_index, column_path, value, needs_review]` ของทุกเซลล์ในแถวเดียวกัน
  ไว้ในอาร์เรย์ `$cellTuples` ก่อน (ตรรกะเดิมทุกอย่างยังอยู่ครบ — merge คอลัมน์ "อื่นๆ", ข้าม hidden
  column, `classifyValue()`, นับ `needsReviewCount`) แล้วค่อย `prepare()`+`execute()` เป็น
  **1 คำสั่ง INSERT หลายแถวต่อ 1 แถวข้อมูล** (`VALUES (?,?,?,?,?), (?,?,?,?,?), ...` สร้างด้วย
  `implode()`/`array_fill()`) แทน — ลด round-trip ของ `submission_values` จากระดับหมื่นเหลือระดับ
  ร้อย (เท่ากับจำนวนแถว/สถานศึกษา) ถ้าแถวไหนไม่มีเซลล์ที่ต้องบันทึกเลยก็ข้าม INSERT ไปเลย (ตรงกับ
  พฤติกรรมเดิมที่ข้ามเซลล์ว่าง)
* ทดสอบไม่ได้จริงบนเครื่อง dev (ไม่มี PHP) ตรวจแค่ความสมดุลวงเล็บ/quote ด้วยสคริปต์ Python เท่านั้น

**⚠️ อัปเดต — สาเหตุจริงไม่ใช่โค้ด/เซิร์ฟเวอร์เลย**: ผู้ใช้แจ้งกลับว่าไฟล์ .xlsx ที่เลือกอัปโหลดอยู่บน
OneDrive แบบ "cloud-only" (ยังไม่ได้ดาวน์โหลดมาเก็บบนเครื่องจริง) พอเลือกไฟล์นี้ผ่าน `<input
type=file>` ตัว OS ต้องดาวน์โหลดไฟล์จาก OneDrive มาก่อนถึงจะแนบไฟล์เข้า request ได้ ทำให้เบราว์เซอร์
"ค้าง" นานจนดูเหมือนเซิร์ฟเวอร์ timeout ทั้งที่ request ยังไม่ถูกส่งออกไปด้วยซ้ำ — พอเปิดไฟล์นั้นให้มัน
sync ลงเครื่องก่อน (เช่นดับเบิลคลิกเปิดใน Excel ครั้งนึง) แล้วอัปโหลดใหม่ก็ผ่านปกติ **ไม่เกี่ยวกับ
`ATTR_EMULATE_PREPARES`/จำนวน round-trip ของ `submission_values` เลย** การแก้โค้ดด้านบน (รวม INSERT
เป็นชุดเดียวต่อแถว) ยังคงเป็นการปรับปรุงประสิทธิภาพที่ถูกต้องและคุ้มค่าที่จะเก็บไว้ (ลด round-trip จริง
สำหรับฟอร์มคอลัมน์เยอะ) แต่ **ไม่ใช่สาเหตุของอาการ ERR_TIMED_OUT ที่ผู้ใช้เจอในครั้งนี้** — ถ้าเจอ
อาการอัปโหลดค้างอีกในอนาคต ให้ถามก่อนว่าไฟล์ต้นฉบับอยู่บน cloud storage (OneDrive/Google
Drive/Dropbox) แบบยังไม่ sync ลงเครื่องหรือเปล่า ก่อนจะไปสงสัยโค้ด/เซิร์ฟเวอร์

### ตัดแถวฟอร์ม 14 ที่ไม่ใช่ ศพด. จริงออก กันนับผู้เรียนซ้ำกับตารางที่ 4 (แก้เมื่อ 2026-08-30)
ผู้ใช้แจ้งว่าข้อมูลผู้เรียน/เด็กเล็กในฟอร์ม 14 ("14.ข้อมูลศูนย์พัฒนาเด็กเล็ก") บางแถวเป็นของสถานศึกษาที่
"ต้นสังกัด"/ประเภทจริง ๆ ไม่ใช่ศูนย์พัฒนาเด็กเล็ก (สังกัดอื่น เช่นสถานศึกษาที่กรอกฟอร์ม 4 อยู่แล้ว แต่
หน่วยงานกรอกฟอร์ม 14 ซ้ำมาด้วยโดยเข้าใจผิด) ถ้ารวมแถวเหล่านั้นเข้าไปในยอด "จำนวนผู้เรียน" ด้วยจะซ้ำกับ
ตารางที่ 4 — สั่งให้ตัดแถวที่ไม่ใช่ ศพด. ออก ทั้งตารางสรุป/ตารางผู้เรียนรายชั้น/การ์ดข้อมูลใหม่ทั้งหมด

* **วิธีระบุว่าแถวไหนคือ ศพด. จริง**: ถามผู้ใช้งานตรง ๆ (เพราะไม่มี DB/PHP บนเครื่อง dev ให้ตรวจข้อมูล
  จริงได้) — คำตอบคือ **ไม่ใช่คอลัมน์ "ต้นสังกัด" (department)** แต่เป็นคอลัมน์ **"รูปแบบการศึกษา"**
  (`education_form`, มาจากทำเนียบโรงเรียน schools_master เท่านั้น ไม่มีคอลัมน์นี้ในไฟล์สำรวจฟอร์มไหน
  เลย — migration 005) ที่มีค่าตรงกับข้อความ **"ศูนย์พัฒนาเด็ก"** เป๊ะ (ไม่ใช่ substring/LIKE)
* **`src/Reporting.php`**: เพิ่มพารามิเตอร์ optional ตัวสุดท้าย `?callable $rowFilter = null` ให้ทั้ง
  `groupedTotalForColumns()` และ `genderTotalsForColumns()` — ถ้าส่งมา จะข้ามแถวที่ `$rowFilter($row)`
  คืนค่า false ก่อนรวมยอด (ทำเป็น callable ทั่วไป ไม่ hardcode เงื่อนไข ศพด. ไว้ใน Reporting.php เอง
  เพราะเป็นเงื่อนไขเฉพาะฟอร์ม 14 เท่านั้น ฟอร์มอื่นไม่ต้องใช้)
* **`public_report_data.php`**: ประกาศ `$onlyChildcareCenterRows` (closure เช็ค
  `trim($row['education_form'] ?? '') === 'ศูนย์พัฒนาเด็ก'`) ก่อน `$metrics` แล้วส่งเป็น element ที่ 4
  ของ tuple ฟอร์ม 14 ทั้งใน `$metrics['students']['sheets']` และ `$genderSheets` — `compute_metric_totals()`
  อ่าน `$sheet[3] ?? null` แล้วส่งต่อให้ `groupedTotalForColumns()`/`genderTotalsForColumns()` จุดเดียว
  จึงกระทบทุกอย่างที่คำนวณจาก `$metrics['students']` อัตโนมัติ (ตารางสรุป, การ์ดภาพรวม, กราฟ
  ต้นสังกัด/อำเภอ/รูปแบบการศึกษา, อัตราส่วนนักเรียนต่อครู, แนวโน้มรายปี, สัดส่วนชาย:หญิง) — จุดเดียวพอ
  ไม่ต้องแก้หลายที่ ตามแพทเทิร์นเดิมของไฟล์นี้
* **`public_school_grade_table_data.php`**: ไม่ได้เรียกผ่าน Reporting เมธอดข้างบน (วน `pivot14['rows']`
  เอง) จึงเพิ่ม `if (trim($r['education_form'] ?? '') !== 'ศูนย์พัฒนาเด็ก') continue;` ก่อนเข้าลูปรวม
  `childcare_total` ตรง ๆ (เงื่อนไขเดียวกัน คัดลอกมาเพราะไฟล์นี้กับ public_report_data.php ไม่ include
  ซ้อนกัน — แพทเทิร์นเดียวกับ `$privateNonformalSheets` ที่คัดลอกไว้แล้ว)
* **อัปเดต (แก้ต่อทันที 2026-08-30)**: ผู้ใช้ยืนยันว่าฝั่งครูผู้สอนก็เจอปัญหานับซ้ำแบบเดียวกันจริง —
  แก้ตามแพทเทิร์นเดิมทุกจุด:
  * `public_report_data.php`: เพิ่ม `$onlyChildcareCenterRows` เป็น element ที่ 4 ของ tuple ฟอร์ม 14
    ทั้งใน `$metrics['teachers']['sheets']` และ `$teacherGenderSheets` (ก่อนหน้านี้ตัวเดียวกันใช้กับ
    ฝั่งผู้เรียนแล้ว — ใช้ closure เดียวกันซ้ำ ไม่ต้องประกาศใหม่)
  * `public_teacher_grade_table_data.php`: เพิ่ม
    `if (trim($r['education_form'] ?? '') !== 'ศูนย์พัฒนาเด็ก') continue;` ก่อนรวม `childcare_total`
    ในลูป `pivot14['rows']` (เหมือน `public_school_grade_table_data.php` ฝั่งผู้เรียนเป๊ะ)
* ทดสอบไม่ได้จริงบนเครื่อง dev เหมือนเดิม ตรวจแค่ syntax balance เท่านั้น

### รวมฟอร์ม 11.3 เข้ากราฟ "นักเรียนพิการแยกตามประเภทความพิการ" (เพิ่มเมื่อ 2026-09-01)
ผู้ใช้ถามว่าฟอร์ม 11.3 (ผู้เรียนพิการ กศน. แยกตามประเภทความพิการ) รวมกับฟอร์ม 8.2 (นักเรียนพิการแยก
ตามประเภทความพิการ) หรือยัง — ตรวจโค้ดแล้วพบว่า **11.3 ไม่ถูกใช้ที่ไหนในระบบเลยนอกจาก registry.php**
(มีแค่ 11.1 กับ 11.2 ที่ถูกใช้ในหน้าสถิติสาธารณะ) เป็นแหล่งข้อมูลที่ตกหล่นแบบเดียวกับที่เคยเกิดกับ
ฟอร์ม 14 มาก่อน — ผู้ใช้ยืนยันให้รวมเข้ากราฟ `$disabilityByType` เท่านั้น **ไม่เพิ่มเข้า
`$metrics['disability']` (ยอดรวม/การ์ดหน้าภาพรวม/ตารางสรุป)** เพราะจะนับซ้ำกับฟอร์ม 11.2 ที่ metric
นั้นใช้อยู่แล้ว (คนพิการกลุ่มเดียวกัน แค่แยกคนละมิติ — 11.2 แยกตามกิจกรรมการศึกษา ใช้เป็นยอดรวมอยู่แล้ว,
11.3 แยกตามประเภทความพิการ เอาไว้ทำกราฟเท่านั้น เหมือนคู่ 8.1/8.2 ของฟอร์ม 8 ที่ระบบมีอยู่แล้วเป็น
ต้นแบบ)
* **ตรวจโครงสร้างคอลัมน์จริงด้วย script (`unzip`+node อ่าน sharedStrings.xml/worksheet XML ตรง ๆ —
  เครื่อง Windows นี้ไม่มี PHP/python แต่มี `node`/`unzip`)** เทียบ `reference_templates/8_จำนวนนักเรียน
  พิการ.xlsx` (ชีท 8.2) กับ `reference_templates/11_ข้อมูลการศึกษา_กศน.xlsx` (ชีท 11.3) พบว่า **10
  ประเภทความพิการ ใช้ชื่อเดียวกันเป๊ะทั้ง 2 ฟอร์ม** (ทางการมองเห็น, ทางการได้ยิน, สติปัญญา, ร่างกาย/
  สุขภาพ, ทางการเรียนรู้, ทางการพูดและภาษา, พฤติกรรมหรืออารมณ์, ออทิสติก, พิการซ้ำซ้อน, อื่น ๆ ไม่ระบุ)
  — รวมยอดผ่าน key ชื่อเดียวกันได้ตรง ๆ ใน `Reporting::sumByColumnPathParts()`
* **ความละเอียดของข้อมูลไม่เท่ากัน 2 ประเภท**: ฟอร์ม 8.2 แตกคอลัมน์ย่อย "ตาบอด"/"เลือนราง" ใต้
  "ทางการมองเห็น" และ "หูหนวก"/"หูตึง" ใต้ "ทางการได้ยิน" (ฟอร์ม 11.3 ไม่มีคอลัมน์ย่อยพวกนี้ มีแค่
  ระดับเดียว) ผลคือกราฟจะมีแท่ง "ทางการมองเห็น"/"ทางการได้ยิน" (จากฟอร์ม 11.3 ล้วน ๆ) แยกต่างหากจาก
  แท่ง "ทางการมองเห็น / ตาบอด" ฯลฯ (จากฟอร์ม 8.2) ไม่ได้รวมเป็นแท่งเดียวกัน — ยอมรับตามที่ผู้ใช้งานขอ
  ไม่ได้ยุบระดับย่อยของ 8.2 ทิ้งเพื่อให้รวมกันสนิท
* แก้ที่ `public_report_data.php` จุดเดียว (`$disabilityByType`) — วน merge ผลจาก
  `sumByColumnPathParts('11_nfe', '11.3', 0, 1, $selectedYear)` เข้า array เดิมของ 8.2 ก่อน
  `arsort()` ไม่ต้องแก้ `$metrics`/`$genderSheets`/ตารางรายสถานศึกษาใด ๆ เพราะเป็นกราฟเดี่ยว ไม่ผูกกับ
  ยอดรวมอื่น
* ทดสอบไม่ได้จริงบนเครื่องนี้เช่นกัน (ไม่มี PHP) ตรวจแค่ syntax balance + โครงสร้างคอลัมน์จาก .xlsx จริง

### แยก "ผู้เรียน สกร." (ฟอร์ม 11.1) เป็นคอลัมน์ตามกิจกรรมการศึกษาในตารางผู้เรียนรายชั้น (เพิ่มเมื่อ 2026-09-01)
ผู้ใช้ขอให้ตารางผู้เรียนรายชั้น (`public_school_grade_table*.php`) แยกคอลัมน์ "ผู้เรียน สกร." (เดิมเป็น
ยอดรวมเดียว 1 กลุ่ม ชาย/หญิง) ออกเป็นคอลัมน์ตามกิจกรรมการศึกษาของฟอร์ม 11.1 จริง (เช่น "การส่งเสริมการ
รู้หนังสือ", "ประถม", "ม.ต้น" ฯลฯ) นำหน้าด้วย "สกร.-" และไม่ต้องโชว์คอลัมน์ที่ไม่มีข้อมูล พร้อมขอให้
ตรวจสอบว่ายอดรวมผู้เรียนของตารางนี้ตรงกับยอดรวมหน้าภาพรวมหรือไม่
* **ตรวจโครงสร้างคอลัมน์จริงของฟอร์ม 11.1 ด้วย `unzip`+`node` อ่าน worksheet XML ดิบ** (เครื่องนี้ไม่มี
  PHP) พบว่ามี **13 กิจกรรม × 2 เพศ = 26 คอลัมน์ข้อมูล** (ไม่มีคอลัมน์ "รวม" ปน) หลายกิจกรรม header
  แตกเป็น 2 ระดับ (เช่น "มัธยมศึกษา" + "ตอนต้น" รวมเป็น column_path "มัธยมศึกษา / ตอนต้น / ชาย")
  ต้องดู merge cell จริงในไฟล์ (`<mergeCells>`) ประกอบเพราะ script อ่าน XML ดิบธรรมดาไม่ merge-fill
  ให้เหมือน `XlsxReader.php` — สังเกตคอลัมน์ลำดับที่ 10 ในแม่แบบสะกดขาดคำว่า "กระ" ("การบวนการเรียนรู้
  ตามแนวปรัชญาเศรษฐกิจพอเพียง" แทนที่จะเป็น "กระบวนการ...") เข้าใจว่าเป็นการพิมพ์ผิดในแม่แบบเอง แก้คำ
  สะกดเฉพาะ label ที่แสดงผล แต่ column_path ที่ค้นหาข้อมูลจริงใน DB ยังคงสะกดตามต้นฉบับเป๊ะ
* **`public_school_grade_table_data.php`**: เพิ่ม `$nfeActivityGroups` (label => [column_path เพศชาย,
  เพศหญิง]) แบบเดียวกับ `$gradeGroups` ของฟอร์ม 4 — ปลอดภัยที่จะ hardcode เพราะฟอร์ม 11 ไม่มี
  `merge_extra_columns_into` เหมือนกัน เปลี่ยน field เก็บข้อมูลต่อแถวจาก `nfe_total` (male/female
  เดี่ยว) เป็น `nfe_by_activity` (array ตาม label) `grade_table_blank_row()` รับพารามิเตอร์เพิ่ม
  `$nfeActivityLabels` ต้องแก้ทุกจุดที่เรียก (5 จุด) ให้ตรงกัน — loop รวมข้อมูลของ section 3 (ฟอร์ม
  11.1) เปลี่ยนจากวน `$pivot111['columns']` ทั้งหมดด้วย regex ปลายชื่อ เป็น lookup ตรงทีละกิจกรรม
  ผลรวมข้าม 13 กิจกรรมเท่ากับของเดิมเป๊ะ (ยังครอบคลุมทุกคอลัมน์เหมือนเดิม แค่แยกเก็บเป็นก้อนย่อย) จึง
  ไม่กระทบยอดรวม
* **ไม่โชว์คอลัมน์ที่ไม่มีข้อมูล**: เพิ่ม `$nfeActivityLabelsShown` (กรองเฉพาะกิจกรรมที่มีผลรวม > 0
  อย่างน้อย 1 สถานศึกษา) คำนวณจากข้อมูล**ทั้งหมดก่อนกรองค้นหา** (แพทเทิร์นเดียวกับ
  `$agencyOptions`/`$amphoeOptions` ที่มีอยู่แล้ว) เพื่อให้ชุดคอลัมน์ที่แสดงคงที่ ไม่เปลี่ยนไปมาตามตัว
  กรองค้นหาที่เลือกอยู่ — ใช้ `$nfeActivityLabelsShown` แค่ตอน**แสดงผล** (หน้าเว็บ+CSV) เท่านั้น ส่วน
  `grand_total`/`$gradeTotals` ยังรวมจาก `$nfeActivityLabels` (ครบทั้ง 13) เสมอ ไม่ใช่แค่ที่แสดง (ไม่มี
  ผลต่างเพราะกิจกรรมที่ซ่อนไว้มีค่า 0 อยู่แล้ว แต่เขียนให้ถูกหลักการเสมอ)
* **`public_school_grade_table.php`/`public_school_grade_table_export.php`**: แทนที่กลุ่มคอลัมน์
  "ผู้เรียน สกร." 2 คอลัมน์เดิม (ชาย/หญิง) ด้วย loop ตาม `$nfeActivityLabelsShown` (2 คอลัมน์ต่อ
  กิจกรรม) ทั้งหัวตาราง 2 แถว (`colspan=2` + แถวชาย/หญิงย่อย) และ CSV header — ต้อง loop
  `$nfeActivityLabelsShown` ด้วยลำดับเดียวกันทุกจุด (หัวตาราง 2 แถว + แถวข้อมูล + CSV) ไม่งั้นคอลัมน์
  จะเลื่อนไม่ตรงกัน
* **ตรวจสอบยอดรวมผู้เรียนตรงกับหน้าภาพรวมตามที่ผู้ใช้งานขอ**: เทียบ `$metrics['students']['sheets']`
  ใน `public_report_data.php` (8 รายการ: ฟอร์ม 4, ฟอร์ม 14 เฉพาะแถว ศพด. จริง, ฟอร์ม 15×4 ชีท, ฟอร์ม
  11.1, ฟอร์ม 16.2) กับ 5 ส่วนที่รวมเข้า `rowsByCode` ใน `public_school_grade_table_data.php` — ตรงกัน
  ครบทุกฟอร์ม/ชีท/คอลัมน์/filter ทุกจุด (ฟอร์ม 11.1 หลังแก้ยังคงรวมทุก 26 คอลัมน์เหมือนเดิม แค่แยกเก็บ
  เป็นรายกิจกรรมแทนยอดเดียว) ไม่พบจุดหลุดแบบที่เคยเกิดกับฟอร์ม 14 มาก่อน — ทดสอบจริงไม่ได้เพราะเครื่อง
  นี้ไม่มี PHP ตรวจแค่ syntax balance + เทียบรายการ sheet/column ด้วยมือ

### รวมฟอร์ม 11.2 (ผู้เรียนพิการ กศน.) เข้ากับ 11.1 (ผู้เรียนปกติ) เป็นยอดผู้เรียนเดียวกัน (เพิ่มเมื่อ 2026-09-01)
ผู้ใช้ถามว่าควรเอาฟอร์ม 11.2 มารวมกับ 11.1 ในตารางผู้เรียนรายชั้นหรือไม่ เพราะเป็น "จำนวนผู้เรียน"
เหมือนกัน — **ตอบว่าใช่ ถูกต้อง แต่เป็นคนละสถานการณ์กับ 11.2/11.3 ที่คุยกันไปก่อนหน้านี้** (11.2/11.3
ห้ามบวกกันเพราะเป็นข้อมูลกลุ่มผู้เรียนพิการชุดเดียวกัน มองคนละมิติ บวกแล้วนับซ้ำ) ส่วน 11.1/11.2 คือ
**คนละกลุ่มผู้เรียนที่ไม่ทับซ้อนกันเลย** — สังเกตจากชื่อเต็มของชีท: 11.1 = "จำนวนผู้เรียน**ปกติ**..."
ส่วน 11.2 = "จำนวนผู้เรียน**พิการ**..." คำว่า "ปกติ" ในชื่อ 11.1 เป็นตัวบ่งชี้ชัดเจนว่าแบบฟอร์มนี้ตั้งใจ
แยกเก็บผู้เรียน 2 กลุ่ม (ปกติ/พิการ) คนละตาราง ไม่ใช่ 11.2 เป็น subset ของ 11.1 — ยืนยันแพทเทิร์นนี้ด้วย
ฟอร์ม 11.4/11.5 (ผู้สำเร็จการศึกษา) ที่แยก "ปกติ"/"พิการ" แบบเดียวกัน ตรวจโครงสร้างคอลัมน์ของ 11.2 เทียบ
11.1 ด้วย unzip+node แล้วเหมือนกันทุกประการ (13 กิจกรรม×2 เพศเท่ากัน) จึงบวกรวมกันได้ปลอดภัยทั้งทาง
เทคนิคและความหมายข้อมูล
* **`public_report_data.php`**: เพิ่ม `['11_nfe', '11.2', null]` เข้าทั้ง `$metrics['students']['sheets']`
  และ `$genderSheets` (ต่อจาก 11.1) — **11.2 ยังคงอยู่ใน `$metrics['disability']` เดิมด้วย ไม่ได้ย้าย
  ออก** เพราะเป็นคนละคำถาม: metric `students` ตอบ "มีผู้เรียนทั้งหมดกี่คน" ส่วน `disability` ตอบ "ในนั้น
  มีผู้พิการกี่คน" ผู้เรียนกลุ่มเดียวกันตอบ 2 คำถามพร้อมกันได้ ไม่ใช่การนับซ้ำภายใน metric เดียว
* **`public_school_grade_table_data.php`**: section 3 (เดิมวนแค่ pivot ของ '11.1') เปลี่ยนเป็น
  `foreach (['11.1', '11.2'] as $nfeSheetName)` วนทั้ง 2 ชีท สะสมยอดเข้า `nfe_by_activity` ก้อนเดียวกัน
  (ไม่แยกคอลัมน์ปกติ/พิการในตารางนี้ เพราะต้องการแค่ยอดรวมผู้เรียนต่อกิจกรรม ไม่ใช่แยกตามสถานะความพิการ)
  ใช้ `$nfeActivityGroups` ชุดเดียวกันกับ 11.1 เพราะโครงสร้างคอลัมน์เหมือนกัน
* **บทเรียน**: ทุกครั้งที่เจอฟอร์มคู่ "ปกติ"/"พิการ" (หรือคำคู่ตรงข้ามอื่นที่คล้ายกัน) ให้เช็คชื่อเต็มของ
  ชีทก่อนตัดสินใจว่าจะบวกรวมกันได้ไหม — ถ้าเป็นคนละกลุ่มประชากรที่ไม่ทับซ้อนกัน (เช่น "ปกติ" vs
  "พิการ") บวกกันได้ปลอดภัย แต่ถ้าเป็นข้อมูลกลุ่มเดียวกันที่แค่แยกนำเสนอคนละมิติ (เช่น 11.2 แยกตาม
  กิจกรรม vs 11.3 แยกตามประเภทความพิการ ของกลุ่มผู้เรียนพิการกลุ่มเดียวกัน) ห้ามบวกกันเด็ดขาด

### เพิ่ม section "ข้อมูลครู" (ตารางที่ 10.3-10.5) ในหน้ากราฟสาธารณะ แยกจากข้อมูลผู้เรียน (เพิ่มเมื่อ 2026-09-01)
ผู้ใช้งานขอให้แสดงจำนวนครูตามตารางที่ 10.3 (แยกตามอันดับ/วิทยฐานะ), 10.4 (แยกตามตำแหน่งทางวิชาการสาย
อว.), 10.5 (แยกตามวุฒิการศึกษาสูงสุด) ใน `public_report.php` ทั้งแบบตารางและกราฟ โดยไม่ให้ปนกับข้อมูล
ผู้เรียนที่มีอยู่เดิม (หน้านี้ก่อนหน้านี้ไม่มีการแบ่ง section ชัดเจน การ์ดครู/นักเรียนเรียงต่อกันเรื่อย ๆ
ตามลำดับที่เขียนโค้ด ไม่มีจุดคั่นให้สังเกต)
* **ตรวจโครงสร้างคอลัมน์จริงของทั้ง 3 ชีทด้วย python (`zipfile`+`xml.etree.ElementTree`) อ่าน
  `reference_templates/10_ข้อมูลครูV2.xlsx` ตรง ๆ** (เครื่องนี้ไม่มี PHP) พบว่าทั้ง 3 ชีทมีแค่ 2 ระดับ
  หัวตาราง "<หมวด> / เพศ" ล้วน ไม่มีคอลัมน์ "รวม" ปน — ต่างจากกราฟอื่นในไฟล์นี้ตรงที่ผู้ใช้งานต้องการ
  **ทั้งยอดรวมต่อหมวด (กราฟ) และยอดแยกชาย/หญิงต่อหมวด (ตาราง) พร้อมกัน** ซึ่ง
  `Reporting::sumByColumnPathParts()` เดิมมีแต่ตัวเลือกรวมเพศเข้าด้วยกันหรือทิ้งกลุ่มไปเลย ไม่รองรับ
* **`src/Reporting.php`**: เพิ่มเมธอด `sumByColumnPathPartsWithGender($formKey, $sheetName, $dropFirst,
  $academicYear)` — โครงสร้าง input เดียวกับ `sumByColumnPathParts()` แต่เก็บ level สุดท้าย (ชาย/หญิง)
  แยกไว้แทนที่จะตัดทิ้ง คืนเป็น `หมวด => ['male'=>.., 'female'=>..]` ข้ามคอลัมน์ที่ level สุดท้ายไม่ใช่
  ชาย/หญิง (กันคอลัมน์รวมชนิดอื่นที่อาจมีในอนาคต ไม่ error แต่ข้ามเงียบ ๆ เหมือน sibling เดิม)
* **`public_report_data.php`**: เพิ่ม `$teacherByRank`/`$teacherByAcademicRank`/`$teacherByEducation`
  (เรียกชีท 10.3/10.4/10.5 ด้วย `dropFirst=0`) + ฟังก์ชันช่วย `sort_gender_breakdown_desc()` (เรียง
  หมวดตามยอดรวมมากไปน้อย ใช้ร่วมกันทั้งตาราง/กราฟให้ลำดับตรงกัน), `totals_from_gender_breakdown()`
  (แปลงเป็น หมวด=>ยอดรวม ให้ `render_bar_chart()` เดิมใช้ต่อได้ตรง ๆ ไม่ต้องเขียนกราฟใหม่), และ
  `render_gender_breakdown_table_and_chart()` (ตาราง หมวด/ชาย/หญิง/รวม ต่อด้วยกราฟแท่งยอดรวมในการ์ด
  เดียวกัน — ใช้ร่วมกันทั้ง 3 การ์ด ต่างกันแค่ข้อมูล/ชื่อคอลัมน์หมวดแรก)
* **`public_report.php`**: เพิ่มหัวข้อคั่น section แบบใหม่ (`.section-divider`) แยก "ข้อมูลผู้เรียน"
  (ก่อนกราฟผู้เรียนเดิมทั้งหมด) กับ "ข้อมูลครู" (การ์ดใหม่ 3 ใบ ต่อท้ายสุด) + anchor link เลื่อนหน้าจอ
  ไปแต่ละ section ไว้บนสุดของหน้า — เป็น section header ระดับหน้าตัวแรกของระบบ (เดิมมีแต่ `<h2>` ระดับ
  การ์ดเดี่ยว ไม่มีตัวคั่นกลุ่มใหญ่) ถ้าจะเพิ่ม section ใหญ่อื่นในอนาคตให้ใช้ pattern เดียวกันนี้ต่อได้
  เลย (ดู `.section-divider` ใน `public/assets/public_report.css` — accent สีต่อ topic กำหนดผ่าน CSS
  var `--section-accent` ต่อ modifier class `.students`/`.teachers`)
* ทดสอบไม่ได้จริงบนเครื่องนี้เช่นกัน (ไม่มี PHP) ตรวจแค่ syntax balance (brace/paren count) +
  โครงสร้างคอลัมน์จาก .xlsx จริง ตามแพทเทิร์นเดิมของ session อื่น ๆ ในไฟล์นี้

### เพิ่มข้อมูลประชากรรายอายุ (กรมการปกครอง) + คำนวณอัตราการเข้าเรียน (เพิ่มเมื่อ 2026-09-01)
ผู้ใช้งานอยากแสดงจำนวนผู้เรียนเทียบกับประชากรวัยเรียนจริง แยกช่วงอายุ 5 ช่วง (3-5, 6-11, 12-14, 15-17,
18-19 ปี) x อำเภอ โดยให้อัปโหลดไฟล์ข้อมูลประชากรจาก stat.bora.dopa.go.th (กรมการปกครอง) ได้ทุกปี — คนละ
แหล่งข้อมูลกับฟอร์มสำรวจ 1-16 ที่หน่วยงานกรอกส่งทั้งหมด

**⚠️ บทเรียนสำคัญที่สุดของงานนี้ — เกือบ ship โค้ดที่นับประชากรขาดไป 17% ทั้งจังหวัด**: ไฟล์ต้นทาง (pipe-
delimited, 1 บรรทัด/1 พื้นที่) มีบล็อก "อำเภอ..." + ตำบลลูกเรียงมาก่อน แล้วมีอีกบล็อกแยกต่างหากท้ายไฟล์
ขึ้นต้นด้วย "เทศบาลตำบล.../เทศบาลเมือง..." + ตำบลลูกของเทศบาลนั้น (บางชื่อตำบลซ้ำกับที่เห็นในบล็อกอำเภอ
บนสุด บางชื่อไม่เคยเห็นมาก่อนเลย) **สมมติฐานแรกที่เขียนโค้ดไปคือ "ชื่อตำบลซ้ำ = ข้อมูลซ้ำ ต้องข้าม กัน
นับซ้ำ"** — ฟังดูสมเหตุสมผล แต่**ผิด** ตรวจสอบด้วยไฟล์จริงของจังหวัดปัตตานีแล้ว: ยอดรวม 12 อำเภอ
(618,143 คน) + ยอดตำบลในเขตเทศบาลที่ชื่อไม่ซ้ำเท่านั้น (97,752 คน) = 715,895 คน **ยังขาดจากยอดรวม
จังหวัดจริง (744,971 คน) อยู่ 29,076 คน** พิสูจน์ด้วยการรวมยอด "เทศบาล..." header ทั้ง 12 กลุ่มตรง ๆ
(126,828 คน) เทียบกับ 618,143+126,828 = 744,971 **ตรงเป๊ะ** — สรุปว่า**ตำบลในเขตเทศบาลเป็นคนละกลุ่ม
ประชากรที่ไม่ทับซ้อนกับตำบลชื่อเดียวกันในบล็อกอำเภอเลย แม้ชื่อจะซ้ำกัน** (คือ "ในเขตเทศบาล" vs "นอกเขต
เทศบาล" ของตำบลเดียวกัน คนละกลุ่มคน) **ต้องบวกทุกแถวในบล็อกเทศบาลเสมอ ไม่มีข้อยกเว้นจากชื่อซ้ำ** — ถ้า
ไม่ได้ลองตรวจสอบยอดรวมระดับจังหวัดเทียบกับผลลัพธ์ก่อน (ตามที่ผู้ใช้งานขอไว้ตอนออกแบบ "auto-check ก่อน
บันทึก") จะไม่มีทางรู้เลยว่าอัลกอริทึมแรกผิด เพราะโค้ดรันได้ไม่ error แค่ตัวเลขผิดเงียบ ๆ — **บทเรียน
ทั่วไป**: ข้อมูลจากแหล่งภายนอกที่มีโครงสร้างซับซ้อน (โดยเฉพาะที่ "ดูสมเหตุสมผล" แต่ไม่เคยเห็นเอกสาร
ทางการอธิบายโครงสร้างจริง) ต้อง**ตรวจสอบยอดรวมระดับบนสุดเทียบกับผลลัพธ์ทุกครั้งก่อนเชื่อ** ไม่ใช่แค่เขียน
โค้ดที่ "ดูสมเหตุสมผล" แล้วเชื่อเลย — ดู reconciliation logic เต็มที่ `src/PopulationImporter.php`
(มี unit-test-style validation ด้วย python สคริปต์แยกไว้นอก repo ระหว่างพัฒนา ไม่ได้เก็บสคริปต์)

* **โครงสร้างไฟล์ต้นทาง**: `ชื่อพื้นที่|อายุ0_ชาย|อายุ0_หญิง|...|อายุ100_ชาย|อายุ100_หญิง|(หมวดพิเศษ
  4 ชุด ไม่ใช้)|รวมชาย|รวมหญิง|รวมทั้งหมด` — field ที่ 220 (1-indexed) เป็นยอดรวมทั้งหมดเสมอ ตรวจสอบ
  ด้วย python (`zipfile` ใช้ไม่ได้เพราะเป็น .txt ธรรมดา ใช้ `curl`+`awk`/python string split แทน)
* **`src/PopulationImporter.php`** (คลาสใหม่ ไม่ผูกกับ Reporting/Importer เดิม เพราะเป็นคนละ
  แหล่งข้อมูล/โครงสร้างไฟล์คนละแบบ): `parse()` คืนยอดต่ออำเภอ 5 ช่วงอายุ x เพศ + รายงานผล (อำเภอไหน
  บวกเพิ่มเท่าไหร่จากเขตเทศบาล, ตำบลไหนหาอำเภอไม่เจอ, คำเตือนถ้ายอดรวมไม่ตรงกับยอดจังหวัดในไฟล์) —
  ตำบลในเขตเทศบาลหาอำเภอเจ้าของด้วยการเทียบชื่อกับ **ทำเนียบโรงเรียน (schools_master) ปีเดียวกัน**
  (`buildTambonLookup()`) เพราะไฟล์ประชากรเองไม่บอกว่าตำบลกลุ่มนั้นเป็นของอำเภอไหน — ถ้าทำเนียบยังไม่มี/
  ไม่ครบ ตำบลนั้นจะไปโผล่เป็น "unresolved" ให้แอดมินเห็นชัดเจน ไม่ใช่นับหายไปเงียบ ๆ
* **`migrations/006_population_by_age.sql`**: ตารางใหม่ `population_by_age` (ปี, อำเภอ, ชาย/หญิง x
  5 ช่วงอายุ) แบบ wide column เหมือน schools_master (ไม่ใช่ key-value แบบ submissions เพราะช่วงอายุคงที่
  ไม่ผันแปรเหมือนคอลัมน์ของฟอร์มสำรวจ) คีย์หลัก (academic_year, amphoe) — อัปโหลดใหม่ = แทนที่เฉพาะปีนั้น
* **`population_upload.php`** (แอดมิน, ต้อง login): เหมือน `schools_master.php` ทุกประการ (upload,
  แทนที่ทั้งปี, ลบทั้งปี) เพิ่มส่วนแสดงรายงานผลการนำเข้าหลังอัปโหลดทุกครั้ง (อำเภอที่บวกเพิ่ม/ตำบลที่หา
  อำเภอไม่เจอ/คำเตือนยอดไม่ตรง) — **ต้องอัปโหลดทำเนียบโรงเรียนปีเดียวกันไว้ก่อนเสมอ** ไม่งั้นตำบลในเขต
  เทศบาลทั้งหมดจะ unresolved
* **`public_population.php`** (สาธารณะ หน้าใหม่ "ประชากรวัยเรียน" ในเมนู — เพิ่ม nav link ใน
  `render_report_start()` ของ `public_report_data.php` ทำให้ public_report.php/public_report_table.php
  ได้ลิงก์ไปด้วยอัตโนมัติ + เพิ่มมือใน public_school_grade_table.php/public_teacher_grade_table.php/
  public_school_search.php ที่มี nav แยกก้อนของตัวเอง ไม่ใช้ฟังก์ชันร่วม): ตารางประชากร x ผู้เรียนจริง x
  อัตราการเข้าเรียน 4 ช่วงแรก (ไม่รวม 18-19 ปี ตามคำขอผู้ใช้งาน — ระดับอุดมศึกษาอายุจริงคลาดเคลื่อนจาก
  ชั้นปีมากกว่าระดับอื่นมาก ไม่แม่นพอจะนำเสนอเป็นอัตรา แสดงแค่จำนวนประชากรอย่างเดียว) + กราฟแท่งอัตราการ
  เข้าเรียนต่อช่วงอายุ
* **`src/Reporting.php`**: เพิ่ม `sumByColumnPathPartsByDimension()` — เหมือน `sumByColumnPathParts()`
  (drop header level หน้า/หลังตามจำนวนที่กำหนด ใช้ส่วนที่เหลือเป็น category) แต่ cross-tab ด้วย dimension
  (อำเภอ) ด้วย แทนที่จะรวมเป็นยอดเดียวทั้งจังหวัด — ใช้จับคู่ระดับชั้นฟอร์ม 4 เข้าช่วงอายุ x อำเภอ โดย
  **ไม่ hardcode ชื่อคอลัมน์เอง** (จับคู่ด้วย regex บน category ที่ระบบแยกให้อัตโนมัติแทน เช่น
  `/อนุบาล\s*[123]/u` กันไม่ให้ปนกับ "เตรียมอนุบาล" ที่ไม่มีเลขต่อท้าย, `/มัธยมศึกษาตอนต้น/u` ต้องคง
  dropFirst=0 ไว้ไม่งั้นแยกต้น/ปลายไม่ได้เพราะแถวย่อยกว่าเขียนแค่ "มัธยมศึกษา" เหมือนกันทั้งคู่) — ดู
  comment เต็มที่ `public_population.php` **regex พวกนี้ตรวจกับแม่แบบเปล่าเท่านั้น (ยังไม่เคยรันกับข้อมูล
  จริงที่อัปโหลดแล้ว) ถ้าช่วงอายุไหนออกมาเป็น 0 ทั้งจังหวัดหลัง deploy ให้สงสัยจุดนี้ก่อน**
* **เกณฑ์จับคู่ระดับชั้น→ช่วงอายุ** (ผู้ใช้งานยืนยันแล้ว): 3-5 ปี = อนุบาล 1-3 เท่านั้น (ไม่รวมเตรียม
  อนุบาล/เด็กเล็ก กันซ้ำกับข้อมูล ศพด. ฟอร์ม 14 ที่ใช้แยกอยู่แล้ว), 6-11 ปี = ประถม 1-6, 12-14 ปี =
  ม.ต้น 1-3, 15-17 ปี = ม.ปลาย 1-3 + ปวช. 1-3, 18-19 ปี = ไม่คำนวณอัตรา (แสดงประชากรอย่างเดียว)
* ทดสอบไม่ได้จริงบนเครื่องนี้เช่นกัน (ไม่มี PHP) — แต่**ตรวจ reconciliation algorithm ด้วย python
  reimplementation กับไฟล์ประชากรจริงของจังหวัดปัตตานีจนยอดรวมตรงกับยอดจังหวัดในไฟล์เป๊ะ (744,971 คน,
  unresolved=0) ก่อนพอร์ตกลับมาเป็น PHP** ไม่ใช่แค่เขียน PHP เดาแล้วปล่อยผ่าน — ควรทำแบบนี้ทุกครั้งที่
  เขียน parser สำหรับไฟล์จากแหล่งภายนอกที่มีโครงสร้างซับซ้อน/ไม่มีเอกสารทางการ

### ปักหมุดเมนู (topbar) ไว้บนสุดเสมอ + แก้บั๊ก sticky header ชนกัน 2 รอบ + กราฟแท่งบนมือถือ (แก้เมื่อ 2026-09-01)
ผู้ใช้งานขอให้เมนูบนสุด (topbar) ไม่เลื่อนหายไปพร้อมเนื้อหาตอน scroll หน้ารายงานยาว ๆ (สับสนต้องเลื่อน
กลับขึ้นบนสุดทุกครั้งกว่าจะสลับหน้าได้) แก้ที่ `public/assets/style.css` เพราะ `.topbar` เป็น class
ร่วมที่ทุกหน้าในระบบใช้ (แก้จุดเดียวมีผลทุกหน้าทั้งแอดมิน+สาธารณะ):
* `.topbar { position: sticky; top: 0; z-index: 100; }` + เงาบาง ๆ (`box-shadow`) ให้ดูลอยเหนือเนื้อหา
  เสมอโดยไม่ต้องพึ่ง JS ตรวจ scroll + ปรับดีไซน์ลิงก์ active ให้เป็น pill พื้นหลังแทนแค่ตัวหนา (ลบ
  `.topbar nav a.active` เดิมที่ซ้ำอยู่ใน `public_report.css` ออก ให้เหลือกฎเดียวใน `style.css`)

**บั๊กที่ตามมา (สำคัญ ถ้าเจออีกให้อ่านตรงนี้ก่อน)**: `th { position: sticky; top: 0 }` เดิมในระบบ
(มีมาก่อนงานนี้) ใช้กับตารางที่ห่อด้วย `.table-scroll` (แดชบอร์ด + หน้าแอดมินแทบทุกหน้า) — ตอนแรกคิดว่า
แค่เปลี่ยน `top: 0` เป็น `top: var(--topbar-h)` (58px) กันหัวตารางไปซ้อนทับใต้ topbar ที่ sticky ใหม่ก็
พอ **แต่กลับทำให้หัวตารางเลื่อนหล่นลงมาทับแถวข้อมูลแรกทันทีที่โหลดหน้า ไม่ต้อง scroll เลย** (ผู้ใช้งานส่ง
ภาพหน้าจอมาเจอ) — ตรวจสอบแล้วสาเหตุคือ **`.table-scroll` ตั้ง `overflow-x: auto` อย่างเดียว แต่สเปก CSS
บังคับให้กล่องที่มี overflow แกนไหนแกนหนึ่งไม่ใช่ visible กลายเป็น "scroll container" ของตัวเองไปทั้ง
กล่อง (ทั้ง 2 แกน ไม่แยกทีละแกน)** แม้จะตั้ง `overflow-y: visible` ตรง ๆ ก็ไม่ช่วย (ลองแล้วในคอมมิทแรก
ไม่ได้ผล สเปกบังคับ compute ทับอยู่ดี) — ผลคือ sticky `th` ยึดตำแหน่งกับกล่อง `.table-scroll` เอง (ไม่มี
`max-height` ให้ scroll แนวตั้งจริง สูงเท่าเนื้อหาเสมอ) ไม่ใช่กับหน้าเว็บทั้งหน้า `top` ที่ไม่ใช่ 0 จึงดัน
หัวตารางลงมาทับแถวข้อมูลทันที (top:0 เดิมบังเอิญไม่โชว์ปัญหานี้เพราะไม่ต้องดันไปไหน) **สรุปคือหัวตาราง
กลุ่มนี้ไม่เคย sticky ตามหน้าเว็บได้จริงตั้งแต่แรกอยู่แล้ว** — แก้จริงคือถอด `position: sticky` ออกจาก
`th` เปล่านี้ไปเลย (ไม่กระทบพฤติกรรมที่เคยเห็นจริงเพราะมันไม่เคยทำงานถูกอยู่แล้ว) ตารางที่ต้องการ sticky
header แบบเห็นผลจริงต้องใช้ `.report-table-scroll` แทน (มี `max-height` + `overflow` ทั้ง 2 แกนจริง สร้าง
scroll context ของตัวเองอย่างตั้งใจ ดู `.stats-table.two-row-header` ใน `public_report.css`)
* **บทเรียน**: `overflow-x: auto` เพียงแกนเดียว ก็ทำให้ element กลายเป็น containing block ของ
  `position: sticky`/`fixed` ลูกหลานไปทั้งกล่องแล้ว ไม่ใช่แค่แกนที่ตั้งไว้ — ถ้าจะให้ลูกหลาน sticky
  ทะลุออกไปยึดกับหน้าเว็บ/ancestor ที่สูงกว่าได้ ต้องไม่มี ancestor ไหนเลยระหว่างทางตั้ง overflow (แกน
  ไหนก็ได้) เป็นค่าอื่นนอกจาก visible — แก้ที่ `overflow-y` ของ ancestor นั้นไม่ช่วยถ้า `overflow-x`
  ของมันยังไม่ visible อยู่ดี

**กราฟแท่งบนมือถือ**: ผู้ใช้งานส่งภาพหน้าจอ iPhone มาด้วย พบว่า `.bar-label` (240px ตายตัว) +
`.bar-value` (72px ตายตัว) รวมกันเกินความกว้างจอที่เหลือหลังหัก padding ของ `.container`/`.card` บนจอ
มือถือ (~390px เหลือใช้ได้จริงไม่ถึง 320px) ทำให้ `.bar-wrap` (แท่งกราฟ) ถูกบีบจนความกว้างเหลือเกือบ 0
มองไม่เห็นแท่งเลย เห็นแต่ป้ายชื่อ+ตัวเลขไปกองชิดขวาจอ — แก้ด้วย `@media (max-width: 480px)` สลับ
`.bar-row` เป็น `flex-wrap: wrap` แล้วให้ `.bar-label` กว้างเต็ม 100% (บังคับขึ้นบรรทัดใหม่ ชิดซ้ายอ่าน
ง่ายกว่าชิดขวาตอนวางเดี่ยว ๆ เต็มบรรทัด) แท่ง+ค่าตัวเลขตกไปอยู่บรรทัดถัดมาแทน ครอบคลุมทั้งกราฟทั่วไปและ
กราฟในคอลัมน์แคบ (`.kpi-col .bar-label`/`.kpi-col .bar-value` — เช่น 5 อันดับสูงสุด/ต่ำสุด) ด้วย ไม่ต้อง
คำนวณ pixel budget ให้พอดีทุกขนาดจอเหมือนวิธีเดิม
* **บทเรียนทั่วไป**: กราฟ/ตารางที่ทดสอบแค่บนจอเดสก์ท็อประหว่างพัฒนา (เครื่องนี้พรีวิวจริงไม่ได้เลยเพราะ
  ไม่มี PHP) ต้องคิดเผื่อความกว้างจอมือถือด้วยเสมอ โดยเฉพาะ layout ที่ใช้ fixed px width หลายคอลัมน์ใน
  แถวเดียว (label+bar+value) เพราะ breakpoint ที่พังมักไม่ error ให้เห็นชัดเจน แค่บีบจนมองไม่เห็นเนื้อหา
  สำคัญไปเงียบ ๆ (เหมือนบั๊ก sticky ข้างบนที่ก็ "รันได้ไม่ error" แต่ผลลัพธ์ผิด)

### ข้อจำกัดของเครื่องมือพัฒนา (สำหรับ AI ที่มาทำงานต่อ)
* Mac เครื่องที่พัฒนาระบบนี้ไม่มี PHP ติดตั้ง (macOS ไม่ bundle PHP มาให้แล้ว และไม่มี Homebrew)
  จึงไม่เคยรัน PHP จริงบนเครื่อง dev เลยตลอดการพัฒนา — โค้ดทุกไฟล์เขียนแบบตรวจสอบด้วยมืออย่าง
  ละเอียด แต่ **ยังไม่เคย execute จริง** จนกว่าจะ deploy ขึ้น Plesk ถ้าเจอ syntax error หรือ edge
  case แปลก ๆ ตอน deploy ครั้งแรกของแต่ละไฟล์ ให้ตรวจสอบเป็นพิเศษ
  * ใช้ Perl (`Archive::Zip`) เป็นเครื่องมือช่วยตรวจโครงสร้าง .xlsx ระหว่างพัฒนาแทน (ไฟล์ scratch
    ไม่ได้เก็บไว้ใน repo)
  * ถ้าเครื่องมี Xcode Command Line Tools ติดตั้งแล้ว `git`/`python3` จะใช้ได้ปกติ แต่ `php` ยังต้อง
    ติดตั้งแยกเอง (เช่นผ่าน Homebrew) ถึงจะรัน/lint ไฟล์ PHP ในเครื่องนี้ได้
* **โฟลเดอร์โปรเจกต์นี้ sync ผ่าน OneDrive** — OneDrive จะ flip permission bit ของไฟล์ที่มันแตะ
  (644 → 755) เป็นระยะโดยไม่เกี่ยวกับเนื้อหาไฟล์เลย ทำให้ `git status`/`git diff --summary` โชว์
  "mode change" ปนอยู่กับทุกไฟล์ในเครื่องแม้ไม่ได้แก้ไขอะไร (เจอครั้งแรกตอน commit section ข้อมูลครู
  2026-09-01 — เกือบทุกไฟล์ในเครื่อง repo ขึ้น modified หมด) ก่อน `git add` ไฟล์ที่ตั้งใจแก้จริง ให้
  เช็ก `git diff --summary -- <ไฟล์>` ก่อนเสมอ ถ้ามีแต่บรรทัด `mode change 100644 => 100755` ไม่มี
  content diff ให้ `chmod 644 <ไฟล์>` คืนก่อน commit เพื่อไม่ให้ mode change ปนเข้าไปในทุก commit
  โดยไม่ตั้งใจ — **ห้ามตั้ง `git config core.fileMode false`** (แก้ที่ระดับ config ทั้ง repo กระทบ
  ทุกคนที่ clone) ใช้วิธี chmod เฉพาะไฟล์ที่จะ commit จริงแทน

---

*อัปเดตล่าสุดเมื่อ: 2026-09-01 (ปักหมุดเมนู topbar ไว้บนสุดเสมอ + ปรับดีไซน์เมนู แก้บั๊ก sticky header
ของตาราง .table-scroll ชนกับ topbar ที่ sticky ใหม่ (2 รอบกว่าจะเจอสาเหตุจริง — overflow-x เพียงแกน
เดียวก็ทำให้กลายเป็น scroll container ทั้งกล่อง) + แก้กราฟแท่งบนมือถือจอแคบที่แท่งกราฟถูกบีบจนมองไม่เห็น
— ดูหัวข้อเต็มด้านบน "ปักหมุดเมนู (topbar)...")*

*อัปเดตก่อนหน้าเมื่อ: 2026-09-01 (เพิ่มหน้า "ประชากรวัยเรียน" — อัปโหลดข้อมูลประชากรรายอายุจากกรมการ
ปกครอง (population_upload.php) เทียบกับผู้เรียนจริงคำนวณอัตราการเข้าเรียนต่อช่วงอายุ x อำเภอ
(public_population.php) แก้บั๊ก reconciliation logic ที่เกือบทำให้นับประชากรขาดไป 17% ทั้งจังหวัด — ดู
หัวข้อเต็มด้านบน "เพิ่มข้อมูลประชากรรายอายุ...")*

*อัปเดตก่อนหน้านั้นเมื่อ: 2026-09-01 (เพิ่ม section "ข้อมูลครู" ในหน้ากราฟสาธารณะ — ตารางที่ 10.3/10.4/10.5
แยกตามอันดับ-วิทยฐานะ/ตำแหน่งทางวิชาการ/วุฒิการศึกษา ทั้งตาราง+กราฟ แยก section ชัดเจนจากข้อมูลผู้เรียน
ด้วยหัวข้อคั่นสี เพิ่มเมธอด Reporting::sumByColumnPathPartsWithGender() ใหม่)*

*อัปเดตก่อนหน้านั้นเมื่อ: 2026-08-29 (แก้ไฟล์ฟอร์ม 14 อัปโหลดไม่ผ่านเมื่อชื่อแท็บชีทเป็นชื่อเก่า
"14.สถานพัฒนาเด็กปฐมวัย" — กลับมาใช้ db_sheet_name/optional รับได้ทั้ง 2 ชื่อแท็บ)*

*อัปเดตก่อนหน้านั้นเมื่อ: 2026-08-28 (แก้บั๊กอัปโหลดฟอร์ม 10.1 ไม่ผ่านเพราะคอลัมน์ผีท้ายไฟล์ — แก้ที่
XlsxReader กลาง; เพิ่ม seq_no เรียงใหม่เองทุกตาราง; แทนที่สังกัด/อำเภอ/ตำบล ด้วยทำเนียบโรงเรียนเสมอ
เมื่อจับคู่ school_code ได้; เพิ่มดาวน์โหลดรวมทุกชีทของฟอร์มเดียวกัน (sheet=__all__) พร้อมแก้บั๊ก
tidyRows ว่างเปล่าสำหรับชีทไม่มี value column; เพิ่ม project_name ให้ฟอร์ม 12; เพิ่ม
merge_extra_columns_into ให้ฟอร์ม 10.6 รวมคอลัมน์ที่แทรกเพิ่มเข้ากับ "อื่นๆ" แทนบล็อก)*

*อัปเดตก่อนหน้านั้นเมื่อ: 2026-08-27 (ผู้ใช้งานส่งไฟล์ต้นฉบับเวอร์ชันใหม่ — ปรับโครงสร้างฟอร์ม 10 ใหม่
ทั้งฟอร์ม (5→6 ชีท), แก้ไขฟอร์ม 13 เล็กน้อย (ตัดคอลัมน์ซ้ำ), เปลี่ยนฟอร์ม 14 จากไฟล์คู่ 14ก+14ข เป็น
ไฟล์เดียว, เพิ่มฟอร์ม 15 เข้ามารองรับครบ 5 ชีทเป็นครั้งแรก — ตามคำขอผู้ใช้งาน ไม่ทำ versioning ตามปี
ข้อมูลปีเก่าของฟอร์ม 10/14 ที่เคยนำเข้าด้วยโครงสร้างเดิมจึงไม่โผล่ในหน้าเว็บอีกต่อไป; เพิ่มตารางสรุป
ยอดรวมทั้งจังหวัดแยกตามช่วงอายุในหน้าดูข้อมูลฟอร์ม 13 ผ่าน key ทั่วไป `summary_group_field`)*

*อัปเดตก่อนหน้านั้นเมื่อ: 2026-08-23 (เพิ่มฟอร์ม 13, ตัดฟอร์ม 15, เพิ่มการตรวจซ่อนแถว/คอลัมน์และตรวจ
โครงสร้างคอลัมน์เทียบต้นฉบับ, จัดกลุ่มแดชบอร์ดตามฟอร์ม, เพิ่มปุ่มลบทั้งไฟล์ในหน้าประวัติการอัปโหลด,
แก้บั๊กแถวหมายเหตุหลายบรรทัดหลุดเข้าเป็นข้อมูล, รวมฟอร์ม 14ก+14ข เป็นชุดข้อมูลเดียว, เพิ่มการตรวจสอบ
รหัสสถานศึกษาเทียบทำเนียบโรงเรียน (schools_master.php, migration 003), เพิ่มการรองรับหลายปีการศึกษา
ทั้งระบบ (academic_year, settings.php, migration 004 — ต้องรันก่อนอัปโหลดได้))*
